# nathanhaslewood.com.au · v1.39.1

Static site, built with AI assistance. No framework, no build step. Every file in this folder deploys as-is.

## Deploy
Hosted on Cloudflare Workers static assets, deployed automatically from `main` in this repository. Build command: none. Output directory: root. Config is in `wrangler.jsonc`; see the Cloudflare notes in CLAUDE.md before changing it.

Cloudflare caches at the edge, so a changed page can serve stale for a while after a deploy. That is caching, not a failed build. Purge in the dashboard before concluding something is broken.

## Preview locally
Links are root-absolute, so use a server: python3 -m http.server 8000

## Design system
Instrument Sans for everything a person reads (weights 400 to 700), JetBrains Mono for the machine layer. Three-reader palette: signal orange, cobalt, green, fused in the spectrum bar, hero aurora, and bento accents. Tokens in /assets/css/style.css.

## Generated imagery (wired in v1.3)
Live: gen-hero.jpg + gen-hero-loop.mp4 (home hero, video hidden on mobile and for reduced-motion users, poster fallback), gen-work.jpg (/work/), gen-systems.jpg (/systems/), gen-book.jpg (/book/). The home page og:image is gen-hero.jpg.
Pending: gen-writing.jpg (4:3, /writing/): generate with the original prompt, same filename, and it wires in one line.
Optional upgrade: regenerate stills at 2400px+ wide for very large screens; current masters are 1408px.

## Contact policy
No email address anywhere by design. All contact routes to LinkedIn.

## Follow-ups
- OG share image (1200x630) per page.
- September 2026: KDP Select auto-renew OFF now; on lapse, publish full book text and change /book/ status line.
- Second book: 2027 edition live in full at /super/ (done in v1.9). Download assets refreshed in v1.9.1 (PDF now carries the cover as page 1). The bold-label '.:' artifact is still in the supplied PDF/EPUB (77 hits) and still corrected on the site pages only; fix it in the manuscript source on the next regeneration. Verification register sign-off still pending.
- Analytics: Cloudflare Web Analytics or Plausible only. GA contradicts the footer promise.
- BankVic case study after 6 to 12 months, with permission.

## v1.4
- Full second book live at /super/: hub + 10 chapters + appendices page, each with prev/next, a general-information disclaimer strip, and Book structured data on the hub. PDF and EPUB downloads sit in /super/.
- All generated imagery wired: hero (regenerated), book (regenerated), gobiz, credit-cards, land-use, writing, super.
- Neutral text pass: brand colour removed from all text glyphs (links, hovers, eyebrows, numerals, wordmark dots). Colour now lives in the spectrum bar, aurora, dots and imagery only.
- Per-page og:image set from the artwork. Footer links to Super sitewide; book page and home band cross-promote it.
- The verification register (delivered separately) lists every regulatory figure in the book awaiting Nathan's confirmation; patch the text after sign-off.

## v1.5 (final)
- Header nav set in Instrument Sans and now includes Super, matching the footer.
- Footer and colophon claims corrected: static HTML built with AI assistance, no framework, no tracking, no cookies.
- Work index rows aligned on a fixed three-column grid (the tag column no longer floats per row).
- Origin corrected: WordPress, not AEP/Braze, and reframed around researching, creating, building and publishing Origin's first-ever AI-generated web pages. Cascaded to about, are-you-ai and the LinkedIn pack.
- Six colleague testimonials added: a wall on /about/, one solo quote on the home page.
- Book PDF rebuilt properly: xelatex, Roboto Light 10pt, real table of contents with page numbers, every chapter starting on a new page. EPUB rebuilt with navigation ToC.

## v1.9
- Second book replaced with the fully rewritten 2027 edition (post-reform): hub + 14 chapters in five parts + appendices A to O, all regenerated from the new manuscript. Same URL scheme where chapters carried over (right-for-you, when-to-walk-away, rules, numbers, borrowing, buying-process, compliance, endgame); six new chapter URLs (new-landscape, commercial-for-residential-investors, asset-class-tour, business-premises, gst-duty-and-money, residential-with-cash).
- Retired /super/residential-vs-commercial/ and /super/what-and-where-to-buy/; 301s in the new _redirects file.
- New PDF (148pp print interior) and EPUB dropped in under the existing download filenames. New cover at /assets/img/super-cover-2027.jpg, shown on the /super/ hub (headgrid + cover-photo, same pattern as /book/).
- New /updates/ corrections-log page, because the 2027 edition's disclaimers and About-this-edition page point readers at nathanhaslewood.com.au/updates.
- Hub TOC grouped by part (.toc-part rule added to style.css). Sitemap, llms.txt and the /book/ cross-promo updated. Known manuscript artifact '<strong>Label.:</strong>' corrected to 'Label.' on the site pages only; it remains in the PDF/EPUB pending a regeneration pass.

## v1.9.1
- Download assets and cover refreshed, no content change. New PDF (149pp, cover embedded as page 1, 8.1 MB) and new EPUB dropped in under the existing filenames. Cover regenerated to /assets/img/super-cover-2027.jpg (800x1280) from the new master. Hub links and img src already pointed at these paths, so no HTML changed.
- Note: the supplied PDF/EPUB still contain the '<strong>Label.:</strong>' artifact (77 instances). It stays corrected on the site pages only until the manuscript source is fixed.

## v1.9.2
- Added a Games nav item (header and footer, sitewide) linking to novlr.com in a new tab, positioned after Super and before About. Only outbound nav item that opens in a new tab. Note: the extra nav item overflows the header's horizontal-scroll band between 721px and 824px; fix is to raise the nav media query in style.css from 720px to 860px (not yet done).

## v1.9.3
- Removed the rotating hero typeline on the homepage (the three cycling "reader one/two/three" sentences) and its JS. It was causing vertical layout shift as it typed and deleted. The static three-readers lede above it carries the same message. Dead CSS (.typeline/.tdot/.cursor) left in place, inert.
- Redacted a specific personal debt figure from the Build, Stabilise, Leverage sampler. The "$1.4 million in debt" line is in the risk-management excerpt ("The most uncertain week of my career", section four, page 191), not chapter 9. Changed to "Significant debt across our properties". The figure remains in the manuscript and will resurface if the full text is published in September, so fix it at source before then.
- Dialled Rankline down sitewide, pending the conflict-of-interest check against BankVic policy and public launch readiness. Removed: the hero "Founder, Rankline" line, the homepage product card (swapped for the autonomous-agent system), all rankline.ai hyperlinks, the contact-page "Advisory: the Rankline form" button and routing, the rankline.ai contact routing in are-you-ai and llms.txt, and rankline.ai from both JSON-LD sameAs arrays. Softened "launched 2026"/"my own product" framing to "early-stage ... run outside my employed work". Rankline still appears as a quiet, unlinked, factual mention in the about narrative, the systems page, two essays and the machine bios. Restore a single link when it's launch-ready and the conflict question is cleared.
- Not changed (flagged for a decision): the contact page's broader "advisory for hire" block, and the specific "truck and equipment finance brokers" market wording.

## v1.9.4
- Corrected the Origin Energy claim. Removed the assertion that the full electrification hub was delivered end to end (it was not). Kept what is accurate and now confirmed: from a blank brief, Nathan did the groundwork (research, IA, content, 20+ stakeholders), and created web pages from scratch with generative AI for the first time and published a number of them, Origin's first AI-generated web pages, taking generative content from an 80 to 90% publishable mockup to live production. Edited the work-page card body, the about-page prose, and the about-page timeline. The are-you-ai bullet was already accurate (it only claimed the AI pages, not the hub) and was left unchanged. LinkedIn pack updated to match.

## v1.9.5
- Renamed the second book from "How to Buy Property With Your Super" to "SMSF Property Investing" sitewide (title tags, H1, Book schema, prose, cross-promo, llms.txt, updates page). Case-sensitive replace: title-case title updated, the sentence-case hub H1 became "SMSF property investing", and the descriptive lowercase prose sentence in the right-for-you chapter ("...how to buy property with your super, we need to talk about whether you should") was deliberately left untouched. The manuscript text is unchanged; only the title and cover changed.
- Swapped the download assets for the new edition: PDF (149pp, cover embedded) and EPUB, both renamed to smsf-property-investing.pdf / .epub. Old filenames 301 to the new ones in _redirects. New cover regenerated to /assets/img/super-cover-2027.jpg (filename kept to avoid breaking the og:image; content updated).
- Added the Amazon edition: "Buy on Amazon" button on the hub (same-tab, matching the /book/ convention), a Kindle-and-paperback note, an Amazon mention in the /book/ cross-promo and llms.txt, a workExample in the hub Book schema, and a title-update entry on /updates/. Amazon URL: https://www.amazon.com.au/dp/B0HB3XTGJS/
- URL scheme unchanged: the book stays at /super/ with the same chapter slugs. Renaming the paths would have broken every deep link, citation and redirect for no real gain.
- OPEN ITEM: the book is still presented free in full on the site. If its Kindle edition is enrolled in Kindle Unlimited / KDP Select, that breaches Amazon's digital-exclusivity terms (the same rule that keeps Build, Stabilise, Leverage a 10% sampler). Either publish the SMSF Kindle wide, or cut the web version to a sample. Not resolved in this version, flagged to Nathan.

## v1.9.6
- Renamed the nav item "Games" to "Challenges" (header and footer, all 31 pages). Link unchanged: still novlr.com, new tab, rel=noopener, same position after Super and before About. The longer label widens the header slightly, so the pre-existing horizontal-scroll band now runs roughly 721px to 855px (was 721 to 824). The same unimplemented fix still clears it: raise the nav media query in style.css from 720px to 860px.

## v1.9.7
- Added a second external game link, "Beakdown" -> https://beakdown.fun (new tab, rel=noopener), in the header and footer of all 31 nav pages, positioned after Challenges and before About so the two game links sit together. Nav is now 9 items.
- WARNING, nav overflow is now a real problem, not a marginal one. With 9 items the header's hidden horizontal-scroll band spans roughly 721px to 955px, and the last item ("Are you AI?") is clipped off the right with no visible scrollbar. That band now includes 768px (iPad portrait) and common small-laptop widths. The one-line breakpoint fix still works but the value must rise with the item count: change the nav media query in style.css from max-width:720px to max-width:960px so the nav wraps to two rows instead of clipping. At 9+ items, consider a wrapping nav or a menu toggle rather than pushing the breakpoint higher each time. Still unimplemented per Nathan's standing direction.
- Note: at time of adding, beakdown.fun returns HTTP 503 (upstream TLS handshake failure between edge and origin), so the game was not serving yet. The link is correct; the deployment needs fixing host-side.

## v1.9.8
- Renamed the beakdown.fun nav label from "Beakdown" to "Game" (header and footer, all 31 pages). Link, new tab and position unchanged. Header is now: Work, Systems, Writing, Book, Super, Challenges, Game, About, Are you AI?. The shorter label narrows the header slightly, but it is still 9 items and the nav still clips its last item on a hidden horizontal scroll on mid widths (see v1.9.7 note); the max-width:720px to 960px breakpoint fix remains the recommendation, still unimplemented per Nathan.

## v1.9.9
- New page /games/ showcasing the two browser games, Beakdown (beakdown.fun) and Hurtle (hurtle.site), each with a cropped 21:9 screenshot (assets/img/game-beakdown.jpg, game-hurtle.jpg via the existing .artband treatment), a description drawn from each game's own site, and a Play button opening the game in a new tab. Built on the standard page template (skip link, pagehead, section-head, prose, dl-btn) plus one small CSS rule (.game-tag). Includes CollectionPage + VideoGame schema.
- Renamed the nav item "Game" to "Games" and repointed it from beakdown.fun (external, new tab) to the new internal /games/ page (same tab), header and footer, all 31 existing pages. beakdown.fun is now reached via the games page rather than directly from the nav. Challenges (novlr.com) is unchanged.
- Added /games/ to sitemap.xml and a Games section to llms.txt. Still 9 nav items, so the mid-width nav clip from v1.9.7 remains; breakpoint fix (720px to 960px) still the recommendation.

## v1.9.10
- Games page: reordered so Hurtle sits above Beakdown, and made each screenshot a link to its game (new tab), wrapping the artband img in an anchor. Added .artband a{display:block} plus a subtle scale/brightness hover so the images read as clickable. Updated og:image to the Hurtle shot and reordered the schema hasPart to match the new sequence. Only games/index.html and style.css changed.

## v1.10.0
- Replaced the hero's looping background video with a three.js WebGL particle field that changes mode with the three reader tabs: person (amber, organic drift), google (green, snaps to a grid lattice with connection lines), ai (violet, neural web with a magnetic pull toward the cursor). New file assets/js/background-canvas.js, exposing window.setReaderMode('person'|'google'|'ai').
- three.js r128 loaded from cdnjs in the head of index.html with defer, SRI (sha384) and crossorigin, so it cannot block first render and cannot be swapped under us. background-canvas.js loads deferred before the closing body tag; deferred scripts run in document order, so three.js is always ready first.
- The video element was removed. The .hero-media div stays as the canvas host and keeps its /assets/img/gen-hero.jpg background, which is now the fallback for reduced motion, a blocked CDN, or no WebGL.
- Mounting note: the supplied script mounted a position:fixed, z-index:-1 canvas on document.body. Tested and rejected. body has an opaque background (#fbfaf6) and .home .hero an opaque panel (#131109), so the canvas painted behind the hero and was invisible in exactly the region where the reader tabs sit. The canvas is instead absolutely positioned inside .hero-media, where the video was, and sized to the hero via ResizeObserver. The .hero-media::after contrast gradient still paints above it, so hero text contrast is unchanged.
- Changes to the supplied script, behavioural rather than cosmetic: honours prefers-reduced-motion on load and if toggled mid-session (the site claims this on /colophon/); returns silently if THREE or WebGL is unavailable; the per-frame Float32BufferAttribute allocation was replaced with a single preallocated buffer plus setDrawRange, since the original allocated a fresh typed array 60 times a second; squared-distance comparison instead of Math.sqrt in the O(n^2) link loop; segment cap of 2400; pauses when the tab is hidden; canvas is aria-hidden.
- Tab hookup uses a MutationObserver on the tabs' aria-selected rather than click handlers, so the background also follows the tablist's arrow-key navigation and stays decoupled from site.js. window.setReaderMode is still a global and no-ops when called with the already-current mode.
- OPEN ITEM: three.min.js r128 is 603 KB raw, 146 KB gzipped, from a third party. Every page footer says "Static HTML. No framework, no tracking, no cookies", and /colophon/ says "a handful of images, and inline SVG are the only assets". Both are now arguably false on the homepage. Either reword those two claims or reconsider the dependency. Flagged, not changed.

## v1.10.1
- Rebuilt the hero WebGL render layer. In v1.10.0 the effect was measurably invisible: only 1 to 2 per cent of background pixels changed between the three modes, and mean brightness and standard deviation were identical across all three (41/41/40, sd 62). After this pass the modes differ by 9 to 11 per cent of background pixels and are plainly distinct on screen.
- Root causes, all environmental rather than faults in the original script: gen-hero.jpg was still painted behind the transparent canvas and dominated the frame; two stacked overlay gradients at up to 95 per cent opacity crushed what was left; 180 particles at size 0.7 were far too sparse for a 1280x1100 hero; line opacity of 0.10 to 0.35 was invisible; and square point sprites with normal blending read as scattered pixels rather than an effect.
- Fixes: the hero photo is dropped once the canvas is live (.hero-media.webgl-on) and kept only as the no-WebGL and reduced-motion fallback; the overlay stays strong over the text column and eases to zero on the right; round soft sprites generated in-page on a 2D canvas, so no extra request; additive blending for glow; 380 particles at size 1.9; per-mode link thresholds; segment cap raised to 5000; snappier position and colour interpolation.
- Modes are now structurally distinct rather than just differently coloured: person is warm amber bokeh with no links, google snaps to a 22-column lattice with crisp connecting lines, ai is a dense violet neural web that clusters toward the cursor.
- Added an IntersectionObserver so rendering pauses when the hero leaves the viewport and resumes when it returns.
- Legibility verified by measuring the backdrop with the hero copy hidden: mean contrast against white text is 19.4:1, and the worst single pixel (a particle core directly behind a letter) is 4.9:1, both above the WCAG AA 4.5:1 threshold. Reduced motion, blocked CDN and mobile all re-verified.

## v1.11.0
- Rewrote the hero motion system. In v1.10.1 the field animated into formation over a second or two and then stopped: measured settled movement was 2.6px (person), 0px (google, literally frozen) and 0.9px (ai) over two seconds, and the only pointer interaction was a weak attraction in ai mode that lost 6:1 to the restoring lerp.
- Motion is now three independent layers, which is what allows a snappy mode transition and a highly responsive pointer at the same time; driving both through one position lerp forces a trade-off between them. base lerps toward the mode formation, drift is a continuous per-particle wander so the field never freezes, and repel is a springing displacement away from the pointer.
- Measured after the change: settled movement is 97px (person), 24px (google) and 31px (ai) over two seconds, so all three modes stay alive. Pointer repel displaces 35 to 50 particles by up to ~220px and the void tracks the pointer across the field; verified it recentres when the pointer moves from one side to the other.
- Repel now applies in all three modes, not just ai. Radius 14 world units, quadratic falloff, spring constant 0.18, so the void opens and closes smoothly. Links are built from final drawn positions, so the lattice and the neural web visibly distort around the pointer.
- Added touch support (touchstart, touchmove, touchend) so the effect is interactive on phones and tablets, which it previously was not at all. Added subtle camera parallax, and normalised all interpolation to a 60fps step so the animation does not run at double speed on 120Hz displays.
- Device-aware field: a narrow hero only shows a slim slice of the world, so below 700px the spread, particle count (200 rather than 380), segment cap and pixel ratio all scale down. Previously about 90 per cent of particles sat off-screen on a phone and were computed for nothing.
- Thinned the neural web (threshold 6.5 to 5.6), cutting ai from ~3720 to ~2650 segments per frame and reducing clutter over the copy.
- Contrast: the extra movement initially pushed the worst-case backdrop behind the hero copy to 3.1 to 4.1:1, below the WCAG AA 4.5:1 threshold. The overlay was reshaped to protect the text column to 50 per cent width and open up after 72 per cent. Re-measured over eight frames per mode: mean 19.4 to 19.7:1 and worst-case 7.3 to 10.4:1, comfortably above AA, with the right side of the hero left bright.
- Reduced motion, blocked CDN, no WebGL, touch and mobile fallbacks all re-verified with no console errors.

## v1.12.0
- Evidence over decoration on /work/. Removed the decorative artband (gen-work.jpg), which carried no information and pushed the case studies down the page: on a 390x844 phone the first case study sat at 778px, effectively at the fold. It now sits at 510px.
- Added an evidence visual to each of the eight case study rows, built from four reusable SVG primitives rather than eight bespoke graphics: delta (before and after, used for the credit card uplift and the Versa turnaround), rank (search position held over time, used for GoBiz), magnitude (a figure at scale, used for the chapter portfolio and the BAE organisation) and sequence (staged progress, used for Land Use Victoria, Origin and the home loan journeys). Variety comes from the data, not from inventing a new treatment per page.
- The visuals are inline SVG with no JavaScript, so they survive with JS disabled (verified: all eight still render) and stay parseable by the machine readers, consistent with the /colophon/ claim that turning JavaScript off loses the interactive demonstrations and not one word of content. The draw-on animation is CSS only and is fully disabled under prefers-reduced-motion (verified: all strokes render complete rather than stuck mid-animation).
- New .evi-cell wrapper: the row is a three-column grid, so adding the SVG as a fourth child initially pushed the metric label into the wrong cell. The visual and its label now share one cell, right-aligned on desktop and moved above the heading on mobile.
- Site-wide interaction craft: focus-visible rings on links and buttons, a considered row hover that shifts the heading and the evidence stroke to the deeper palette tone, and small lift transitions on buttons and cards. All disabled under reduced motion.
- Still to do from the agreed plan: the machine-reader lens (upgrading the existing "view this page as machines do" pill into the site's signature interaction) and the /about/ set piece. The same evidence treatment should also be applied to /systems/, where the first real content currently sits at 895px, below the fold on a phone.

## v1.13.0
- Rebuilt /work/ as an evidence wall. The v1.12 treatment was a garnish: 200px sparklines in the right margin restating what the caption already said. Each case study is now a band led by its metric at display scale (clamp up to 2.65rem), with an optional mono prefix carrying the lower bound of a range, a colour-coded range bar, and the story alongside.
- Ranges are shown as ranges. Most of Nathan's headline figures are honest ranges ($5m to $7.5m, 10 to 15%, $8.5m to $10.7m), so a count-up animation to a single number would imply a precision he has deliberately refused to claim. The prefix plus band pattern ("10 TO" above "15%", bar spanning 52 to 80 per cent) keeps the claim honest while giving the number presence.
- Motion is CSS scroll-driven (animation-timeline: view()), not JavaScript. It runs on the compositor thread and scrubs in both directions, so scrolling back up reverses the reveal. Verified: scaleX 0 to 0.28 to 0.55 to 0.83 to 1 scrolling down, and the identical values in reverse scrolling up.
- Page signature: the existing top spectrum rule (body::before) becomes a scroll progress indicator, dimmed to a track with a full-strength body::after fill driven by animation-timeline: scroll().
- Both guarded twice, by @supports (animation-timeline) and prefers-reduced-motion, so Firefox (still flagged as of 152) and reduced-motion users get the finished state rather than an element stuck at zero. Verified: bars full and figures visible under reduced motion, and all 8 figures and bars render with JavaScript disabled.
- BUG FIX, non-obvious: .section carried overflow:hidden, which establishes a scroll container, so view() timelines resolved to the section rather than the document and never activated. Changed to overflow:clip, which clips identically without creating a scroll container. This is the difference between the scroll choreography working and silently doing nothing anywhere on the site.
- BUG FIX: the v1.12 row restructure left a stray closing div in all 8 rows (a non-greedy regex stopped at the first </div> rather than the row close). Repaired; div and anchor counts now balance inside main.
- BUG FIX: .ev-fig and .ev-lab are paragraphs, so .row p (one class plus one element) beat them on specificity and the display figures rendered at body size. Selectors raised to .rows .ev-fig etc.
- Still to do: apply the same treatment to /systems/ (first content currently at 895px, below the fold on a phone), the machine-reader lens, and the /about/ set piece.

## v1.14.0
- Much bolder scroll choreography on /work/. v1.13 was underwhelming for two concrete reasons, both mine. The animation ranges (entry 6% to cover 34%) meant each reveal finished while the row was still in the bottom third of the viewport, so it was over before the reader looked at it. And the motion itself was opacity .16 to 1 over 9px of travel, which is not perceptible movement.
- Display figures are now split into per-character spans and rise individually from behind a mask, with a scroll-driven stagger: each character's animation-range is offset by 2.2% of the cover range via calc(), so the number assembles letter by letter with no JavaScript. Measured mid-scroll opacities across one figure: 0.5 0.4 0.3 0.2 0.1 0.0 0.0, a clear cascade.
- Retimed everything to play across the middle of the screen: the prefix slides in at cover 22%, characters run from cover 26% to 48% staggered, the bar draws at cover 36% to 60%, the label lands at 44%, and the body copy rises from cover 24%. Verified the sequence runs while the row travels from 56% to 33% down the viewport, and reverses exactly on scroll-up.
- Figures enlarged (clamp 2.4rem to 3.5rem, 2.9rem on mobile), bar thickened from 3px to 5px, column widened to 15rem.
- Per-character splitting is done in the markup, not by JavaScript, so the figures remain plain text to a crawler and render complete with JS off (verified: 8 figures, 32 characters, 8 bars).
- Fallbacks re-verified: under prefers-reduced-motion every character, bar and body block sits at full opacity and final position; @supports still guards Firefox.

## v1.15.0
- /systems/: replaced the static pipeline SVG with an interactive simulation, and removed the decorative artband. The SVG was a 760-unit viewBox squeezed into 300px on a phone, a 0.39 scale factor rendering the stage labels at 7px, and it sat 1506px down the page behind the artband. The simulation is vertical, legible at full size, and is now the first thing on the page.
- The simulation runs one article through the pipeline in front of the reader: topic discovery, voice profile, draft, critical review pass one, revision, critical review pass two, factual verification, publish. It autoplays once on entry and can be replayed.
- The content is not invented. The rejection is the leading slip-through pattern from Nathan's own AI content detection signals research (v2.1, section 14.2): an unfounded population claim of the collective-experience form, caught in production on the Inform Physio account. The narrative beat that matters is the second pass: the writer softens "nearly every" to "many" rather than removing the claim, and the independent editor catches the softening. That is straight from the research ("a writer that turns 'most homeowners don't realise' into 'many homeowners don't realise' has not solved the problem") and it is the behaviour that demonstrates governance rather than asserting it. The final state resolves to the auto-publish confidence tier from the CJD architecture spec.
- Degrades to a static, readable pipeline diagram with JavaScript off (verified: 8 stages listed, sentence visible), and reduced motion shortens the beats rather than animating.

## v1.16.0
- Nav: Games moved to third place, after Systems and before Writing, in the header and footer of all 32 pages. Four pages (systems, writing, and the two essays) needed a separate pass because their own nav item carries aria-current, which broke the match pattern.
- /games/ rebuilt as the SnapHit arcade, modelled on snap-hit.online: a hero cabinet plus three cabinets for Drift Fever, Hurtle and Beakdown, each with the marquee, screen, press-to-play coin and deck, and the studio counter block (games shipped, in development, ads served, sign-ups required, install size).
- The games are embedded live from snap-hit.online/play/*.html rather than copied in, so there is one source of truth and the portfolio never carries a stale build. Verified the demos send no X-Frame-Options or CSP frame-ancestors, so cross-origin framing is permitted.
- Click to play, not autoload. The four demos total about 1.9MB and Drift Fever alone is 1.5MB, which would be heavier than the rest of the site combined. Each cabinet holds its URL in data-src and only becomes a live iframe on press (verified: zero frames loaded on arrival). Starting a cabinet unloads the previous one, and scrolling a playing cabinet out of view unloads it, so audio never continues off screen and a phone never runs two games at once.
- BUG FIX: .cab-start sets display:flex, which overrides the user-agent [hidden]{display:none} rule, so the press-to-play overlay stayed on top of the running game. Added .cab-start[hidden]{display:none}.

## v1.16.1
- Repo preparation. Removed two unreferenced assets before first commit: gen-hero-loop.mp4 (2.6MB, orphaned since the WebGL hero replaced the looping video in v1.10.0) and game-beakdown.jpg (orphaned when /games/ became the arcade in v1.16.0). Added a .gitignore for OS, editor and local-tooling files.
- Note on licensing: this repo contains the full text of SMSF Property Investing and excerpts from Build, Stabilise, Leverage. Do not add an open-source licence. With no LICENSE file the default is all rights reserved, which is the intended position.

## v1.16.2
- /games/ closing paragraph corrected. It read "finished when you tap it", which said the opposite of what happens: a tap starts a game, it does not end it. Now reads "Every one starts the moment you tap it", with the no store, no download, no account line extended to "nothing to uninstall afterwards". Links to snap-hit.online and novlr unchanged. Text only, no CSS or JS touched.

## v1.17.0
- /games/ hero rebuilt to match snap-hit.online. The SnapHit demo is no longer inside a cabinet and no longer needs a tap to start: it loads on arrival and fills the viewport under the nav (100dvh minus header height). The page heading, studio counters and the three cabinets now sit below it.
- The way onward is gated. The game is framed from snap-hit.online and posts { snaphit: 'cleared' } to the parent with a wildcard target origin when a run is cleared, so the signal crosses origins. hero-game.js listens for it and reveals the More link. Cleared state is stored in localStorage, so a returning visitor is never gated twice. Verified: the More link goes from opacity 0 with pointer-events none to fully interactive on the cleared message, persists across reload, and a message forged from another origin is rejected.
- Deliberately not a scroll lock. The gate is the full-height hero plus a withheld affordance, the same as the source site. A hard scroll lock would be hostile and inaccessible.
- Three escape hatches so nobody is trapped: a keyboard-reachable skip link visible on focus, automatic reveal on first Tab press, and a 45 second timeout that reveals the link if the game fails to load or never signals. The markup also ships unlocked and JavaScript adds the lock, so with JS disabled the visitor gets a working link rather than a dead end.
- The hero frame parks itself (src removed) when scrolled out of view and restores on return, so the game is not running under the rest of the page.

## v1.17.1
- Machine reader pill suppressed on /games/ only. The pill is fixed to the bottom right, which is where the hero gate reveals the More link, so on that one page the two sat on top of each other and the More link was hard to hit on a phone.
- Done as an opt out rather than a special case in the script: site.js now builds the pill only when the body has no data-no-reader-pill attribute, and /games/ is the only page carrying it. The overlay code is untouched, so any page can opt back in by dropping the attribute.
- Verified in Chromium at 390px and 1280px: pill absent on /games/, present on all ten other pages that carry it, and still absent on the home page, which has never had it because the reader frame is inline there. No console errors on any page.
- Not shipped in this version: the port of the real snap-hit.online cabinets into the rack. The session doing the work could not reach snap-hit.online, so the source could not be read, and the old .cab and .rack cabinets stayed in place for one more version. Landed in v1.18.0 instead, from the studio site's own repo rather than from memory.

## v1.18.0
- The real SnapHit cabinets are now on /games/. Markup, CSS and behaviour ported from the studio site's source (SnapHit/SnapHit_Online, docs/index.html) rather than reconstructed, so the portfolio and snap-hit.online show the same machine: lit marquee, brushed phone in an aperture, island and home bar over the screen, printed control panel and coin door.
- Every ported class carries an sh- prefix. This stylesheet already defined .cab and .rack for the old cabinets and still uses .bar, .screen, .deck and .panel elsewhere, so an unprefixed port would have hit live rules on other pages. The custom properties are namespaced too (--sh-pw and friends). Checked: no ported selector matches anything outside /games/, and the two other pages that grep for 'sh-' are false positives from heading ids containing 'cash-'.
- The old .cab, .cab-* and .rack rules and markup are gone. The studio counters and the v1.17 hero gate are untouched.
- The deck artwork is 18KB of SVG. Three inline copies would have put 55KB of duplicated markup in a hand maintained file, so it is one <symbol> referenced three times, with its gradient ids namespaced. Verified it paints identically in all three cabinets.
- Behaviour carried forward from the old arcade.js and the source: nothing loads until tapped, one game plays at a time, a frame unloads when it scrolls out of view and its card comes back, and holdUntilLoaded keeps the card up until the iframe actually loads, with a 20 second fallback so a game that never loads does not leave a dead card. Tap to expand styles the phone in place rather than moving it, so the iframe is never reloaded and the run survives; Escape and the close button collapse it.
- BUG FIX during the port: .sh-attract was given z-index:5, which lifted the tap-to-play card above the island, glass and home bar and stopped the phone chrome painting over the screen. The source has no z-index there on purpose. Removed, and confirmed by screenshot that the notch and home indicator sit over the card again.
- Deliberate change from the source: the outbound links carry ?from=portfolio, not ?from=snaphit. The referral is there so each game's logs can tell where a visitor came from, and from this site that is the portfolio.
- Verified in Chromium at 390px and 1280px: three cabinets, deck artwork painted, no horizontal overflow at either width, no console errors. Tap creates the frame and reveals the expand button, starting a second cabinet parks the first, scrolling away unloads and restores the card, expand fills the viewport with the close and 'play on the real site' buttons in opposite corners, and Escape collapses it. Reduced motion switches off the marquee glow, the falling signal and the hero arrow. With JavaScript off all three cabinets, both links out per cabinet and the deck artwork still render, so nothing but the playable demo is lost.
- Note: the three demos are framed live from snap-hit.online and that host was unreachable from the session that did the port, so the machinery was verified against a local stub served in place of the games. The cabinets, the loading hold and the unloading were all exercised; what has not been watched end to end here is a real game running inside the frame.

## v1.19.0
- /games/ now runs dark end to end, and the SnapHit studio card sits at the bottom of the arcade section.
- THE DARK DECISION: the whole arcade run went dark, not just the rack. Darkening only the cabinets would have given dark hero, light heading, light counters, dark rack, light prose, dark footer: six bands and five hard edges, and a near-black stripe in the middle of a light page reads as a rendering fault rather than a decision. The page already opened dark on the full-viewport hero, so going light straight after it was the odd part. Ground is the source's #04070e, with the source's shelf gradient (radial blue wash over a linear ramp) on the section holding the cabinets and the card.
- Done by overriding tokens (--paper, --ink, --ink-2, --line, --ok) on 'body.arcade main' rather than rewriting each rule, so .pagehead, .section and .prose came along untouched. Scoped to main deliberately: the sticky nav keeps its sitewide light treatment and the footer keeps its warm dark, and the footer's existing 3px spectrum bar already marks that boundary, so the two near-blacks read as layering rather than a seam.
- Studio card lifted from the studio site's source (the .op block), prefixed sh-, placed after the three cabinets, with the source's own gap and thread element as the transition into it. Order is deliberate: you walk past the machines, then meet the person who made them.
- Banner image copied into the repo at /assets/img/snaphit-studio.jpg rather than hotlinked, because no page asset may come from another origin. 168KB, lazy, with width and height set so it cannot shift layout. It sits below three cabinets and two long gaps, so it costs nothing until someone scrolls that far.
- The scoreboard moved into the card and the standalone .studio-stats block was deleted, along with its CSS. The five figures now appear once on the page instead of twice. They belong in the card: they are the studio's figures, so the studio presents them. Kept as a faithful replica, not reframed, because this is Nathan's own studio and the page lede already says so in the first person. The .game-tag pull quote went too, because the card now makes that point with a mark and a scoreboard behind it, and its CSS rule went with it.
- The 'watch the build' button and the 'Made by Nate' credit were kept verbatim. On a site that says Nathan Haslewood on every page the credit is mildly redundant, but @GamerNate is the one identity here that is genuinely new information, and the register shift tells a reader the games ship under a different name.
- Deviation from the source: the card bio drops its closing clause. The source reads 'finished when you tap it', which is the exact wording corrected on this page in v1.16.2 because a tap starts a game rather than ending it, and the shortened bio also stops the card repeating the closing paragraph directly beneath it word for word.
- BUG FIX found in review, and the reason this got a review at all: the sitewide 'a:hover{color:var(--ink)}' is specificity (0,1,1) and the ported '.sh-op-btn' was (0,1,0), so hovering the only call to action in the card repainted its label and its currentColor play triangle with the arcade --ink. Measured worst-case pixel 1.19:1 against the amber gradient, far under AA, and animated over 150ms so it was a visible fade to near white. The cabinet button escaped only because it happened to be ported as a two-class selector. Now scoped '.sh-op .sh-op-btn' with the colour pinned through hover and focus: 13.09:1.
- BUG FIX: '::selection{color:var(--paper)}' resolves var() against the originating element, so the --paper override painted selected text near black on the orange highlight, 3.83:1 against 5.05:1 before. Pinned back to paper inside the arcade. This is the hazard of redefining global tokens on a subtree: the rule that broke sits a thousand lines from the change.
- Two tidy-ups from the same review: the ported h2 rule was dead, because the heading carries .sh-sr and is hidden by design with the banner carrying the name, and the scoreboard's dotted leaders were a <span> sibling between <dt> and <dd>, which is not valid inside a <dl>. The leader moved to the head of the <dd>, verified pixel identical.
- Page weight: HTML 32.4KB to 33.7KB raw, 6.9KB to 7.2KB gzipped. CSS 63.3KB to 67.7KB raw, 15.2KB to 16.7KB gzipped. JavaScript unchanged. So about 1.8KB more over the wire on first paint, plus the 168KB banner only if the reader scrolls to the card. For comparison the other pages load a hero image of 54KB to 211KB eagerly.
- Verified in Chromium at 390px and 1280px: no horizontal overflow, no console errors, the five figures once, the card below the cabinets and nowhere else. Every text surface inside main measured against its painted background, worst case 6.38:1 on the card bio, everything else above 9:1. Reduced motion leaves no animation running, including the page heading's aurora. With JavaScript off the cabinets, both links out per cabinet, the studio card, the scoreboard, the banner and the hero 'more' link all render. No new off-origin asset requests: the Google Fonts stylesheet is sitewide and pre-existing, and the game iframes are intended. The hero game and its clearing gate are untouched and still gate on clear, Tab and timeout.

## v1.19.1
- Copy pass on /games/. No CSS, no JavaScript, no structural change.
- Removed the eyebrow above the heading, so the page now opens straight into 'Games' after the hero.
- New lede: 'I make games for fun under the name SnapHit Studios. Arcade games built to play on any phone, any device, free, no ads, no sign-in, nothing to install. Simply tap each one to play.' SnapHit Studios links to the studio with ?from=portfolio, in a new tab, matching the cabinets.
- Removed the 'The cabinets' section heading and its 'three live games' label. The cabinets now follow straight from the lede.
- Removed the closing paragraph and its wrapper, so the studio card is the last thing before the footer. That was the page's only in-body novlr link; novlr is still in the nav and the footer. Spacing after the card was measured rather than assumed: 93px at 390px and 154px at 1280px between the card and the footer, then the footer's own spectrum bar, so the join still reads as deliberate and nothing collapsed.
- No dead CSS from any of the four removals: .eyebrow is on 32 pages, .prose on 30, .section-head on 2.
- Note for the next copy pass: the lede no longer states how many games there are, so the scoreboard's GAMES SHIPPED 04 is now the only count on the page and nothing contradicts it. An earlier draft of this lede said 'released three', which would have collided with that 04. If a count goes back into the lede, it has to agree with the scoreboard or the scoreboard has to move with it.
- Verified in Chromium at 390px and 1280px: no horizontal overflow, no console errors, heading sits correctly with no eyebrow, the studio link resolves with target and rel intact. With JavaScript off the heading, the lede link, all three cabinets and the studio card still render. Hero game, clearing gate, cabinets, studio card, dark treatment and reader pill suppression all untouched.

## v1.20.0
- The machine reader lens, a pilot on /book/ and no other page. The reader pill opens a modal that describes a page's machine reading. This turns the page itself into that reading, in place, covering nothing and without moving the reader off the line they were on. Three states from one control: person, Google, AI.
- One registered custom property carries the whole feature. @property --lens on body holds 0, 1 or 2, and every rule derives from it. Registering it makes it a real number rather than a string, which is what lets a drag land halfway and genuinely show a half transformed page. The control writes that value and does nothing else to the page.
- The control is a three position slider, fixed to the bottom of the viewport, both tappable and draggable, keyboard operable with arrows, Home and End, announced as role=slider with the state named rather than numbered. It is a direct child of body on purpose: a filter on an ancestor makes position:fixed resolve against that ancestor, and the control would scroll away mid transition. Verified there is no filter, transform or containment on any of its ancestors.
- Google state: colour recedes to monochrome, imagery fades out and its own alt text appears in its place, every heading gains its real tag read from tagName, every link reveals its destination shortened to host and last segment, and the invisible head material rises in at the top of main with the title, description, canonical, lang and the JSON-LD rendered as a readable card.
- AI state: layout collapses to a single monospace column in document order, decoration retreats, and a response header appears with values read at runtime rather than described. Status and content type come from a same origin fetch of the same URL, the way /colophon/ already does it. Transfer size and protocol come from Navigation Timing. The 'rendered' row is a measurement, not a claim: the served HTML is re-parsed and its main text compared against what was on the page before any script ran, so it reports server side only when it can show the characters were already there.
- The audit layer marks images with no alt, images with an empty alt not declared decorative, headings that skip a level, and non-descriptive link text. On /book/ it finds nothing, and that is the point. To show the checks work rather than merely return zero, they were run against a fixture with all four faults and all four fired, while an image correctly marked role=presentation correctly did not.
- The chapter picker keeps working untouched in all three states. In both machine states every one of the thirteen excerpts opens, because all thirteen are in the HTML and that difference between what the reader sees and what a machine receives is the demonstration. The fifteen buttons are labelled once, with the count read live, rather than tagged individually.
- BUG FIX during the build: the stylesheet sets scroll-behavior:smooth on html, so every scroll correction the lens makes to hold the reader in place was being animated. At sixty frames a second each new one cancelled the last and none ever landed, dragging the reader up to 405px down the page. Smooth scrolling is now suspended on html for the duration of a gesture and the corrections are instant.
- BUG FIX: the anchor was a content block, so pinning it held the block while everything growing inside it pushed the words the reader was on. It now pins the deepest element actually painted under the reader's eye, and holds it to its original offset rather than nudging it by each frame's delta, so a bad frame costs nothing by the next one. Measured drift is now 0px on all four transitions at both widths, down from 405px.
- BUG FIX: the link audit read each link's text after the destination had been appended to it, so 'click here' read as 'click hereexample.com/.../c' and the check could never match. An auditor that silently stops finding things is worse than no auditor. Label is now read first, and the fixture proves it fires.
- BUG FIX: the link destination annotation was a fixed blue, and the first link on this page is a solid orange button with white text, so the destination inside it measured 1.30:1. It now inherits the link's own colour, which is already required to pass, and the monospace face is what marks it as annotation. Measured 5.27:1.
- Contrast measured on every surface the lens introduces, in both machine states, against a composited backdrop: worst case 5.12:1 on the card captions, everything else between 6.5:1 and 17.6:1.
- Also fixed before shipping: the AI state narrowed every .wrap including the header's, which squeezed the nav; and the per button labels clipped inside the 250px chapter column.
- Note, not fixed, reported instead: across the whole document /book/ skips a heading level, h2 to h4, at the footer. The footer's 'Pages' and 'Machine layer' headings are h4 with no h3 above them. That is a sitewide pattern on all 32 pages, not this page's content, so the on page audit is scoped to main and does not mark it. It is a real finding and worth a decision separately.
- Verified in Chromium at 390px and 1280px in all three states plus held mid drag: no horizontal overflow, no console errors, the control fixed at 16px from the bottom in every state, and the scrub continuous with no jump cut. Reduced motion snaps with no tween and no stagger. With JavaScript off there is no control, no injected markup, no lens-host, and the page renders as it did. The reader pill is suppressed on /book/ via the existing data attribute and is untouched on every other page. No off-origin requests beyond the sitewide Google Fonts stylesheet. Confirmed the other pages carry no lens markup at all.

## v1.20.1
- Fixes for the lens pilot, from an independent review of v1.20.0 that confirmed 24 defects after adversarial verification. Six were high severity and none were caught by my own testing before shipping. /book/ only, no other page touched.
- HIGH, content loss: every one of the twelve revealed excerpts was clipped mid sentence in both machine states, up to 2,116px of text at 390px. Two causes. The height was measured out of flow with position:absolute and width:100%, which resolves against the sampler stage's padding box rather than the content box the excerpt renders in, 348px against 306px. And box-sizing is border-box globally, so the open state's own 25.6px padding and 1px border ate that much again out of a height measured without them. Now measured in flow, with the same padding and border, forced inline because a measuring class lost on specificity to the five class rule it had to beat. Clipping is 0px on every excerpt at both widths in both states.
- HIGH, the same measurement again: it was taken with the injected annotations collapsed, and on the longest heading the H3 tag is exactly what tips it onto a second line. Measured with the annotations at full width, which is the settled state the height has to fit.
- HIGH, keyboard unusable at speed: the key handler stepped from the live mid tween value rather than the tween's destination, so a second press inside 620ms recomputed the same stop and went nowhere. Two quick presses landed on Google and holding the key never reached AI at all. Steps from the destination now: two presses reach AI at every gap from 0ms up, and 36 presses at 33ms reach it.
- HIGH, announced when it should not be: both injected cards sat collapsed but still in the accessibility tree, so 29 text nodes and 1,158 characters were read out before the page in the person state, where the feature has not been touched. They are aria-hidden until they open.
- HIGH, anchoring race: the settle chain's animation frame handle was never stored, so it could not be cancelled, and a stale chain reaching zero during the next tween switched anchoring off and smooth scrolling back on mid transition. Measured 216px of drift when a second stop was tapped about 620ms after the first. Each gesture now takes a ticket and a stale chain stops at the first check. Drift across that window is 1px.
- HIGH, contrast: the link destination was a fixed blue and the first link on the page is a solid orange button, giving 1.13:1 through the greyscale filter. It inherits the link's own colour now.
- MEDIUM: the three reader dots were each one position out and the AI dot had no colour at all, because nth-child counted the thumb, which is appended first. Numbered explicitly. MEDIUM: the active chapter label and its background cross faded from opposite ends and met at the same pixel at the midpoint of the AI band, 1:1, so the chapter name vanished into its own pill. They switch together at the threshold now, 17.58:1 throughout. MEDIUM: the picker moves chapters on arrow keys as well as clicks, and a panel that became non current without a measurement collapsed to a 27px sliver; every panel is measured now. MEDIUM: both cards were inserted outside .wrap and ran full bleed, breaking metadata values mid word at 390px. MEDIUM: the same lens value rendered different layouts depending on the path taken to reach it; direct and stepped now agree to the pixel.
- LOW: .callout was silently rounded from its own 12px to 16px the moment lens.js loaded, in the untouched person state. The nav note added a grid row and its gap at rest for the same reason, so it is display:none until the lens opens. Card radius was dead code. Transfer figures divided by 1024 but were labelled kB, so they are decimal now. The registered --a was computed on every block and read nowhere; it now drives the AI band's block level decoration, which also gives that band the document order wave the first band already had. ARIA was rewritten about 37 times per transition and is now written on arrival.
- Also added: a re-measure once webfonts are ready. The first measurement runs at DOMContentLoaded while the page is still in fallback metrics, which would have been wrong in production even though the sandbox blocks Google Fonts and hid it.
- Correction to the v1.20.0 note on scroll position. Holding one paragraph across a font change is not the right measure, because text below the reader genuinely reflows when the font changes and no anchor can prevent that. Measured properly, by pinning the text under the reader's eye and tracking that same line: 0px drift and 0px excursion across nine consecutive state changes at 390px, 0px final and 1px maximum at 1280px.
- Re-verified after the fixes: no clipping, no horizontal overflow, no console errors, control fixed at 16px in all three states, path independent, reduced motion instant, JavaScript off completely inert, and all eleven other pages carry no lens markup.

## v1.20.2
- Alignment fix inside the lens control on /book/. Two rules, CSS only, no behaviour touched.
- The dot and label in each of the three stops sat hard against the left of their segment. The stops are grid items, so they blockify and inherit text-align:start, while the thumb covers the whole segment: the pair therefore sat well left of the pill it was meant to sit inside. Worst on AI, where the short label left about 30px of empty pill to its right. Measured before: the content centre fell 18px, 19px and 32px left of the thumb centre at 390px, and 19px, 19px and 34px at 1280px.
- Fixed with flex centring on .lens-stop and the dot and label separated by a gap rather than a margin, so the two centre as one unit and the dot travels with the words. The dot keeps its own size and colour rules.
- Geometry proven unchanged, which was the constraint: track 272px at 390px and 304px at 1280px, stop width 91px and 101px, stop positions [59,150,240] and [488,589,691], thumb width and x identical at all three stops. Only the alignment of content inside each stop moved. Content offset from the thumb centre is now 0px in all three states at both widths, and all three stops read 0px from their own segment centre, active or not.
- Verified: the thumb still sits exactly where the value says at 0, 0.5, 1, 1.5 and 2 during a real pointer drag, the keyboard still reaches Person, Google and AI, no horizontal overflow at either width, no console errors, and all six screenshots checked by eye.
- Note on the fix itself: a text-indent was added first to pull back the trailing letter-spacing, then measured, found to change the result by 0.01px, and removed. A property that does nothing is worse than no property when it ships with a comment claiming it does something.

## v1.21.0
- The homepage is the lens now. The reader frame used to be a tablist with three panels that built machine views inside itself; it is the control, and the whole page is the output. Same mechanism as /book/, same registered --lens property, same tween, keyboard and announcements, shared from the same file. Only the presentation differs: large and in the flow here, because this page's job is to teach the interaction that turns up as a small floating pill everywhere else. Two presentations of one lens, not two lenses.
- One value drives three things that used to be separate. --lens is read by the page (its palette, its tags, its machine reading), by the control (the thumb and the announced state) and by the particle field (its colour, its density, its links and how far it spreads). Because the property is registered as a number it interpolates, so a drag holds the page genuinely part way rather than snapping.
- The palette is the one thing that does not interpolate, and it took a measurement to see why. Ink and paper both cross the middle of the range if you tween them, so at the halfway point of a drag the page was dark grey text on a dark grey card: 1.08:1 on the section headings, 1.37:1 on body copy, across about 38% of the control's travel, and a drag can be held anywhere in it. There is no continuous path out of that; two colours that swap places have to pass through each other. So the palette steps at the middle and everything else keeps moving around it. This site had already learned it once, on the chapter label in v1.20.1, and fixed it the same way.
- The field left the hero. It was a canvas inside .hero-media; it is two body level fixed layers now, the canvas and a veil above it, with the dark ground below both. The ground moved from body to html so a layer at z-index -1 is visible at all: a fixed layer behind an opaque body background paints under it and cannot be seen, which is the bug that cost the v1.10.0 field. Every page still paints exactly what it painted before, because html now carries what body used to.
- The spread is the cloud growing, not the camera retreating. Pulling the camera back was the first attempt and it does the opposite of what it says: the frustum widens faster than the cloud, so the field shrinks into the middle of the screen and leaves the page edges empty, which is the one place the veil lets it be seen. Scaling the group keeps the coverage, and because the links are computed on unscaled positions the mesh is identical, just larger.
- The three reader panels are gone from index.html, and so is the frame wiring in site.js. buildGoogleInto and buildAIInto stay: the reader pill on every other page still uses them.
- The three stops arrive as spans, not buttons. With JavaScript off nothing can act on them, and a button that does nothing when pressed is worse than a label that never claimed it would. Both sentences that ask the reader to use the control are hidden until lens.js has taken the frame over, and a second sentence takes their place, so nothing is claimed that cannot be done and not one word of the argument is lost.
- BUG FIX, and it was already live: moving the canvas out of .hero-media dropped the webgl-on class that goes on it, and that class does two things. It turns off the photograph, which is only ever the no-WebGL fallback, and it swaps the weak fallback overlay for the strong one. Without it the page ran the photograph, the weak overlay and the particle field all at once. Restored.
- BUG FIX, shipped and measured on main: the hero copy fails WCAG AA on the live site, on both paths. Worst pixel under the glyphs at 1280px with WebGL running: the meta line and the eyebrow 1.00:1, the h1 1.76:1, the lede 2.96:1, all against amber particles blown out to rgb(196,139,55) and rgb(201,201,78) by additive blending. Without WebGL, over the photograph, the meta line is 3.31:1. The cause is the same in both: the v1.11.0 overlay starts easing off between 45% and 72% of the viewport while the hero copy runs to 90% of it, so the back half of every line sits on the unprotected part.
- The veil is the fix, and its shape comes from the layout rather than from percentages. It holds alpha .90 across the text box, which is the wrap inset by its own padding and then sixteen pixels of margin, and opens to .25 out past it. The number is measured against a solid white field, which is the worst an additive blend can produce: the hero copy reads 3.81:1 at .80, 4.68:1 at .86, 5.36:1 at .90. An earlier attempt derived it arithmetically instead and got .974, because the sum was done in linear light while the browser composites in gamma encoded sRGB; that buried the field under a scrim three times heavier than the text needed. The stops are written as 50% plus or minus half the wrap width, so at 1600px the field shows in 278px each side, at 1280px in 118px, and below 1049px the stops fall outside the viewport and the veil closes flat. That is the honest answer when the text is nearly the whole width: on a phone the field is a dim wash and the text wins.
- The photograph's own overlay got the same treatment, because it is now the reduced motion and no-WebGL path and nothing else protects it. Same shape, alpha .90, which measures the meta line at 5.22:1 against 3.31:1 on main.
- BUG FIX: the field stayed amber in every state. three.js copies a Color passed through the constructor rather than holding the reference, so material.color was a separate object and the per frame lerp updated something nothing rendered. The old tablist init block happened to re-alias it; that block is gone, so the aliasing is explicit now.
- Colour, opacity, drift and spread are read straight off --lens rather than eased toward it. The value is already tweened when a reader taps a stop, and it is the pointer itself when they drag, so easing a second time only added about a quarter second between the thumb and the field.
- An independent adversarial review ran before this shipped, not after, and confirmed fifteen defects. Six were severe and all six were mine. What it found, and what was done:
- HIGH, the halfway palette collapse described above, 1.08:1 at the middle of the range.
- HIGH, the page was unreadable in both machine states whenever three.js was blocked or a reader asked for reduced motion: the ground was a layer the field built, so it did not exist on either path, while the palette crossed over anyway. White headline on white paper, 1.04:1, at both widths. The ground is built by lens.js now, which runs on every path. It was briefly body's own background instead, which was the v1.10.0 stacking bug written out a second time: body does not create a stacking context, so the field at z-index -1 belongs to the root's, where negative layers paint before in flow block backgrounds. The field vanished completely, a flat 0.0056 luminance across the whole page. It is an element at z-index -2 again.
- HIGH, the Google state's own payload card was white with near white text, 1.19:1 on every value it reports, because it carries its own #fff and was left out of the crossover. That card IS the Google reading. Both injected cards and their captions cross with everything else now.
- HIGH, the reader pill's overlay lost every rule on twenty nine pages: the rules had been narrowed to a class the homepage's spans carry and the overlay's buttons do not, leaving two default operating system buttons with no reader dots and no selected state. Both are matched now, and the overlay is pixel identical to main again.
- HIGH, a full width strip of the hero swallowed vertical scrolling on touch. A swipe starting anywhere on the control scrolled 0px against 656px on the shipped page, because every frame of it was scrubbing the lens and the anchor was dutifully putting the page back. Touch now waits: the gesture belongs to the browser until it proves itself sideways, and the moment it proves itself vertical the control lets go. Measured after: an upward swipe scrolls, a sideways drag still scrubs the full range, a tap still selects. The same fix opens /book/'s pill, which had the same defect since v1.20.
- MEDIUM, the AI state narrows every .wrap to 74ch including the hero's, so crossing that threshold mid drag moved the control 183px sideways and 218px down under the finger, and because the value was read from the live rectangle everything above about 1.6 became unreachable. A drag maps against the rectangle the gesture started with now. Verified reaching 2.0 through the snap.
- MEDIUM, the three stops were not thirds below about 430px, because a flex item defaults to min-width auto and "An AI assistant" held its own width: 96/96/125 at 360px, while the thumb and the value maths both assumed thirds, so the leftmost 19px of that label selected Google. Thirds to within a pixel at every width now, with the type scaling so the longest label still fits.
- LOW, and it mattered more than its rating: the greyscale filter that makes colour recede was landing on the frame, which is an ancestor of the control, so the three reader dots lost all colour in both machine states. Measured 0 saturation. The frame's body carries the index instead. The dots hold 211, 196 and 155 through all three states.
- LOW, comments that had become untrue: three files still described the lens as a /book/ pilot, the field as mounting into .hero-media with the photograph as its fallback, the filter as never touching the control, and the veil's opening width as 1100px when it is 1049px. All corrected. A comment claiming a measurement that is not true is a defect in this repository.
- LOW, dead code a visitor could see the effect of: the frame's scan line had lost the only code that ever triggered it and was a permanently empty 3px strip, so it is gone; a declared token was read nowhere; and a mouseleave was bound to an element with pointer-events none, so the pointer repel never released.
- Three more, found by measuring every value a drag can be held at rather than only the three it settles on. The reader tags faded in on their own value, so they were translucent for most of the band and read 2.29:1 held a quarter of the way open; they slide open at full opacity now. The AI band's cross fade dipped main to 0.55 opacity at its midpoint, which composites everything toward the page behind it, so the smallest label read 3.83:1 there; the dip is set by contrast now rather than by taste, at 0.25. And the two unchosen stop labels sat under a translucent tint of the thumb mid travel at 2.95:1, so all three labels carry full ink and which one is chosen is left to the thumb, the weight and the ring on the dot. All three changes reach /book/ as well, which had the first two since v1.20.
- Also fixed, pre-existing and found by measurement: the homepage header nav was --panel-dim on the header's own 84% panel, 4.05:1 at every scroll position and in every state, so it is the lit variant now at 4.97:1. And the ticker, the four stat cards and the Google state's lens tags kept their light palette while the page went dark, at 2.94:1, 2.94:1 and 2.86:1.
- Contrast, worst pixel under the glyphs with the text masked out. Settled states, live field: nineteen surfaces in all three states at 390px and 1280px, range 4.97:1 to 19.71:1, nothing under 4.5:1. Held mid drag, with the canvas replaced by solid white, which is the worst an additive blend can produce: twenty seven surfaces at twelve values of --lens from 0 to 2, at both widths, zero readings under 4.5:1. Veil geometry re-measured the same way at 1024, 1048, 1049, 1100, 1101, 1280, 1440 and 1600px: worst 5.36:1.
- Scroll position: measured the way v1.20.1 settled on, by pinning the element under the reader's eye and tracking it, drift is 0px at 390px and 1px at 1280px across six state changes, which is what /book/ measures on the same run. Reported rather than fixed: the individual character under that point can still move up to 48px entering the AI state at 1280px, because the paragraph it sits in reflows from the proportional face to monospace and grows two lines. No anchor can hold a character inside a paragraph that is reflowing around it.
- Reported rather than fixed, second one: at 1280px the AI state pushes the control 94px below the fold, because holding the reading position is worth more than holding the control and the hero grows underneath it. At 390px it stays on screen throughout. A reader who wants the control back scrolls a little.
- Verified in Chromium at 320, 360, 390, 420, 430, 520, 768 and 1280px: no horizontal overflow, no clipped control labels, no console errors. The field's clip follows the hero while scrolling in the person state and opens to the whole viewport as the value rises; held at 0.49 the clip, the ground and the hero band are each half way. Reduced motion reaches a state with no tween, the field is never built, and the hero falls back to the photograph. With JavaScript off there is no field, no ground, no veil, no lens markup and no webgl-on: the page is the photograph, the three static stops and every word of content. Every other page renders pixel identically to main, /book/ included, in all three of its states.
- Note on how this was built: there is no GPU in the environment this was made in, so the segment budget is a workload number, not a timing. Particles are unchanged from the hero only field and the sub-700px halving carries through; what bounds the page wide states is the segment cap. The three.js CDN is also blocked there, so the field was tested behind a local interception of that one request. That harness is test only and is not in the repo: the pinned r128 tag and its SRI hash in index.html are untouched.
- Fixed in v1.21.1: /colophon/ and this file both said the site was hosted on Netlify. Corrected to Cloudflare Workers. Worth correcting deliberately rather than as a side effect of this change.

## v1.21.1
- Corrected the hosting claim. /colophon/ said "Hosted on Netlify" and this README carried a "Deploy to Netlify" section, both left over from before the move to Cloudflare Workers. The colophon now reads "Hosted on Cloudflare Workers, deployed straight from a public GitHub repository", which is accurate and also says something more interesting than the old line did. The README deploy section describes the real pipeline, including the edge caching behaviour that makes a fresh deploy look stale. The analytics note now names Cloudflare Web Analytics rather than Netlify Analytics.
- Copy only. No CSS, no JavaScript, no structural change.

## v1.22.0
- The veil follows the text now. In v1.21.0 it was one 90deg gradient, which makes its alpha a function of x and nothing else, so it could not tell a paragraph from the space between two paragraphs. On a phone, where the text column is the whole viewport, that means it dimmed empty space exactly as hard as it dimmed text. Measured, brightest field pixel inside the text column over eight frames: 0.0166 luminance in a text band and 0.0166 in the gap between two blocks, at 390px and at 1280px, in all three states. A ratio of 1.00. It was not a mask, it was the lights turned down on the whole page, and the field read as smudges rather than as light.
- The shape comes from the page, not from a percentage. For every run of text in main, the veil takes the union of that run's own line boxes and the box of the smallest element holding it, in document coordinates, pads it, merges what is close and emits the result as a mask. Solid over text, transparent between. The horizontal shape stays where it was, in the background, so wide screens still open their gutters; the two multiply, because a mask multiplies whatever alpha it is applied to, which gives dense where text is and open where it is not without needing mask-composite and its patchier support.
- The feather at each band edge is a share of the gap it has to fit inside rather than a fixed distance. Forty pixels either side of every band closes any gap under eighty and the mask goes straight back to being a flat dimmer, which is the bug. A share opens a small gap a little and a generous one completely.
- Same measurement after: 0.0166 in a text band, 0.9553 in a gap. A ratio of 57.5x, at both widths, in all three states.
- The veil is a child of the field, so it inherits the field's clip and cannot darken anywhere the field is absent. It was tried as a body level sibling carrying its own copy of the spill geometry, and an independent review caught the two copies disagreeing: at lens 0.3, scrolled past the hero, the field's clip removed all of it while the veil still covered the whole viewport at .90, so the page went black under a palette that is still light. Dark ink on black, 2.06:1 against 7.33:1 on v1.21.0, and a drag can hold that value. A shape that has to agree with another shape will eventually not. Being a child makes it viewport positioned, so the mask is sized to the document and slid by the scroll offset instead, written in the same handler and the same frame as the field's clip.
- BUG FIX, pre-existing since v1.21.0 and found while verifying that one: the palette step was not a step. Multiplying by a thousand leaves a ramp a thousandth of a lens unit wide, and the control writes the value to four decimal places, so a drag can land inside it and get exactly the mid grey collapse the step was introduced in v1.21.0 to prevent. Measured 11.09:1, and as low as 1.1:1 on the case study heading. The ramp is a millionth wide now, narrower than any value the control can write, and it flips on the same value as the lens-goog class: 0.4999 reads 17.58:1 and 0.5 reads 15.88:1, with nothing reachable in between.
- Two more from the same review. The gutters going fully open made the no mask-image fallback weaker than the thing it falls back from, so opening them now sits behind an @supports test and without it they are what v1.21.0 had. And the ResizeObserver was never stored, so the reduced motion teardown removed the three listeners and the rAF but left a 218 node forced layout walk running against a detached node.
- The gutters are fully open now. Nothing in them is text at any width, and the vertical mask is what decides where the veil is needed at all.
- Brightness, mean field luminance over the viewport with the page content hidden, against v1.21.0: Google 0.00427 to 0.00790 at 1280px, and its brightest pixel 0.4174 to 0.8174. At 390px, where the gaps are the only opening there is, Google's brightest pixel goes 0.3518 to 0.8145. Person 0.11365 to 0.14915 at 1280px.
- AI still recedes, and by more than it did. It rose least of the three, 0.00354 to 0.00453, and sits at 57% of Google's mean where it was at 83%: the opened gaps give Google's mesh far more to show than AI's, which is the relationship that state is supposed to have.
- BUG FIX found while building this: moving the veil out of the field shifted every layer down one, which left .hero-media at the same depth as the field and later than it in the document, so an opaque hero band painted straight over the particles and the amber went out. The stack is now written down and checked: ground at -4, the hero band at -3, the field at -2, the veil at -1.
- BUG FIX: line boxes alone are not enough to build a band from. A heading whose element box is 54px tall has a 38px line box inside it, so a band built from the line box began eight pixels below the top of the element and the mask was still climbing its ramp where the first row of glyphs sat: 2.65:1 on that heading and 2.21:1 on the case study body, against a solid white field. The band takes the element's own box as well now.
- BUG FIX: the mask went stale. The page changes shape under it as a reader goes down it: the reveals settle, images arrive, and the lens re measures its two cards when its fetch resolves. A mask built once and never rebuilt read 0.604 alpha at a paragraph's own top edge and 3.53:1 against a white field. It is rebuilt now on lens:change, resize, scroll, fonts.ready and a ResizeObserver on main, rAF throttled so it never runs twice in a frame. One rebuild walks every run of text in main, which is a little over two hundred of them, and two independent measurements put it at 0.8 to 2.5ms.
- A measurement trap, recorded because it invalidates any contrast number taken the obvious way. The worst pixel harness finds glyphs by differencing two screenshots, one with the text transparent. The field animates, so with it running the difference is polluted by every particle that moved between the two shots and the glyph set is noise. Every contrast figure here is taken with the canvas hidden and the field replaced by a solid white plane, which is also the brightest an additively blended field can ever be, so it is the worst case as well as a still one.
- Contrast, worst pixel under the glyphs, against the actual text colours and against that white field: zero readings under 4.5:1 across 27 surfaces at twelve held values of --lens from 0 to 2, at 390px and 1280px, and across eight widths from 1024px to 1600px, worst 5.36:1. The binding constraint is the veil's own density over text, which is unchanged at .90, not the mask.
- Verified: the sticky header holds 4.97:1 at every scroll position in every state, which it has to be checked for separately because a veil positioned in the document cannot cover a header positioned in the viewport. Reduced motion and a blocked three.js both still render a readable page, worst 6.03:1, with no field, no veil and the photograph back in the hero. With JavaScript off there is no field, no veil, no ground and no webgl-on. Every other page renders pixel identically to v1.21.0, /book/ included, in all three of its states.
- Honest limit, on a phone. The safety pad and the feather both have to fit inside the gap they are opening, and at 390px the gaps between consecutive paragraphs run about 28px, so those close and the field opens in the larger ones: the band above the hero copy, the padding around each section, the space between rows. The tallest gap in the document opens completely. That is why the phone numbers improve on the brightest pixel far more than on the mean.

## v1.22.1
- The person state read as faint smudges rather than as a field, and the cause was neither of the two obvious ones. Both were ruled out by measurement, not by argument, and both are recorded because ruling them out is most of the value.
- Not the double veil. The v1.11.0 hero overlay still exists, but body.home .hero-media carries z-index:-3, which makes it a stacking context, so its ::after paints entirely below the field at -2. Turning the overlay off changes the person state by nothing: 0.12115 to 0.12115 at 390px, 0.14697 to 0.14695 at 1280px. It is inert on the WebGL path. It is not redundant either: on the no WebGL and reduced motion path it is the only protection the hero has, at 5.22:1 on the meta line, so it stays.
- Not the segments either, though they are most of what Google's brightness is. Stripping Google's lines takes it from 3.46 per cent of its area lit to 1.43. But adding 380 segments to the person state took it from 1.24 per cent lit to 2.64 and left it with zero pixels above 0.10 luminance, which is the shape of a ceiling rather than a shortage of light.
- The cause. The veil's horizontal stops were calc(50% - var(--w) / 2 + var(--gutter) - 16px) and its mirror. --w is 1100px, so on a 390px phone that stop computes to -351px, entirely off screen, and CSS clamps a gradient whose stops run backwards: the veil sat flat at .90 across the full width and the horizontal falloff the rule claims to have did not exist at that width at all. The vertical mask could not save it, because the phone hero is 89 to 98 per cent glyph rows depending on how tightly the band is drawn, and its one gap is 24px wide. Measured over the area the field can actually reach: 100.0 per cent of it under mask alpha above 0.75 and 0.0 per cent open, at both widths, against 8.5 per cent open for Google at 390px. There was nowhere for a particle to show. The brightest pixel in the band was byte 35 of 255.
- The stops come from the wrap's own content edge now, max(0px, (100% - var(--w)) / 2) + var(--gutter), which is the viewport inset by the gutter below 1100px and the centred column above it. Same rule at every width, derived from the layout rather than from a box that is off screen half the time.
- Full density is reached eight pixels outboard of that edge rather than at it, and an independent review is why. A focus ring sits outside the box it belongs to, so on an element flush with the column edge it lands in whatever the veil is doing just outside it. Measured against a solid white field, the first version of this change dropped such a ring from 3.51:1 against the background beside it to 1.12:1. The old stop's 16px of slack had been carrying that and nothing said so. Eight pixels of full density puts every ring back on .90, measured 0.902 at both pixels a ring occupies, at both widths.
- Point size is per state now, the way opacity already was: 3.4 in the person state against 2.4 in the other two. That is where the person state gets back the light the focus ring guard cost it, rather than by reopening the eight pixels the ring sits in. Particle count also went up, 200 to 280 below 700px and 380 to 500 above, which is one buffer and lifts all three.
- Coverage of the field's own area, six frames, as the share of pixels above 0.010, above 0.030 and above 0.100 luminance. At 390px, person before: 1.24 per cent, 0.01, 0.00. After: 13.94, 1.68, 0.85. Google before: 3.46, 1.41, 0.52. After: 5.60, 2.53, 1.05. At 1280px after: person 9.72, 2.44, 1.28 and Google 6.58, 3.31, 1.40. AI after: 3.69, 0.65, 0.01, which is under one per cent of Google's brightest share, so it recedes by more than it did. Coverage rather than mean or peak, because the mean of a mostly black frame is dominated by the black and the peak is one pixel.
- The segment budget rises with the count, 1200 to 1580 and 600 to 840, and had to. The link buffer is filled in index order and the outer guard stops the search once it is full, so with the count raised and the budget held the mesh reached a smaller share of the grid: the google formation uses fixed columns, so more particles make it taller rather than denser, and the links ran out further from the top of it. Holding segments per particle constant keeps the mesh covering what it covered. Found by review, confirmed against the code and visible in the unveiled mesh.
- BUG FIX, pre-existing and found by the same review. The link search ran on threshold > 0, and the threshold is mixed from 0, so a hundredth of the way out of the person state it is 0.042: no pair is ever inside it, the buffer never fills, the guard never trips, and the entire pair scan ran every frame and drew nothing, through roughly the first quarter of the drag. Mean spacing in this field is about four world units, so it gates on threshold > 1 now.
- Two dead ends, recorded so they are not tried again. Lowering the veil and protecting the hero copy with a text shadow halo instead does not work: blur spreads the ink, so directly under a thin stroke a halo is far weaker than it looks, and at .55 with a four layer halo the hero eyebrow reads 2.87:1 against 5.36:1 at .90. And segments in the person state went backwards, 0.75 per cent of the area clearly lit against 0.88 with none: under a veil that dense, spreading the same emission along a line rather than concentrating it in a point costs more than the extra coverage is worth.
- Contrast, worst pixel under the glyphs against the actual text colours, with the canvas replaced by a solid white plane: zero readings under 4.5:1 across 27 surfaces at twelve held values of --lens from 0 to 2, at 390px and 1280px, on both the settled and the held mid drag battery. The veil's density over text is unchanged at .90. No text that the field can reach renders outside the wrap's content box; the footer wordmark does, but the footer paints its own opaque ground over the field.
- Verified: the no mask-image fallback profile is v1.21.0's to three decimal places at both widths, which it has to be, because the fallback must never be weaker than the thing it falls back from. Reduced motion and JavaScript off both build no veil element at all, so neither is touched by this, and every contrast target passes in both. The band versus gap ratio the v1.22.0 mask exists for is unchanged at 57.5x, at both widths, in all three states. Every other page renders pixel identically to v1.22.0, /book/ included, in all three of its states.
- Cost: the JavaScript the field runs each frame, timed from the top of the tick to the render call, is 0.1 to 0.3ms at 390px and 0.3 to 0.5ms at 1280px, p95 0.8ms, and the same to the tenth of a millisecond at the v1.22.0 counts and budget as at these. A whole frame time on the machine this was built on is the software rasteriser and says nothing useful, which is why it is not quoted.

## v1.39.1
- THE BURGER BREAKPOINT IS 50em NOW, NOT 700px, AND THE UNIT IS THE POINT. Swept in 2px steps from 620 to 1600 on three documents: the nav clipped "Contact" by up to 27px from 702 to 730px, with the page's own scrollWidth equal to its clientWidth throughout, because .top nav is overflow-x:auto with the scrollbar hidden. 50em is 800px at a default 16px root.
- A FIXED PIXEL BREAKPOINT WOULD HAVE FIXED ONE CASE OUT OF THREE. The nav is sized in rem, so anything that widens the text widens the nav without widening the viewport, and the sweep only shows the band at the settings it is run under. Measured: with the web font blocked, which is what a reader sees during a FOUT or behind a proxy that blocks fonts.gstatic.com, the clip runs to 776px rather than 730px. At an 18px browser default it runs to 808px. At 20px it runs to 990px. A 760px breakpoint leaves all three clipping and an 800px one leaves two.
- A media query in em resolves against the browser's default font size rather than the page's, which is exactly the preference that widens the nav, so the breakpoint follows the reader. Verified by launching with --blink-settings=defaultFontSize, which moves the real default rather than overriding html{font-size} in the page, because the latter does not move an em media query and is why the earlier sweep could show the problem and not the fix. At a 16px default the burger runs to 800px and the nav is clear from 802 to 1600. At 18px, burger to 900, clear from 902. At 20px, burger to 1000, clear from 1002. Zero clipped items and zero page overflow at every width in all three, with the web font loaded and with it blocked.
- The gap tightening from v1.33.1 stays, and it is still load bearing rather than vestigial: reverted, the clip runs to 776px at a 16px default even with the font loaded. Its bounds moved to em for the same reason as the breakpoint.
- Four media queries moved together, because the breakpoint is not one rule. The burger and its panel; the no JavaScript wrapped header, which otherwise clips for exactly the readers who cannot have the burger; the reduced motion partner of the burger block; and both /games/ hero height rules, which subtract a header height that depends on which header that band gets. The homepage hero rhythm block stays at 700px: it is about small screen spacing, not the nav, and the homepage at 701 to 800px now gets the burger with its ordinary hero.
- Verified: 66 of 66 document and width pairs correct, every document at 760px showing the burger with the nav hidden and at 900px showing eight items with nothing clipped. The burger opens, shows eight rows, moves the shell, traps focus, closes on Escape and returns focus to the button, at 390, 701, 740, 799 and 800px. The header is 65px at every width from 560 to 1600. With JavaScript off the nav is one row of eight with nothing clipped at 690, 740, 799, 801 and 900px.
- A PRE-EXISTING GAP GOT WIDER AND IS RECORDED RATHER THAN CHASED. /games/ with JavaScript off takes its hero height from a constant, calc(100dvh - 92px), and 92px was the wrapped header's height when it had more rows. Measured now: the JS off header is 91px below about 550px, where the nav wraps to two rows, and 51px above it, so the hero falls 41px short of the fold and a strip of the next section shows under it. That band was 551 to 760px before this version and is 551 to 799px now. The fix is a height that tracks the real header rather than a constant, and the only pure CSS version of that is a second media query keyed to where the nav wraps, which is font dependent and a new fragility. Not taken. The +9px overshoot above 800px, where the hero subtracts 56px from a 65px header, is the same class of staleness and also unchanged.

## v1.39.0
- REEL HAIKU'S LINE NO LONGER DEPENDS ON READING ORDER. "The same puzzle, drawn from films" only parses if Novlr is above it, which it is for somebody scrolling and is not for somebody arriving by deep link, by heading navigation, or through a screen reader's heading list. It now reads "The same puzzle as Novlr, drawn from films", which is the wording llms.txt has carried since v1.35.0. This was flagged in the v1.35.1 entry and left as the owner's wording; it is fixed now. Verified: one line at 390, 860, 1280 and 844 by 390, zero horizontal overflow at all four, and the other two puzzle lines untouched.
- LANDSCAPE IS PART OF THE STANDARD VERIFICATION SET NOW, and the reason is in CLAUDE.md beside it. Three consecutive versions checked the /games/ hero control at portrait widths only. v1.37.1 then measured it at 844 by 390 sitting at y417 in a 390px viewport, with not one pixel of it on screen, because min-height:420px on .hero-game wins below about 485px of viewport height and the hero's foot leaves the screen. The rule added is broader than that one element: anything positioned against the bottom of the viewport gets checked in landscape, because a short viewport hits min-height floors no portrait width reaches and the element's anchor stops being the fold.

## v1.38.3
- THE /books/ LEDE NAMED THE WRONG DIFFERENCE AND MADE A CLAIM THE SITE CANNOT DEFEND. It read "One is sampled here and sold on Amazon, the other is free here in full", which sets up Amazon as the axis. Both books are on Amazon, and the same page says so three times in the SMSF block: the meta line "Also on Amazon in Kindle and paperback", the "Buy on Amazon" button, and the JSON-LD workExample. The real difference is how much of each text this site gives away, which is what the eyebrow two lines above already said.
- Worse, the trailing clause "because Kindle Unlimited exclusivity caps samples elsewhere at 10 per cent while a book is enrolled" asserted by implicature that SMSF Property Investing is NOT enrolled in Kindle Unlimited. Nothing on this site states that, and README v1.9.5 records it as an unresolved open item: "If its Kindle edition is enrolled in Kindle Unlimited / KDP Select, that breaches Amazon's digital exclusivity terms... Not resolved in this version, flagged to Nathan." Writing a sentence whose sense depends on the answer to an open question is exactly what CLAUDE.md rule 4 forbids.
- Rewritten so every clause is sourced: the two books' subjects, that both are on Amazon, that all of the second is here and an excerpt from every chapter of the first, and that the first's Kindle edition is in Kindle Unlimited until September 2026 with exclusivity capping samples at 10 per cent. Every one of those is on /book/ or /super/ as they shipped. Nothing is asserted about the second book's enrolment. "covers residential property in your own name" also restores the word the first draft dropped: /super/ says the first book covers residential property, and "investing in your own name" widened its stated scope.
- THE CHAPTER NUMBER ON THE SELECTED PICKER ROW HAS BEEN INVISIBLE SINCE THE PICKER SHIPPED. .sampler-nav button.on sets background:var(--ink), and .sampler-nav button.on .num sets color:var(--ink). Measured: rgb(23,20,16) on rgb(23,20,16), 1.00:1. The "00" beside "Introduction" is painted in the button's own background colour. It came to /books/ with the port. Now colour mixed to 62 per cent paper over the same ground: 7.25:1, still quieter than the label's 17.58:1, which was the intent.
- THE SECTION HEADINGS INSIDE EACH BOOK WERE 16.96px AGAINST 16.48px BODY TEXT. "What is in it", "Read a page from every chapter", "Why the full text isn't here yet" and "The chapters" were h2 on the pages they came from and became h3 here, one level under each book's title. .prose h3 is 1.06rem, so they were half a pixel larger than the paragraphs under them. They now take the .prose h2 treatment, 24px, which is the size they had before the port and level with the thirteen excerpt headings inside the picker, exactly the relationship /book/ had. Sizing them between the two inverts it: at 1.3rem the excerpt titles came out larger than the heading introducing them.
- ARIA-CURRENT WAS SILENTLY DROPPED ON ALL SIXTEEN PAGES UNDER /super/ by v1.38.2, reverting a site convention. Those pages carried aria-current="page" on the Super nav item, the same way the three /work/ case studies carry it on Work. Replacing two nav items with one removed the attribute along with the anchor it sat on. Restored: 16 documents now mark /books/, 4 mark /work/, and the other six destinations mark themselves. Six documents carry no current item, which is correct: the homepage, /colophon/, /updates/, /writing/ and its two essays are not nav destinations.
- THE TWO RETIRED HUB FILES ARE DELETED, and the reason is the redirect rather than tidiness. book/index.html and super/index.html were still deploying at the exact paths the two new 301s target, and this is the first time a rule in _redirects has had a live asset as its source: the four pre-existing rules all point away from paths that no longer exist. Whether Cloudflare evaluates _redirects before or after asset matching could not be checked from here, because the network policy blocks the documentation and a local static server ignores the file entirely. Deleting is correct under either answer: if assets win, the redirect would never have fired and now will; if redirects win, nothing changes. Their canonicals pointed at URLs that now 301 away, which was its own small mess.
- THE NAV CLIP IS NARROWED, NOT FIXED, and v1.38.2 said it was. Swept every 2px from 690 to 900 rather than sampled at five widths: with nine items the band was 702 to 778px with two items clipped, "Are you AI?" and "Contact". With eight it is 702 to 730px with one, "Contact". The v1.38.2 entry's "nothing is clipped now at 700, 760, 860, 1000 or 1280" is true of those five widths and steps straight over the survivor, which is the failure mode CLAUDE.md records from v1.26.0. Not fixed here: the fix is raising the burger breakpoint past 730px, which the stylesheet has named as the outstanding fix since v1.9.6, and it is a sitewide nav change rather than part of this merge. The entry above is corrected.
- /updates/ SAID THE READING VERSION IS AT /books/, which holds no chapter text. The sentence was rewired from /super/ without rereading it. It now says the reading version is linked chapter by chapter from /books/, which is what is there.
- The three fragment targets this work introduced, #build-stabilise-leverage, #smsf-property-investing and #sampler, landed under the sticky header. llms.txt and the JSON-LD both cite the first two, so they are arrival points rather than decoration. scroll-margin-top:80px on those three only: measured, each lands at y80 against a header ending at y68, at 390 and 1280. The rest of the site's anchors behave as they always have, which is a sitewide question and not this version's.
- Verified after all of it: 37 distinct internal destinations, every one 200. The heading outline is unchanged and still has no skips. Zero horizontal overflow at 390 and 1280.

## v1.38.2
- NINE NAV ITEMS BECOME EIGHT. "Book" and "Super" are replaced by one "Books" pointing at /books/, in the position "Book" held, between Puzzles and About. Verified on EVERY document rather than a sample: 34 of 34 carry the eight items in the right order with the label "Books". 404.html is the one document with no primary nav and no footer, which is why the count is 34 and not 35.
- The aria-current trap was handled by matching it rather than working around it. The current page's own item carries aria-current="page", so a pattern matching the plain anchor misses exactly those pages, which is how a previous nav change on this project missed four. The replacement matched both anchors with an optional aria-current on either, in one expression, so /book/ and /super/ were caught along with everything else. Eleven documents carry aria-current after the change: the eight nav destinations on their own pages, plus the three /work/ case studies, which mark /work/.
- The burger panel needed no change to its markup, because it is built from the header's own anchors at runtime: one list in the markup, two presentations of it. Only its description map needed the new key, and the two old keys were removed in this commit rather than the last one, so the panel was never showing a row with no description.
- A PRE-EXISTING CLIP GOT NARROWED, NOT FIXED. CORRECTED IN v1.38.3: swept every 2px, the clipped band goes from 702 to 778px with nine items to 702 to 730px with eight, and "Contact" is still cut there. The five widths sampled below step over it. At 760px the nine item nav had one item clipped by its own box, which is overflow-x:auto with a hidden scrollbar, so it was invisible and unscrollable rather than visibly cut. With eight items nothing is clipped at 700, 760, 860, 1000 or 1280, the header stays one row at 65px at all five, and the nav is 554px wide at 1280 against 608 before.
- The burger panel's last row still ends below the fold on a 900px tall phone, at y908, and that is better rather than fixed: with nine rows it ended at y1004. The panel itself scrolls, so nothing is unreachable. Not touched here: shortening the rows is a change to the panel's design.
- llms.txt HAS BEEN WRONG SINCE v1.33.1 AND IS NOW RIGHT AGAIN, without being edited. The Contact entry says "One of the eight destinations in the site's primary navigation". That was true until /puzzles/ was added in v1.33.1 and made nine. It is true again now. The line is not a lucky coincidence worth keeping quietly: it is a claim that drifts every time the nav changes, and the next nav change breaks it again.
- Verified, nothing else moved: all 34 documents diffed full page at 390 and 1280 against v1.38.1, 68 page and width pairs, with a noise floor from renders of the same build. At 390 the diff is zero everywhere, because the nav is behind the burger at that width. At 1280 every page differs by about 2,340 pixels in one contiguous band, rows 28 to 41, which is the nav text and nothing else. No size mismatch anywhere, so no page changed height.
- ONE PAGE READ AS A FAILURE ON THE FIRST PASS AND DID NOT REPRODUCE. /puzzles/ at 1280 came back with differences reaching row 965, outside the header band, against a zero noise floor. Re-measured with three renders of each build and a longer settle: 2,366 pixels, one band, rows 28 to 41, identical across two independent pairs, with a zero floor in both builds. The first reading caught one render with the three framed puzzles still loading, and the floor happened to be zero on that particular pair by luck. This is the same artefact this project recorded in v1.34: a zero noise floor on one pair does not prove the page is deterministic, and a page with live frames on it needs the floor taken more than once.

## v1.38.1
- /book/ AND /super/ NOW 301 TO /books/, and everything that pointed at either points at /books/ instead. _redirects carries six rules now. The four that were already there are untouched and are, in order: /super/residential-vs-commercial/ to /super/commercial-for-residential-investors/, /super/what-and-where-to-buy/ to /super/asset-class-tour/, and the two renamed download filenames, how-to-buy-property-with-your-super.pdf and .epub to smsf-property-investing.pdf and .epub. Nothing in the file touched /super/ itself before this version.
- The two new rules are exact paths with no splat and no placeholder, which is what keeps the fifteen chapter and appendix URLs alive: checked by parsing the file and matching every rule against every live path on the site, 2 paths captured (/book/ and /super/) and 0 of the 15 chapter paths captured. Every rule target returns 200. Cloudflare consumes _redirects as configuration rather than serving it, so a local static server cannot exercise the redirect itself; what is verified here is the file's shape and what it matches, not a live 301.
- THE BRIEF'S ASSUMPTION ABOUT THE BACK LINK WAS WRONG, and the real shape is worth knowing. There is no "Back to the book" link on all fifteen chapter pages. Only two of the sixteen pages under /super/ have a chapnav link to the hub at all: /super/new-landscape/ carries "left arrow The book" as its previous link because it is chapter one, and /super/appendices/ carries "Back to the book right arrow" as its next link because it is last. The other thirteen chapters have previous and next chapter links only, so the only route back to the hub from the middle of the book is the header nav and the footer. Both existing links now go straight to /books/ with no redirect hop. No back link was added to the other thirteen: that is a change to the chapter page design and was not asked for.
- Every internal reference found and changed, by location. The footer pair, "Book" and "Super: the free book", on all 34 documents, replaced by one "Books" row. The homepage: the bento cover tile, the "Read the sampler" button, the /book/ path eyebrow, and the prose link to the second book. /about/: two prose links. /are-you-ai/: two prose links, both of which named the old path in their link text and now name /books/. /updates/: two prose links, one of them also naming the old path. /writing/: one prose link. /book/: its own callout link to the second book. /super/new-landscape/ and /super/appendices/: the two chapnav links. assets/js/site.js: the burger panel's description map and the comment naming the lens pages. assets/js/lens.js: two comments naming /book/ as a lens page.
- The burger panel's description map now carries all three keys at once, /books/ alongside /book/ and /super/, deliberately. Removing the two old keys in this commit would have left the panel showing two rows with no description until the nav change landed, because the map is keyed by href and the nav still carries the old hrefs until v1.38.2. The two stale keys go with the nav items they describe.
- sitemap.xml: 32 URLs now, was 33. /books/ added with a lastmod, /book/ and /super/ removed, all fifteen chapter and appendix URLs kept. Every one of the 32 returns 200. llms.txt: the two book sections merged into one ## Books section carrying both books with accurate descriptions and both Amazon links, followed by the fifteen chapter URLs under a line naming what they are. 28 distinct same site URLs in the file, all 200, neither retired hub referenced.
- The two retired pages were NOT deleted, and that is a decision rather than an oversight. book/index.html and super/index.html still deploy, so /book/index.html and /super/index.html remain reachable by their explicit filenames even though /book/ and /super/ redirect. Their canonicals point at the redirected URLs, so a crawler resolves through to /books/. Deleting them is the tidier end state and was not in the brief; it is reversible either way and the redirect is the thing that matters.
- One comment in lens.js was stale for two versions and is fixed in passing: the lens remembers that somebody has used it once, and the comment explaining the key cited "the ducks gate on /games/" as the pattern it copied. That gate was removed in v1.36.0.

## v1.38.0
- /books/ IS ONE PAGE HOLDING BOTH BOOKS' PITCH. Additive only: /book/ and /super/ still exist and still work, nothing links to /books/ yet, and it is in neither sitemap.xml nor llms.txt nor the nav. Those arrive in v1.38.1 and v1.38.2. This entry was written one commit late, in v1.38.1.
- Build, Stabilise, Leverage keeps its cover, its description, both Amazon links and the thirteen excerpt picker, ported without a line changed. The picker is driven by the site's own generic .sampler handler, so nothing in site.js needed touching, and the lens still counts it live: "15 buttons, no href. A crawler cannot follow these, and every one of the 13 excerpts below is already in the HTML." SMSF Property Investing keeps its cover, its description, the Amazon link, both downloads and the contents list out to all fifteen existing /super/ URLs, which do not move.
- ONE CollectionPage CARRYING TWO hasPart BOOKS, not two loose Book objects, so a search engine cannot pick one arbitrarily. Every value is carried across from the page it came from rather than retyped. The two Books take fragment URLs, /books/#build-stabilise-leverage and /books/#smsf-property-investing, which are the ids of the two sections, because two objects sharing one page URL would be the same ambiguity in a different shape.
- The two books make opposite offers and the lede says why in one sentence rather than a paragraph: one is sampled here and sold on Amazon, the other is free here in full, because Kindle Unlimited exclusivity caps samples elsewhere at 10 per cent while a book is enrolled. That is the fact, not a justification for stating the fact.
- THE .headgrid SELECTOR IS NO LONGER SCOPED TO .pagehead. This page uses that two column block twice inside .section, once per book, and the rule, its 820px override and its lens-ai variant were all keyed to .pagehead, so the grid silently did not apply and the cover fell below the text at full width. All three existing uses of the class are inside a .pagehead, so widening the selector cannot reach them: verified by pixel diff, /book/, /super/ and /about/ identical at 390 and 1280 against a zero noise floor. .bookhead is new, and exists because a bare h2 outside .prose falls back to the browser's own size in the body face.
- Verified: one h1 and zero heading skips, with the five chapter part labels promoted from h3 to h4 so they sit under "The chapters" rather than beside it. All thirteen excerpts reachable by button, by the arrows and by the keyboard, at 390 and 1280, with the dot indicator always singular. Twenty same origin links in main, every one returning 200, including all fifteen chapter URLs and both downloads. The in page anchor resolves. Zero horizontal overflow at 320, 360, 390, 414, 670, 820, 821, 860, 1024 and 1280, with the grid collapsing to one column at 820 and splitting at 821.
- Verified: the lens builds its control, all three states apply with zero overflow in each, and the audit reports zero images without alt, zero undeclared empty alts, zero heading skips, zero vague link text, "nothing found", and "outside main: nothing". With JavaScript off all thirteen excerpts are in the HTML, 23,903 characters of main text at both widths, one h1, no overflow and no lens.
- A CONTRAST HARNESS ARTEFACT, recorded because three elements looked like failures and are not. A full page screenshot paints a position:fixed element once, and the lens pill is a dark fixed chip. Any text it happens to sit over reads its ground as the pill rather than the page, which gave 1.05:1, 1.1:1 and 1.11:1 on three ordinary paragraphs. Running the same harness against /book/ on the previous build reproduces exactly the same artefact on an ordinary prose paragraph there, and /super/, which has no lens, shows none of it. The skip link reads 1:1 for the same class of reason: it is parked off canvas, so its ground is itself. The footer's giant wordmark reads 1.15:1 on every page on the site and is aria-hidden decoration. None of these is new and none is a defect; the harness needs the fixed control hidden before it sweeps.

## v1.37.1
- THE CONTROL WAS ENTIRELY OFF SCREEN ON EVERY PHONE IN LANDSCAPE, and v1.37.0 shipped it. Measured: at 844 by 390 the chip sat at y417 to y461 in a 390px viewport, so not one pixel of it was on screen. Same at 812 by 375, 736 by 414, 667 by 375, 740 by 360 and 390 by 420. This is the page's only visible way onward, so on a phone held sideways there was none.
- The cause is that the hero's foot is only 100dvh while its height rule wins. .hero-game carries min-height:420px, and below about 485px of viewport height that min-height wins instead: the hero stops shrinking, its foot runs off the bottom of the screen, and anything anchored 24px above that foot goes with it. The v1.37.0 comment said the position "is pinned to the fold rather than to a fixed number", which is true of the branch it was written for and false of this one. v1.36's 120px lift covered the same case by accident, not by design, which is why this had never shown up.
- Fixed by adding the overhang back: bottom:calc(env(safe-area-inset-bottom, 0px) + 24px + max(0px, 511px - 100dvh)). 511px is where the hero's foot lands when the min-height wins and JavaScript is off, which is the taller of the two header cases, so the term lifts the control by exactly the overhang and never by more than it needs. Measured after: fully visible at 844 by 390, 812 by 375, 736 by 414, 667 by 375, 740 by 360 and 390 by 420, and unchanged in portrait at y776 to y820. It is also now visible at 740 by 360, where v1.36.1 itself was 5px short.
- A plain bottom declaration now sits ahead of that one as the fallback for a browser with no dvh support. Without it the whole enhanced declaration is dropped on such a browser and the control has no bottom at all, which puts an absolutely positioned box at the TOP of the hero. The hero's own height already depends on dvh, so that browser was already getting the min-height geometry; it should not also lose the control.
- THE SAFE AREA INSETS CANNOT FIRE AS THE SITE IS BUILT, and v1.37.0 described behaviour the page does not have. No document on this site sets viewport-fit=cover: the one viewport meta string in the repository is width=device-width, initial-scale=1. With viewport-fit at its default the layout viewport is already the safe area, so env(safe-area-inset-*) resolves to zero on every device and the chip is always exactly 16px in and 24px up. The "58px up on a phone with a 34px indicator" case in the v1.37.0 comment does not exist. The insets stay, because adding them is the correct form if cover is ever set and the fallback keeps the rule valid meanwhile, but the comment now says they are inert today.
- width:max-content was removed and its justification was wrong. It said max-content "guarantees the pill is its natural width rather than the widest the corner happens to allow". An absolutely positioned box shrinks to fit at min(max-content, available) on its own, and right anchoring leaves it the whole hero to shrink into, so it was asking for a width it already had. Measured with and without: the box is identical at every viewport from 150px up. Removed, with max-width kept as the guard. Re-verified after: 162.58 by 43.94, one line label, zero horizontal overflow at 280, 320, 360, 390, 414, 670, 760, 761, 860 and 1280px, at both 844 and 750 tall.
- A FACTUAL CLAIM WAS ASSERTED IN THE VERSION THAT SAID THE GAME COULD NOT BE LOADED. The v1.37.0 comment states the game's own controls are sound top left and reload top right, as plain fact, three lines above a README bullet saying the frame cannot be reached from here and nothing should be read as if its bounds were known. Both cannot be true. The sentence is the brief's, not a measurement, and the comment now says so.
- Recorded, not fixed: at 761px wide and up the hero uses calc(100dvh - 56px) under a 65px header, so its foot lands 9px past the fold and the chip's real clearance there is 15px rather than the 24px the rule asks for. Still fully visible, measured at 761, 860 and 1280. The 9px is the hero's own arithmetic and predates this version. Also recorded: the box shadow's right and bottom tails are truncated by .hero-game's overflow:clip in the last two or three pixels, where the alpha is near zero. Measurably cut, not perceptibly.
- A FINDING THAT DID NOT REPRODUCE, recorded because four published numbers were nearly changed on it. The review measured the chip at 161.41 by 43.94 and v1.36.1's at 184.67 by 50.41, and derived corrections to the x coordinates and to both hero coverage percentages from that. Re-measured here at deviceScaleFactor 1 with JetBrains Mono confirmed loaded: 162.58 by 43.94 and 178.42 by 50.41, which round to the published 163 by 44 and 178 by 50, at 320, 360, 390 and 414px, with left edges 141.42, 181.42, 211.42 and 235.42. The review ran a different Chromium build from the one this project's harness uses, having found the bundled revision missing, and its font metrics differ. Every number in the v1.37.0 entry stands. The lesson is the older one: a measurement taken on a different engine is a different measurement.
- site.js had a comment stale for two versions. /games/ opts out of the reader pill "because the pill sits bottom right and collides with the More link the hero gate reveals in that corner". The gate went in v1.36. The opt out was still right and its reason was not. Rewritten: the collision is now permanent rather than conditional, because v1.37 moved that link into the corner for good.
- Re-verified after all of it: JavaScript off renders 162.58 by 43.94 and navigates, in portrait and in landscape. Reduced motion gives animation-name none, transition 0s and no running animations. The game loads, paints, parks to src null at the foot and revives on return, with no page errors.

## v1.37.0
- THE CONTROL SAT ON THE SLINGSHOT, so it moves to the bottom right corner. v1.36 centred it 120px off the hero's foot and argued a corner pill reads as a footnote on somebody else's game. That was right about the reading and wrong about the cost: centred at y674 to y724 it covered SnapHit's slingshot, which is the game's primary control and the thing a player most needs to see. Obscuring the game to advertise the page below it is the worse trade. The pill takes the corner and accepts reading as a footnote.
- Position and size, measured. right:calc(env(safe-area-inset-right, 0px) + 16px), bottom:calc(env(safe-area-inset-bottom, 0px) + 24px). The insets ADD to the margin rather than taking the larger of the two, because a home indicator and a rounded display corner are additional to the margin the design wants, not a substitute for it: 24px up on a device with no inset, 58px up on a phone with a 34px indicator. The chip is 163 by 44, down from 178 by 50, on tighter padding and a smaller face. The label is unchanged at "See the games". At 320, 360, 390 and 414px it sits at x141, x181, x211 and x235, top y776, lower edge y820, in a hero ending at y844. At 860 and 1280 the lower edge is y829 in a hero ending at y853. Zero horizontal overflow at all six.
- CORRECTED IN v1.37.1: the bullet below reasons only about the branch where the hero's height rule wins. Below about 485px of viewport height the min-height of 420px wins instead and the hero's foot leaves the screen, which put the control entirely off screen on every phone in landscape.
- ABOVE THE FOLD DEPENDS ON WHICH MODEL OF BROWSER CHROME YOU USE, and the two disagree here for the first time. DYNAMIC: chrome expanding makes 100dvh smaller, so on a 390 by 844 phone showing a 94px band the dynamic viewport is 750 tall, the hero ends at y750 and the chip lands at y682 to y726. Measured at a 750 tall viewport: inside the fold at all four widths, and the screenshot shows it plainly. OVERLAY: chrome painted over a viewport that stays 844. Under that model the chip is at y820 and 70px below a 750px fold at all four widths. The dynamic model is what a browser actually does, and it is what the rule is written against, so this ships. The overlay model is the conservative one the v1.36 brief used, and v1.36 passed it only because a 120px lift is larger than a 94px band. An inset big enough to pass the overlay model would be 94px or more, which puts the chip back over the slingshot and undoes this version.
- THE PLAY FIELD COULD NOT BE MEASURED, which is the one thing this version was asked to measure rather than assume. This container's egress policy denies CONNECT to snap-hit.online at the proxy, verified as a 403, and the same for every alternate route tried. No copy of the game exists in this repository. So the field's real bounds inside the frame at 320, 360, 390 and 414px are not known here and nothing below should be read as if they were.
- What CAN be said is the arithmetic, against the brief's own figure of 70 to 90px of margin each side at 390px. If the field is straight sided, its right edge at 390px is x320 or x300, and the widest chip that would clear it in the corner is 54px or 74px. If it widens toward the bottom, the clearance is smaller still. The chip's irreducible width is about 60px before a single character: a 16px chevron, an 8px gap, 33.6px of padding and 2px of border. So NO chip carrying a label clears the field horizontally at 390px on those figures, and narrowing the label buys nothing against that reading. The label stayed.
- The horizontal reading may also be the wrong question. The move drops the chip 102px as well as moving it right, from y674 to y776, so it now sits 52px below where the slingshot was. Whether the field extends down into y776 to y820 is exactly what could not be checked. The reconstruction used for the screenshots draws the field all the way to the frame's foot, which is the pessimistic assumption; on that reconstruction the slingshot is clear and the chip overlaps the lower field.
- Verified against the live frame: the label reads 16.73:1 over rgb(10,9,7) on white, black, mid grey and neon yellow grounds and at eight separate moments of a moving frame, from 389 measured glyph pixels. The chip is opaque, so the number does not move. The focus ring keeps outline-offset:-6px, lands 4px below the chip's top edge and reads 4.14:1 against the chip's own ground both above and below, identically on all four grounds, so it is still on the chip and not on the game.
- Verified, the boundary is cleaner than before the move. On a 6px grid across the hero at 390px, 8203 points hit the game frame and 182 the control, widest run 162px of 390 on any row, and the chip now covers 2.35 per cent of the hero against 2.96 before. A tap on the game registers in the framed document and moves neither page nor hash; a tap 12px to the left of the chip goes to the game; a tap on the chip navigates; a 260px drag delivers ten touchmoves to the game and never fires the control. Same at 1280px, where the chip covers 0.71 per cent.
- Verified: interactive one second after DOMContentLoaded with no gesture, elementFromPoint at its centre hitting its own span, and a click navigating to #more. With JavaScript off it renders at 163 by 44 and the link works. Reduced motion gives animation-name none, transition 0s, no running animations and zero moving pixels; with motion allowed the only movement is 79 to 108 pixels confined to the chevron's own rows. The game loads, paints, parks to src null at the foot and revives on return, with no page errors.
- Verified untouched: everything below the hero on /games/ is pixel identical to v1.36.1 at 390, 860 and 1280px against a zero noise floor, over regions 5278, 5058 and 5199px tall, so the three cabinets, the studio card and the dark treatment did not move. The homepage and /puzzles/ are pixel identical at 390 and 1280.
- A harness artefact worth recording. A first reduced-motion pass reported 6224 moving pixels in the chip with motion allowed, against 124 in v1.36. Nothing was wrong with the page: that run focused the control and took its first screenshot while the 0.2s background transition was still in flight, so shot one caught the chip mid-change. Given 600ms to settle, the same measurement reads 79 to 108, all of it in the chevron. A screenshot taken during a transition is not a measurement of an animation.

## v1.36.1
- "THE FIRST FOCUSABLE THING INSIDE THE HERO" WAS FALSE, and it falsified the reason v1.36.0 gave for deleting the skip link in the same sentence. Measured tab order on the shipped build at 390 by 844: a.skip, a.brand, button.burger, iframe.hero-game-frame, a.hero-down. The iframe takes a tab stop, so the control is reached only after tabbing into the game, which is exactly the property the entry held against .hero-skip. Old build for comparison: the same, with a.hero-skip between the iframe and a.hero-down. The control is the first VISIBLE affordance in the hero and the first link in it, both true and both defensible. It is not the first focusable thing.
- The skip link's deletion still stands, on the reasons that survive. It was an escape hatch from a gate that no longer exists, it targeted the same #more as the More control, and it sat one tab stop from it, so it duplicated a control that is now visible without being focused. What it was never able to do, on either build, is let a keyboard user past the game: it sat after the iframe, so the game's tab stop came first either way.
- THE FOCUS RING WAS THE ONE PLACE GAME PIXELS STILL REACHED THIS CONTROL, and v1.36.0 made that load bearing without re-checking it. The sitewide rule draws 2px of #3B6BFF at outline-offset:3px, which on an opaque chip puts the entire indicator outside the chip and onto the cross origin game. Sampled down the chip's centre line: 1.58:1 on a white ground, 1.79:1 on mid grey, 1.38:1 on neon yellow, 4.61:1 on black. Three of four under the 3:1 a focus indicator has to hold, and a different number for every frame the game paints. The chip's own focus background change, #0A0907 to #171410, is 1.36:1 and carries nothing.
- Fixed with outline-offset:-6px on this control only. The ring now lands 4px below the border with the chip's own ground on both sides, and measures 4.14:1 against it on all four test grounds identically, which is the whole point: the indicator no longer depends on the game. Not 3px, because the chip is a pill and at the tips of the radius a 3px inset leaves the ring's outer neighbour in the antialiasing at the edge. The border turning blue on focus is a second cue, not the indicator; it sits on the game by definition and reads 1.55 to 4.73:1.
- This is pre-existing sitewide and is not fixed sitewide. Every focused link on the site draws its ring at +3px, and everywhere else it lands on a page background this stylesheet controls. This control was the one place it landed on a document nobody here owns, and the deleted body.arcade .hero-skip rule carried the comment saying that job mattered. The job moved; the rule did not.
- TWO NUMBERS IN THE v1.36.0 COMMENT WERE WRONG AND ONE WAS ARITHMETICALLY IMPOSSIBLE. style.css said #EFEBE2 on #0A0907 is 16.6:1 while the changelog said 16.73:1, and 16.73 is right: L(#EFEBE2) is 0.832584, L(#0A0907) is 0.002752, and the ratio is 16.7307. The fold arithmetic mixed two viewport models that cannot both hold, saying the hero runs to y844 and the control lands at y724 inside the 750px that survives 94px of chrome. Both halves are right, in different viewports. The comment now states the actual property: the lift is 120px off 100dvh, so it is measured against the dynamic viewport. With the chrome collapsed the hero ends at y844 and the lower edge lands at y724; with a 94px band expanded, 100dvh resolves to 750, the hero ends at y750 and the lower edge lands at y630. Above the fold either way, because it is pinned to the fold rather than to a number.
- And "the old rule put it at y802" compared a different edge from the clause before it. y802 is the old control's TOP; its lower edge was y842. Measured on d1f9ad3 at 390 by 844: the v1.17 rule sat 16px off the hero's foot and the gate's own transform pushed it 14px further down. The comment now says which edge, so the 120px lift can be checked against it.
- A COMMENT OUTLIVED THE RULE IT INTRODUCED. v1.36.0 deleted body.arcade .hero-skip and reworded the two other stale .hero-skip comments, and missed the one directly above the rule it deleted. It sat between body.arcade .pagehead::before and the ::selection comment, describing a skip link that no longer exists. Removed. grep for hero-skip across the repository now returns only the two lines that say it was removed and why.
- Nothing else moved. Zero horizontal overflow at 320, 390 and 1280, the control still 178 by 50 with a one line label at all three, and under reduced motion the chevron animation is none, the transition is 0s, getAnimations is empty and the inset offset still applies. The ring change is scoped to .hero-down:focus-visible and matches nothing else on the site.

## v1.36.0
- THE HERO GAME NO LONGER WITHHOLDS THE REST OF THE PAGE, and this reverses v1.17.0 deliberately. Measured on the shipped build at 390px: body carried game-locked, the More control computed to opacity 0 with pointer-events none, and it sat at y802 in a 779px hero, so it was invisible and below the fold at the same time. That was built as a gate and it was the wrong trade here. Someone who lands on a full screen duck game and does not play it concludes that is the whole page, and never reaches Drift Fever, Hurtle or Beakdown, which are what the page is for. Losing those three to a visitor is worse than losing the gate.
- The gate is gone in full, not disabled. Deleted: the game-locked class and every rule keyed to it, the nh_snaphit_cleared localStorage key, the postMessage listener for { snaphit: 'cleared' }, the 45 second safety timeout and the Tab press reveal. hero-game.js was 64 lines and is now one IntersectionObserver that parks the frame when it scrolls away, because the control is plain markup that needs no JavaScript to reveal. The game itself, the hero and the frame parking are untouched.
- THE SKIP LINK WENT WITH IT, and the reason it was redundant is not the one the brief assumed. "Skip the game" was an escape hatch from the gate, but it never worked as a keyboard skip either: it sat after the iframe in DOM order, so a keyboard user reached it only after tabbing through the game. The sitewide "Skip to content" link targets #main, and #main opens with the hero, so that one never skipped the game either. What replaces both is the More control, which targets the same #more and is visible without being focused. CORRECTED IN v1.36.1: this bullet went on to call it the first focusable thing inside the hero, which is false. The iframe takes a tab stop and comes first, exactly as it did before .hero-skip. The control is the first visible affordance in the hero and the first link in it.
- The treatment, and why. Centred rather than tucked into a corner, because a corner pill reads as a footnote on somebody else's game and this is the primary way onward. 178 by 50 against the old 87 by 40. The label says where it goes, "See the games", rather than "More". Lifted 120px off the hero's bottom edge so its lower edge lands at y724 at 360, 390 and 414px, inside the 750px that survives a 94px band of browser chrome. The chevron keeps its 1.9s bob and drops it under reduced motion. No attract animation: the homepage lens control has one, and a second on another page would read as a tic.
- OPAQUE, NOT A TRANSLUCENT WASH, and that is the load bearing decision. Behind the control is a live cross origin game whose ground nobody here controls, so a 72 per cent chip would have made the label's legibility a function of whatever frame is showing. An opaque #0A0907 chip makes it a constant. Measured against four deliberately hostile grounds, white, black, mid grey and neon yellow, the ground painted under the glyphs came back rgb(10,9,7) every time and the label read 16.73:1 in all four. Independent arithmetic agrees: #EFEBE2 on #0A0907 is 16.73:1, and 15.43:1 on the hover ground. The stylesheet said 16.6:1 in its own comment, corrected in v1.36.1. So was the focus ring, which this entry did not check at all. The backdrop blur went with the transparency, having nothing left to blur.
- A CONTRAST NUMBER WAS WRONG BEFORE IT WAS RIGHT, on the trap this project has already recorded once. A hand rolled regex over the computed colour read color(srgb ...) floats as bytes and returned 1.06:1 for light text on near black. The sweep's own parser, parsecol.js, exists precisely for this, and using it gave 16.73:1. The lesson from v1.32.0 still needs applying every single time, not remembering.
- THE 320px LABEL WRAPPED, found in verification and fixed rather than shipped. An absolutely positioned box with left:50% shrinks to fit the space left of the offset, so at a 320px viewport the control had 160px to work in and broke "See the games" over two lines, making it 160 by 72 instead of 178 by 50. width:max-content gives it its natural width, with max-width:calc(100% - 2rem) as the guard for a viewport narrower than the pill. One line of label at 320, 360, 390, 414, 860 and 1280px after the fix, and zero horizontal overflow at all six.
- The interaction boundary, measured rather than reasoned about. On a 6px grid across the whole hero at 390px, 8138 sample points hit the game frame, 247 hit the control and none hit anything else; the widest run of control hits on any single row is 174px of 390. There is no full width strip, which is the shape of the bug this page shipped once before. With real CDP touch: a tap on the game registers in the framed document and moves neither the page nor the hash; a tap 10px below the control does the same; a tap on the control navigates to #more; a 260px vertical drag over the game delivers ten touchmoves to the game and the control never fires.
- Verified: interactive at document commit, before readyState leaves "interactive", with opacity 1, pointer-events auto and body class just "arcade", and a click with no prior gesture and no wait scrolls to #more. With JavaScript off the control renders at the same 178 by 50 and the link navigates. The hero game loads, paints on a rAF loop, parks to src null when scrolled to the foot, and revives and paints again on return, with no page errors. Reduced motion: animation-name none on the chevron, transition 0s on the link, no running animations, and zero moving pixels in the control over 950ms against 124 with motion allowed.
- Verified untouched: everything below the hero on /games/ is pixel identical to v1.35.1 at 390, 860 and 1280px against a zero noise floor, over regions 5278, 5058 and 5199px tall, so the three cabinets, the studio card and the dark treatment did not move. The homepage and /puzzles/ are pixel identical at 390 and 1280. hero-game.js is loaded by /games/ and nothing else, checked before deleting from it.
- One caveat on all of it. This container's network policy blocks snap-hit.online, so every measurement above was taken against a local stub that paints on a rAF loop and counts the pointer and touch events it receives. The stub is what makes "the game is running" and "the control swallowed the input" observable at all, but it is not the real document. The contrast constant does not depend on it, since the chip is opaque, and the four hostile grounds bracket anything the real game can paint.

## v1.35.1
- THE REASON GIVEN FOR KEEPING A HIDDEN h1 WAS WRONG, and it was wrong twice. v1.35.0 said a page whose first heading is an h2 is a skip the lens audit reports on sight. It is not, and this page would not be audited anyway. Both halves measured rather than read: lift the audit's own counter out of lens.js, run it against this document as shipped and again with the h1 deleted from the DOM, and the output is identical, skips 0 and outside 0 either way, with the first heading reading H1 in one and H2 in the other. The counter starts at prevLevel 0 and guards on `prevLevel &&`, so the first heading can never be flagged, and outsideSkips() opens with `if (!h1) return 0`. And /puzzles/ loads site.js and arcade.js only: lens.js is not on this page, so the audit never runs on it at all.
- The hidden h1 stays, on the reasons that actually hold. Every one of the 33 documents has exactly one h1 and no skips since v1.31.0, and that is an invariant of the site rather than a thing a tool polices. A document with no h1 has no name for anything reading its outline. And this one is genuinely exposed: the accessibility tree reports role heading, name Puzzles, level 1, because .sh-sr clips to a 1 by 1 box rather than using display none or visibility hidden. The sentence was a justification invented for a decision that was already correct, which is its own kind of defect.
- "NEITHER USES POSITIONAL BINDING" WAS TOO STRONG. The index binding that once made this page assert the opposite of what it meant is genuinely gone. But "The same puzzle, drawn from films" still binds to the item above it: it depends on Novlr staying first, and it has no antecedent for a reader who arrives at the Reel Haiku heading by heading navigation or a deep link. llms.txt got this right in the same change, naming the referent as "the same puzzle as Novlr". The page copy is the owner's wording and is correct as the page stands, so it is flagged here rather than changed. The v1.35.0 entry is corrected in place.
- One finding did NOT reproduce and is recorded because a claim was nearly weakened on it. The review could not get Forgery to chain out of its inner scroll when the inner document was pinned to its end with scrollTo rather than dragged there, and read that as the chaining holding only for a natural drag sequence. Measured both ways at 390px with the same 60ms dwell: pinned with scrollTo, the page goes 1944 to 2436 on the next drag; reached by dragging, the page goes 1944 to 2436. Identical, and both chain. The v1.35.0 claim stands as written.
- One claim in that entry was broader than what was checked, and is narrowed here. It said the page's machine readable strings are now accurate per puzzle, having listed order, attribution, JSON-LD name and hasPart as the things re-derived. Cadence was not among them. The meta description and llms.txt both say three daily puzzles, and the page's visible copy only supports daily for Novlr, and for Reel Haiku by reference to it; Forgery's line says nothing about how often it changes. The word is the owner's, used consistently in every brief for this page, so it is not corrected, but it rests on that rather than on anything a reader of the page can check.

## v1.35.0
- /puzzles/ IS A DEMONSTRATION PAGE NOW AND NOTHING ELSE. Three phones, three live puzzles, one line each. The whole pagehead is gone: the eyebrow, the visible heading and the Mike Linsey credit line. Nothing replaces them. The page opens straight into the first puzzle, 42px below the header at 390px and 71px at 1280, which is the section's own padding and not a gap anybody had to tune.
- Removing the section rather than emptying it was the point. .pagehead carries a blurred colour blob on an 18 second drift as its own decoration, so an emptied pagehead would have left a band of animated nothing above the first phone. Deleting the section takes the band and the blob together, and it matches /games/, which has no pagehead either. Verified: nothing inside main animates at all now, which also means the cycle sweep this project added in v1.34.1 has nothing to sweep on this page and contrast reads flat at both widths.
- THE h1 STAYS, HIDDEN, and the reasoning matters more than the markup. v1.31.0 fixed the heading outline on all 33 documents so every page has exactly one h1 and no skips, and this page keeping one is what holds that invariant. This is not the pattern rejected earlier in this project, which was adding a heading that duplicates visible text: there is no visible title here, so the hidden one is the document's only name. It uses .sh-sr, the utility this site already has, which /games/ already uses for the same purpose.
- Verified rather than asserted: exactly one h1 at all six widths, box 1 by 1 pixel and clipped, display block and visibility visible so it stays in the accessibility tree rather than being hidden from it, outline 1,2,2,2,2,2 with zero skips. The lens audit's own logic, lifted out of lens.js and run against this document, returns zero heading skips inside main, zero outside it, zero image and link findings, and the result string "nothing found".
- NO CREDIT LINE IS NEEDED BECAUSE NOVLR AND REEL HAIKU CARRY THEIR OWN, and that is the one claim this container could not check. The network policy blocks both hosts, so the framed documents cannot load here and nothing about their actual content was verified. What was measured is the room they have: the framed viewport is 275 by 558 at 360px, 304 by 616 at 390 and 325 by 659 at 1280, and a credit line in the first hundred pixels of a document lands fully inside with 463 to 582px of headroom below it. That is the space, not the fact. The fact wants one look at the deployed page.
- Order is Novlr, Reel Haiku, Forgery, and the two lines were rewritten because the order carried meaning. "The films one" and "the same puzzle, drawn from written works instead" only worked with Reel Haiku first. Novlr now stands alone, "A daily puzzle drawn from written works", and Reel Haiku refers back, "The same puzzle, drawn from films". Neither binds by index, which is the mistake this page once shipped when "films in one and written works in the other" bound to list order. "The same puzzle" does still bind to the item above it, so it depends on Novlr staying first and has no antecedent for a reader who arrives at the Reel Haiku heading directly. llms.txt names the referent outright, "the same puzzle as Novlr", and the page could.
- THE MACHINE READABLE CLAIMS WERE FALSE AND ARE NOW ACCURATE PER PUZZLE. The meta description, the og description, the title, the og title and the JSON-LD all said three daily puzzles made with Mike Linsey. Forgery is not a collaboration. They now name the three puzzles and say which two were made with Mike, the JSON-LD gives Novlr and Reel Haiku both authors and Forgery Nathan alone, its name is Puzzles to match the heading, and its hasPart order matches the page. llms.txt is corrected the same way, including its Novlr entry, which said "the same puzzle as Reel Haiku" and inverted the moment Novlr moved first.
- One more instance of the same false claim, not in the brief's list but the same sentence: the burger panel's description for /puzzles/, in site.js, also said three daily puzzles made with Mike Linsey. It reads "Novlr, Reel Haiku and Forgery, playable here" now.
- Verified, nothing else moved: the phones are untouched at ratio 0.4615 at all six widths, and the island and home bar clearance is unchanged to the hundredth, 3.73 and 3.48px at 320 rising to 5.11 and 4.77 from 414 up. Zero horizontal overflow at 320, 360, 390, 414, 860 and 1280. Contrast on the nine remaining text elements, nothing under the floor, worst 6.42:1. /games/ is byte identical at rest, zero differing pixels at 390 and 1280 against a zero noise floor, and the only difference anywhere in the site is the one intended: the /puzzles/ row in the burger panel, with /games/'s own row unchanged.
- Verified, the reorder did not disturb the observer, which keys off document position. At both widths: Novlr and Reel Haiku live on arrival and Forgery not yet created, all three live when the reader is at the second, Novlr parked and the other two live at the third, and Novlr and Reel Haiku live again with Forgery parked back at the top. The neighbour always survives, which is what the 900px margin is for. Forgery, now last, still scrolls inside its phone and still chains out: the puzzle ends on the sixth drag and the page first moves on the seventh, at 360 and 390px.
- With JavaScript off there are three phones, three names in the new order, three lines, three ordinary links, no visible title and no overflow. Reduced motion is pixel identical everywhere except the sitewide progress bar's top three rows. The .pz-credit rule matched nothing once its element was deleted and was removed with it.

## v1.34.1
- A COMMENT JUSTIFYING A LIVE CONSTANT WAS STILL QUOTING THE BUG THE VERSION BEFORE IT REMOVED. arcade.js explains its 900px observer margin by saying the phones are 800px apart and 548px tall at 390px, and 548 is exactly the squat height v1.34.0 existed to fix. Re-measured on the shipped build: 689px tall, tops 941px apart, 252px of page between one phone and the next. The constant is still right and that was checked by scrolling rather than by arithmetic, but the reasoning attached to it was describing geometry that no longer exists. Fixed, with a line saying what the numbers used to be and why.
- FREEZING ANIMATIONS MAKES A SCREENSHOT DETERMINISTIC AND CAN PIN A MOVING BACKGROUND AT ITS KINDEST MOMENT. This is a fifth way to get a contrast number wrong on this project and it belongs with the other four. .pagehead::before is a blurred colour blob drifting on an 18 second cycle behind the page heading, and every contrast harness here disables animations to stop the screenshot moving. That does not park the blob somewhere neutral, it parks it at its zero second state, which at 390px is the phase where it is furthest from the text.
- Swept at half second steps across the whole cycle instead. At 390px the credit reads 6.23:1 frozen and 5.81:1 at its worst, at about fourteen and a half seconds in; the eyebrow reads 6.27 frozen and 5.84 at worst; the h1 reads 15.28 frozen and 14.27 at worst. At 1280px the blob never reaches the text and all three are flat. Nothing is near the 4.5:1 floor at any phase, so the standard was never in question, but three figures published for this page were the best case dressed as the worst. The v1.34.0 entry is corrected above, and the same correction applies to the pagehead figures quoted in the v1.33 entries, which were measured the same frozen way.
- The lesson, stated so the next version does not repeat it: freezing animation is the right thing to do to a screenshot and the wrong thing to do to a measurement. Where anything animated sits behind text, the sweep has to run over the animation's own cycle, the same way v1.30 learned to sweep the lens rather than sample its three stops.
- The review found nothing else. It proved /games/ byte identical three ways, md5 identical full page screenshots at both widths against a zero noise floor, every phone and cabinet rectangle and computed padding equal at seven viewports including the min-height breakpoint, and the same behaviour step for step through tap, park, revive, expand and collapse. It swept 320 to 1440 in 10px steps and found the ratio at .4615 and the island and bar clearance positive at every one. It drove the scroll trap with real touch and found chaining works in both directions, and it recorded a method note worth keeping: a drag with no dwell between touchStart and the first touchMove reports no chaining, and adding the 60ms hold a thumb actually makes chains every time.

## v1.34.0
- THE PHONES TOOK THEIR WIDTH FROM ONE VALUE AND THEIR HEIGHT FROM ANOTHER, and the cause is a rule about custom properties worth writing down. A custom property is substituted where it is DECLARED, not where it is read. --sh-ph is declared on :root as calc(var(--sh-pw) / 0.4615), so its var() resolved against the root --sh-pw and inherited down as a fixed length. /puzzles/ overrides --sh-pw on .pz-stage, so it got the override's width and the root's height and the phones came out squat: measured, 318 by 548 at 390px and 340 by 517 at 1280, ratios .5806 and .6579 against an intended .4615.
- Fixed by applying the ratio at the point of use rather than reading a token that was resolved somewhere else: .sh-phone takes height:calc(var(--sh-pw) / 0.4615). Measured after, the ratio is .4615 to four figures at 320, 360, 390, 414, 860 and 1280px, and the phones are 318 by 689 at 390 and 340 by 736.7 at 1280. --sh-ph stays on :root for .sh-cab-body, which exists only on /games/ where nothing overrides --sh-pw, and is now also declared on .pz-stage so anything reading the token on this page gets the right number.
- THE ISLAND INSET WAS RE-VERIFIED RATHER THAN ASSUMED, because a taller screen is exactly the sort of change that quietly breaks an offset measured from the bottom. It held, and the reason it held is that both the padding and the bar are expressed in --sh-pw units read from the same inherited value at the same element, so neither moves when the height changes. Measured on all three phones at all six widths: every frame's top clears the island's lower edge by 3.73px at 320 rising to 5.11px from 414 up, and every frame's bottom clears the home bar's top edge by 3.48px to 4.77px. Positive everywhere.
- Scroll behaviour re-tested at the new height with real touch events, because the phone now fills 689 of an 844px viewport at 390px and the gutter is 36px a side. Tracked drag by drag: the first six drags inside Forgery's frame move the page by exactly 0 while the puzzle scrolls, the puzzle reaches its end on the sixth, and the page starts moving on the seventh. That is chaining working, and it is the whole answer to the trap question. Gutter drags scroll the page throughout. One reading needed a second look: a first run said the scroll did not chain, and it was the page already sitting at its own maximum, because Forgery is the last item so finishing it leaves you at the foot of the document with nothing below to chain into.
- THE LEDE IS GONE AND THE HEADING STAYS. The h1 is Puzzles now, matching Games. The h1 was kept deliberately: /games/ has both a heading and a lede, so cutting the heading would go further than matching it, and a page with no h1 is a heading skip the lens audit reports on sight, which is the fault v1.31.0 fixed sitewide. Verified after the change: exactly one h1, outline 1,2,2,2,2,2, zero skips, zero .lede elements.
- The Mike Linsey credit is one mono line under the h1, using .meta, the sitewide component, with a margin reset. Stated once rather than appended to each game's line, because it is a fact about the page and not about any one puzzle, and the per game lines were just cut back to a few words each. It measures 5.81:1 at 390px at the worst phase of the pagehead's drifting blob and a flat 6.42:1 at 1280, both above the floor; the figure first published here was 6.23, which was the blob frozen at one phase, and v1.34.1 explains why. The films and written works relationship is not restated: the two per game lines already carry it.
- Verified: /games/ is byte identical. Zero differing pixels at 390 and 1280 on a full page diff, and the two rules that read --sh-ph are unmoved, .sh-phone at 238.50 by 516.78 and .sh-cab-body padding-bottom at 11.3694px, identical on both builds to four decimal places. One measurement artefact worth recording: a first pass reported over a million differing pixels at 1280 in BOTH the noise floor and the comparison, which was the hero game's stub not having settled in the first screenshot of the batch; taken as a clean pair it is zero.
- Verified on this page: zero horizontal overflow at 320, 360, 390, 414, 860 and 1280px. Contrast, 12 elements at two widths, nothing under the floor. With JavaScript off there are no frames, all three phones and every word render, the heading and the credit are both present, the noscript line still clears the island by 5px, and the three links out are ordinary anchors. Reduced motion is pixel identical everywhere except the top three rows, which is the sitewide scroll progress bar at its finished state.
- Left alone deliberately: the title, the meta description and the JSON-LD name still say three daily puzzles made with Mike Linsey. The h1 no longer does, but none of them has become false, and a search result wants more than one word.

## v1.33.4
- THE NEW LEDE HAD THE TWO PUZZLES THE WRONG WAY ROUND, above the fold, three lines above the page contradicting itself. It read "Novlr and Reel Haiku are the same puzzle, films in one and written works in the other", and "one" and "the other" bind to the order just given, so that says Novlr is the films one. The page's own next two lines say Reel Haiku is the films one and Novlr the written works one, llms.txt says the same, and the names say it loudest: a reel is a film reel and Novlr is a novel. At 390px a reader met both claims in a single viewport.
- Fixed by moving one word. The subject order is Reel Haiku and Novlr now, so "one" binds to Reel Haiku and "the other" to Novlr, and every predicate in the sentence is exactly as it was written. The page and the machine layer agree again.
- Worth recording because it is a shape rather than a one off. The original brief for this page had the same inversion, "Novlr and Reel Haiku are the same puzzle with different source material, films and written works", and v1.33.0 quietly assigned them the other way round from the names without noticing the sentence disagreed. Two paired lists in one sentence bind by position, and prose that relies on that binding is one reordering away from asserting the opposite of what it means. Name the things instead, or check the binding every time the sentence moves.
- llms.txt realigned with the page rather than left to drift. Its Puzzles section still carried "distinct from the SnapHit Studios arcade games above, which Nathan makes alone", which came from the paragraph v1.33.3 deleted as commentary: still true, but the machine layer was making a point the human page had stopped making. Forgery's entry there was still the old non description and now carries the real line.
- Verified after the change: outline h1 then three h2, zero horizontal overflow at 390 and 1280, no console errors, and the lede and all three lines read consistently.

## v1.33.3
- THE LEDE STATED ITS OWN JUSTIFICATION AND NOW STATES THE FACT. Three paragraphs explaining why a shared credit is a different signal rather than a lesser one, why the relationship between two of the puzzles was worth saying plainly, and why the credit differs from the arcade's, are gone. Three sentences remain: made with Mike Linsey, Novlr and Reel Haiku are the same puzzle with films in one and written works in the other, all three are live below. The credit stays because it is a fact and verifiable in one click. The reasoning for printing it was never copy. The standing rule from here: page copy states the fact and never the justification for stating the fact.
- Forgery has its line, verbatim from Nathan: spot the author's real sentence among the forgeries, can you beat the bots. v1.33.2 refused to invent one and shipped a non description instead, which was right at the time and is now unnecessary.
- THE DYNAMIC ISLAND WAS SITTING ON THE FIRST LINE OF TWO PUZZLES. It never happened on the cabinets because those children are full bleed canvases and the island covers empty sky; these are web pages with a line of text hard against the top of the document. Their CSS is cross origin and not ours, so the inset is on our side: .sh-screen takes padding on this page, so the framed viewport starts below the island and stops above the home bar, and the black screen shows through top and bottom exactly as a phone's does.
- The numbers came from the island rather than from taste. It is top .028 and height .082 of --sh-pw, so its lower edge is .110 and the padding is .125. The bar is bottom .030 and height .016, lower edge .046, and the padding is .060. Verified on all three phones at 360, 390 and 1280px: every frame's top edge is at or below the island's bottom edge and every frame's bottom is at or above the bar's top, measured from rendered rectangles rather than from the arithmetic. The island, the glass and the bar keep their absolute positions because padding moves the content box and not the padding box.
- The glass was checked too, since it paints over everything at inset zero. Measured with it shown and hidden: it adds about 5 per cent white at the frame's top corner, nothing at all in the middle, and a point or two of cooling at the far bottom corner. The strong end of the rake, 11 per cent at the very top, now falls on the black padding above the frame rather than on any content. A sheen, not an obstruction.
- FORGERY IS A DOCUMENT AND COULD NOT BE SCROLLED. The cabinets set scrolling=no, which is right for a game canvas and wrong for a long puzzle: most of it was unreachable. It is gated on a data attribute now, so /games/ sets it on every frame exactly as before and this page sets it on none.
- Novlr and Reel Haiku got it too, and the reasoning is worth stating because the brief asked. A document that fits does not scroll, so the attribute costs nothing where it is not needed: measured, a stub at the framed viewport's own size reports no overflow and produces no scroll surface. What cannot be measured here is the real documents, because the network policy blocks both hosts, and at 360px the framed viewport is only 275 by 437. A puzzle truncated with no way to reach the rest is a worse failure than a scroll surface that never scrolls, so all three carry it.
- THE TRAP RISK WAS THE REAL ONE AND IT IS MEASURED, not reasoned about. A scrollable cross origin frame competes with the page for a thumb. Forgery's stub is 3494px against a 437px framed viewport: scrolling over it moves the puzzle to its end, the end of the puzzle becomes visible, and the page itself does not move while that happens, which is correct. Once the puzzle is at its end, further scrolling over it CHAINS OUT and the page moves again, at all three widths, so nobody dead ends inside the frame. Scrolling beside the phone and in the vertical gaps between puzzles moves the page at every width. The gutter either side of the phone is 36px total at 360 and 390px and 940px at 1280.
- Verified, /games/ is untouched: zero differing pixels at 390 and 1280px against v1.33.2 on a full page diff, three attract cards and three expand buttons at rest on both builds, and a tapped cabinet still gets scrolling=no on both. Nothing on any other document matches .pz-stage .sh-screen.
- Verified on this page: zero horizontal overflow at 360, 390, 414, 860 and 1280px. Heading outline h1 then three h2 with no skip, unchanged by deleting the prose section. Contrast on the page's own text, 12 elements at two widths, nothing under the floor and worst 6.27:1. With JavaScript off there are no frames, all three phones and every word render, the noscript line now sits at 40px inside the screen against the island's 35px lower edge so the inset helps it too, and the three links out are ordinary anchors. Reduced motion is pixel identical everywhere except the top three rows, which is the sitewide scroll progress bar at its finished state, the same on /systems/, /about/ and /games/.
- One consequence of cutting the prose, noted rather than fixed: the page no longer links to /games/ from its body, so main now has no internal link at all. The header nav and the footer both still carry Games, so nothing is orphaned, and an in body cross link was never the point of that paragraph.

## v1.33.2
- THE REVIEW CAUGHT THAT v1.33.1 MEASURED CLIPPING AGAINST THE WRONG BOX, and it was right. .top nav is overflow-x:auto, so the nav is its own scroll container and the nav's edge is what clips, not the window's. v1.33.1 compared each item's right edge against innerWidth, which misses every item that is inside the window but scrolled out of the nav. Measured properly, as nav.scrollWidth against nav.clientWidth, swept one pixel at a time from 701 to 900px.
- The corrected numbers, and they change the story in both directions. v1.33.0 with eight items hides an item from 701 to 781px, an 81px band, not the 45px v1.33.1 claimed. Nine items at the untouched 1.2rem gap would have hidden one from 701 to 859px, a 159px band. What actually shipped, nine items at .62rem, hides one from 701 to 778px, 78px. So the gap fix turned a 159px regression into 78px, which is 3px narrower than the site had before the ninth item existed. The review read this as a regression from 9px to 77px, which compares nine items against eight items at the NEW gap: a state that has never shipped. Against what shipped, the band got smaller.
- The gap cannot close it and that is now measured rather than assumed. Swept at .62, .5, .42, .35 and .3rem: the band shrinks to 78, 61, 50, 41 and 33px and never reaches zero. Nine items do not fit above the burger at any spacing anybody would want to read, and neither did eight. The burger breakpoint has been about 80px too low since it shipped, which is the actual defect and the actual fix. That is a sitewide behaviour change at 701 to 790px on all 34 documents, so it is written down here rather than slipped into a follow up.
- THE FRAMED VIEWPORT WAS NARROWEST ON THE WIDEST SCREENS. --sh-pw is height driven above 720px, min(26.5vh,330px), which is right for a cabinet whose child is a canvas that scales to whatever box it gets. These children are real web apps laying themselves out, and that rule handed them 239px at 1280px: narrower than the 253px the same page gave them at 390px, and narrower than any phone made. Width driven on this page instead, so the framed viewport now reads 275px at 360, 304 at 390 and 325 from 414px up. Zero horizontal overflow at every one.
- A SCROLL DESTROYED A PUZZLE YOU HAD TYPED INTO, silently. The phones are 800px apart in flow and 548px tall at 390px, so with the observer's 120px margin, looking at the next puzzle always parked the previous one, and a revive is a fresh load rather than a resume. On the cabinets that is fine and signposted: the attract card comes back and says TAP TO PLAY. Here there is no card and nothing on screen to say a guess has just been thrown away. Autoloaded frames get a 900px margin now, so a neighbour survives; anything a full screen away still parks. Measured: all three stay loaded while the reader is anywhere among them, and the later two park once the page is scrolled back above them.
- Copy and markup corrections from the same review. Forgery's framed path and its link path disagreed on a trailing slash, so the iframe and the button were loading two different URLs; both are the no slash form now, matching the prose. The meta description said "Free to play, nothing to install" and the JSON-LD asserted isAccessibleForFree on all three: that is a pricing claim about three products this page does not control, made to search results and to machines, and stated nowhere in the body where it could be reasoned about. Both are gone.
- Reel Haiku's line said "Every day it gives you a haiku and you work out what it describes", which is three mechanical assertions about a product nobody established. It reads "The films one." now, which is exactly what was established. Forgery's line is "The third one, at novlr.com/forgery." The daily cadence stays, because that is the brief's own description of all three. The three button labels were shouted in the source, copied from the arcade's .sh-visit, which is not a .btn: every other .btn on this site is sentence case in the markup and the stylesheet does the uppercasing, so these are too. Nothing visible changed.
- What the review checked and found clean, which is the part worth keeping. /games/ is untouched by generalising arcade.js: the old file was served by interception and an identical eight step script, tap, scroll, park, revive, expand and collapse, produced zero differences at 390 and 1280px. The autoload loop does not thrash: three bottom and back cycles give exactly six loads each, and forty rapid alternations at 30ms give three, so the sweep coalescing holds. The expand state is genuinely unreachable, including by forty tab presses. No new selector matches anything on any other document. Contrast worst 6.25:1. All four standards pass.

## v1.33.1
- PUZZLES IS IN THE NAV, ninth item, directly after Games, on all 33 documents that carry a primary nav. 404.html has none and is the only one skipped. Verified by reading the rendered list on every one of them: nine hrefs in the right order, the anchor text is Puzzles, and aria-current lands on /puzzles/ on that page alone and nowhere else.
- The aria-current trap did not fire this time. A previous nav change missed four pages because the current page's own item carries aria-current="page" and a pattern matching the plain anchor skips it. The insertion matched the Games anchor with an open attribute list rather than a literal, so /games/, which is the one page where Games carries it, got the new item like everything else. Checked by reading its nav back.
- The burger panel needed nothing beyond a description. It builds its rows from the header's own anchors, so one list in the markup gives two presentations and the panel cannot disagree with the nav it replaces. Nine rows at 390px, each with its line, no horizontal overflow with the panel open.
- NINE ITEMS DID NOT FIT, AND THE FAILURE WAS INVISIBLE TO AN OVERFLOW CHECK. .top nav is overflow-x:auto with the scrollbar hidden, so an item past the right edge is not lost, it is scrollable with no affordance whatsoever: measured at 800px the ninth item put the row's right edge at 823px against a 800px viewport and the document's own scrollWidth stayed exactly equal to its clientWidth. Screenshotted, Contact was sliced down the middle at the edge of the page. This is the band CLAUDE.md warns about, the one v1.26.0 shipped a bug into, and 390 and 1280 both miss it completely.
- Fixed by tightening the nav gap from 1.2rem to .62rem between 701 and 900px, which buys back eight gaps worth of room. The font is untouched at .92rem. Swept 705 to 900 in 5px steps against the shipped build: there is now no width anywhere at which this change clips more than v1.33.0 did, and the header is 65px on one row at every one of them.
- What that sweep also found, pre-existing and not this version's: the shipped build ALREADY hides Contact with eight items in a band just above the burger. The width of that band is corrected in v1.33.2 below, because the sweep behind this sentence measured against the wrong box.
- /puzzles/ is in sitemap.xml with today's lastmod, and llms.txt has a Puzzles section naming Mike Linsey as co-author of all three and marking them as distinct from the SnapHit arcade games above them. The sitemap has 33 url entries and still parses.
- Verified, nothing else moved: every document diffed before against after at 390px and 1280px with animations frozen. At 390px the burger is in charge and not one document differs by a pixel anywhere. At 1280px 32 of the 33 differ, all of them inside the top 66px, and not one differing pixel below it on any page. The one that does not differ is 404.html, which has no nav to add to. No document changed height.
- The full review this version is owed under CLAUDE.md, being a sitewide change, was still outstanding when this was committed, and so was the page review from v1.33.0. Both are follow up commits rather than a reason to hold a verified tree, which is the rule this project wrote after losing nine hours to the opposite.
- Not changed, and worth flagging rather than deciding quietly: the footer's Pages list still has no Puzzles row. That list is not the nav, it already differs from it by carrying Writing and Challenges under their own labels, and adding to it was outside what this version was asked to do.

## v1.33.0
- NEW PAGE, /puzzles/. Three daily puzzles made with Mike Linsey, each framed live in a phone. Dark ground, no lens, no reader pill, body.dark and html.dark so it takes /work/ and /systems/ ground rather than the arcade's #04070e, which exists so neon reads as light in darkness and there is no neon here.
- The phone is the cabinet's phone, reused whole: brushed rail, volume and power keys, dynamic island, glass rake, home bar. What is left out is the cabinet around it and the expand state with it. .sh-phone.sh-big is position:fixed inside the shell, and a transformed shell becomes its containing block: v1.24 measured it at 359 by 5107px at left -303 when reached from inside the open burger. This page has no need of it, so it ships no .sh-grow and arcade.js wires none.
- ONE IMPLEMENTATION, NOT TWO, which is why arcade.js changed rather than a second file appearing. It drives both pages now, gated on a data-sh-auto attribute: the frame loads without being tapped, loading one does not park the others, and a frame that scrolled away comes back by itself instead of waiting for a second tap. .sh-attract and .sh-grow are optional, so a page with neither gets neither. Unloading is shared code, because unloading written twice in two files is how two copies drift apart.
- THE BRIEF SAID AUTOLOAD ALL THREE ON ARRIVAL AND THAT IS NOT WHAT SHIPPED, because the same brief also asked for a frame to unload when it scrolls out of view, and at 390px all three phones start below the fold. The two requirements collide on arrival and the unloading one wins, because it is the one protecting the phone. Measured: starting all three eagerly opened three requests and cancelled two of them within a frame. The observer already knows which phones are near the viewport, so it decides both the first load and every revive after it. Nothing ever needs tapping, which is what autoload was for.
- Getting there cost two wrong versions and both are worth recording. Starting the frames inside the cabinet loop ran while var io was hoisted but still unassigned, so watch() observed nothing and no frame could ever unload again: measured, all three sat at -2475px and stayed loaded. Nothing on /games/ had ever hit that, because there start() only runs from a click, long after the file has finished initialising. Moving the start below the observer then loaded and immediately parked all three at 390px. Observing each screen instead, which exists from the first parse, covers both.
- Verified by scrolling rather than by reading the code: at 1280px the first phone loads on arrival and the other two do not exist yet; walking down the page loads each as it arrives and unloads each as it leaves; returning to the top reloads the first and unloads the rest; five round trips produce five or six src writes in total, which is one per genuine transition and not a thrashing loop. No console errors at any point.
- THE FRAMED DOCUMENTS ARE PINNED BACK TO LIGHT and this was proved rather than copied. A cross origin child that declares no colour scheme of its own inherits dark from the embedder, and Chrome then stops treating its canvas as transparent and paints it opaque white. Measured on this page with a stub child that declares nothing: with body.dark .sh-screen iframe set to light the screen reads rgb(0,0,0), and with the same rule set to dark it reads rgb(249,249,249). That is the whole defect, in one pair of numbers.
- Verified on this page: zero horizontal overflow at 360, 390, 414, 860 and 1280px. Contrast, worst pixel under the glyphs against the actual painted ground, 15 text elements at 390 and 1280: nothing under the floor, worst 6.27:1 on the eyebrow, and the noscript line inside the screen reads 15.85:1. --ink-3 resolves to #9C968A on this page, the dark ground value, while :root keeps #756D5F: that is v1.32.0's override doing its job, and it is the reason this page uses body.dark rather than a class of its own.
- With JavaScript off all three phones render, every word of copy is present, each screen says the puzzle needs JavaScript, and the three links out are ordinary anchors carrying from=portfolio. No iframe is created at all.
- Reduced motion differs from the default by 3840 pixels and every one of them is in the top three rows: the sitewide scroll progress bar sits at its finished state rather than tracking the scroll. /systems/, /about/ and /games/ do the identical thing on the identical build, so it is neither new nor this page's, and CLAUDE.md defines reduced motion as the finished state rather than a mid animation state, which is what that is.
- WHAT COULD NOT BE VERIFIED HERE, said plainly. This container's network policy returns 403 to CONNECT for reelhaiku.com and novlr.com, so the three real documents cannot load in it. Every measurement above was taken against a local stub standing in for them, including the colour scheme proof, which is the same method the arcade used. What remains unconfirmed is only what the real documents do once framed: that they render at phone width, and that none of them sends a framing header despite the check that says otherwise. Both want a look on the deployed page.
- The copy names Mike Linsey in the lede rather than a footnote, because all three sites carry both names and anyone can check that in one click. It also keeps the SnapHit claim distinct: the arcade games are made alone under that name, these three are not, and the page says so. What the page does not do is describe how Forgery is played, because that was not established and inventing it would break the rule that every factual claim has to be defensible.

## v1.32.0
- THE THIRD TEXT TIER FAILED CONTRAST ON 19 DOCUMENTS AND IS NOW A TOKEN. #8a8272 was hardcoded on ten rules and reads 3.64:1 on --paper and 3.81:1 on a white card, against a 4.5:1 floor, at 11.2 to 12.8px where no large text allowance applies. It is --ink-3 now, #756D5F, 4.89:1 and 5.11:1. Eleven rules reference the token: the ten that had the literal, plus .kv dt, which v1.31.0 had set to the same value written out longhand because it was the only rule that version was allowed to move. That is unified, so the site has one quiet tier rather than two greys a step apart.
- Tokenised rather than found and replaced. The third tier now exists in the design system next to --ink and --ink-2, and the palette comment carries all three measured: 17.58:1, 7.33:1, 4.89:1. The steps are not even, and an earlier draft of this entry and of that comment said they were: --ink to --ink-2 is 2.40 times, --ink-2 to --ink-3 is 1.50. --ink-2 was rejected as the substitute because 7.33:1 is body text, and flattening the tier would lose the distinction these ten rules exist to draw. The gap from --ink-2 to --ink-3 is 1.50 times, which keeps the labels visibly quieter than the prose around them.
- THE BLANKET SUBSTITUTION WOULD HAVE SHIPPED A NEW FAILURE, and this is the part worth reading. One of the eleven selectors lands on a dark ground: the testimonial caption on the homepage is a .quote figcaption inside body.home main, whose --paper is a near black mix. On a dark ground the darker grey is the worse one. Measured worst pixel under the glyphs, that caption reads 4.65:1 with the old value and 3.47:1 with the new one: a pass turned into a fail, to fix a failure elsewhere.
- So --ink-3 tracks --ink-2 on all three dark grounds rather than taking its light value there, and the caption goes to 6.03:1 instead. It is declared alongside the existing --paper overrides on body.arcade main, body.home main and body.dark main, so the token means the quiet tier on this ground rather than one particular grey.
- There is no room for a real third tier on those grounds, which is why it collapses rather than getting its own value. --ink-2 is already the dim value at 6.42:1 on --panel, and a step 1.5 times quieter lands at 4.28:1, under the floor. The quietest grey that still clears 4.9:1 on every dark ground, #958F82, is only 1.09 times quieter than --ink-2, which is not a step anybody can see. The first draft of this said 1.30, which came from dividing a ratio on one ground by a ratio on another: same mistake shape as the harness bug below, caught by the review rather than by me. The corrected figure argues for collapsing more strongly than the wrong one did. A near black page has room for two tiers, not three. If that call is wrong the fix is one line.
- Measured, and the method matters because this project has produced confident wrong contrast numbers four separate ways. Worst pixel under the glyphs, with the ground taken from a second render of the same page with the text made transparent, so it is the pixel actually painted under each glyph rather than a computed ancestor background. The foreground is composited analytically through cumulative ancestor opacity and any greyscale filter, because sampling the painted glyph under-reads: mono text at 0.7rem has no pixel at full coverage. Elements are located in the full page render, so nothing is clipped to a viewport that does not contain it.
- 6382 text elements measured on each build, 33 documents at 390px and 1280px. Under the floor: 488 before, 192 after. 296 readings fixed, and ZERO newly under the floor, compared element by element rather than by totals. The old grey: 298 readings across 19 documents, worst 3.64:1, 296 of them failing. The new token: 296 readings, worst 4.89:1, none failing. The two that moved out of that group are the homepage caption at both widths, which is now counted with the dark tier. #8a8272 appears nowhere in the stylesheet.
- A HARNESS BUG CAUGHT BY LOOKING AT A SURPRISING NUMBER, per CLAUDE.md. The first reading of the fixed homepage caption came back at 1.18:1, which is not a credible figure for legible text. The cause was the colour parser: computed colour arrives as color(srgb 0.61 0.58 0.54) wherever color-mix is involved, and reading those 0 to 1 floats as bytes turns #9C968A into near black. Every page on this site that uses color-mix was affected. The parser was fixed and both sweeps re-run from scratch; the corrected reading is 6.03:1, which matches the arithmetic exactly.
- Verified, colour only: every document diffed before against after at 390px, 860px and 1280px. Exactly 19 documents differ, the same 19 at all three widths, and every differing pixel on every one of the 57 rows falls inside the bounding box of an element the change touches. Not one pixel outside. The change is 0.019 per cent of the page on the homepage and up to 0.31 per cent on /about/ and the chapters.
- /super/ is the most affected and was checked directly. On /super/, /super/rules/ and /super/numbers/ at both widths the document height is byte identical, horizontal overflow is zero on both builds, and font, size, weight, tracking, transform, family, margin and line height are unchanged on every affected element. The only difference is rgb(138,130,114) becoming rgb(117,109,95). The crops confirm it by eye: same position, same letterforms, darker ink.
- The lens states were swept at 41 values of --lens from 0 to 2 rather than at the three settled stops, because a coarse sweep stepping over the worst case is a documented failure here. /work/ and /systems/ carry no --ink-3 element at all. The homepage caption reads 5.39:1 at its worst, which is at the AI threshold. /book/ has 27 of them and reads 3.03:1 at the same threshold, which is under the floor and is NOT this version's: the same sweep on the shipped build reads 2.49:1, so the change improves it by more than half a point and leaves it failing. It is the lens opacity dip landing on top of an already quiet tier, and it is recorded below rather than fixed.
- Repository wide grep: #8a8272 survives only in this file's changelog. No JavaScript injects it or any near neighbour. --panel-dim at #9C968A, the dark pages' own quiet tier, passes at 6.42:1 and was not touched.
- WHAT IS STILL UNDER THE FLOOR, reported and not fixed. 192 readings remain, all of them present before this change and none introduced by it. Four are confirmed real by inspection: /book/'s sampler numbers at the AI lens threshold at 3.03:1, described above; the selected sampler chip's own number, which is painted in the same colour as the chip it sits on and is invisible by design on both builds; the giant footer wordmark, which is aria-hidden decoration at 1.15:1 and not text anybody reads; and several items in the /games/ arcade hero, which is a cross origin document. A large share of the rest are artefacts of the harness rather than faults: the reader pill is position fixed and the skip links are position absolute and parked off screen, and a full page render locates all three by document coordinates, which puts them over whatever happens to sit at that spot. Those were not individually confirmed and the 192 should not be read as 192 defects.
- THE FULL REVIEW LANDED AFTER THE COMMIT, under CLAUDE.md's stopping rule, and found three factual errors in the comments and in this entry. None of them touched a rendered pixel and all three are corrected above: the uneven tier steps, a 1.30 that should be 1.09, and a note attributing the homepage's two figures to the arcade ground it sits on. That last one is the instructive one. The note was written on body.arcade main because it is the first of the three dark blocks, and it says "here" about numbers measured on the homepage. /games/ is the only body.arcade document and it matches none of the eleven selectors, so nothing could have been measured there at all. The comment now names the page the case actually came from and gives the figures for all three grounds.
- What the review confirmed, measured independently and more thoroughly than I had. Only colour: 310 elements across all 33 documents at two widths compared on family, size, weight, tracking, transform, all four margins, all four paddings, line height, display, opacity, filter, background and bounding box, giving 308 colour changes and zero non colour differences, with scrollHeight and scrollWidth identical on every document. No token leak: --ink-3 appears nowhere in the repository before this change and the only JavaScript that reads a custom property by name reads --lens. Nothing under 4.5:1 on any of the eleven selectors at either width. The .kv dt ground is rgb(255,255,255) on all 27 overlay pages, opened and checked rather than sampled. No horizontal overflow at 360, 390, 414, 860 or 1280 on any of the 33 documents.
- JavaScript off and reduced motion are confirmed after all, which the entry above had recorded as unverified because the stopping rule cut those runs. With JavaScript disabled every selector resolves to the correct colour, text length is unchanged, there is no overflow, and the homepage dark fix still applies because --gp falls back to clamp(0,0,1). Under reduced motion the results are identical, and no animated property is touched.
- A FIFTH WAY TO GET A CONTRAST NUMBER WRONG, for the list CLAUDE.md keeps. The review's first pass read .prose th at 1.998:1. Chromium paints subpixel antialiasing by default, so glyph edges carry coloured fringes and a worst pixel search finds one of those rather than the text. Launching with lcd text disabled fixes it. That is now four separate mechanisms this project has hit: an ancestor opacity a harness could not see, a clip that did not clamp to the viewport, sub-pixel coverage under-reading painted glyphs, and a colour parser reading floats as bytes. The lesson each time was the same one, which is that a surprising number is a bug in the instrument until proven otherwise.
- Two more pre-existing failures found by the review, improved here and not fixed. /super/'s disclaimer strip ends in a "Full disclaimers" anchor whose underline paints a band at rgb(195,190,182), and where a glyph crosses that band the local ratio is 2.77:1 on 14 chapters at 1280px. The old colour is worse on the same band. And the lens crossfade takes the whole of main to 0.75 opacity at its midpoint, which drops /book/'s .spage .from and .callout>p:first-child to 3.11:1 in transit, against 2.53:1 before this change. Both are transient or partial, both are improved by this version, and both are outside what it was asked to do.

## v1.31.0
- Four fixes that do not interact, except the two that do. A hosting claim left in JavaScript, a heading level skip the lens found, the sitewide footer skip that was hiding behind it, and a contrast failure in the reader overlay that has been there since the overlay shipped.
- THE THIRD NETLIFY STRING. site.js's colophon fallback still told a visitor the site was deployed on Netlify. Two instances were corrected in v1.21.1, in /colophon/ and in this file; this one survived because it is a string inside a catch block rather than markup, so nothing that reads the pages could see it. It now reads "Deployed on Cloudflare Workers", which keeps the clause the sentence had rather than pasting in the colophon's longer wording, which does not fit a one line fallback. Grepping the whole repository afterwards: not one file that deploys contains the word. Every surviving instance is in this file, in three changelog entries: v1.21.0, v1.21.1 and this one. No count is given because writing the count changes it. .assetsignore keeps README.md out of the deploy, so none of them reaches the site. Those are history and stay.
- /WORK/ WENT H1 STRAIGHT TO H3, and so did /writing/. The brief for this version said every other page was well formed; /writing/ is h1 then two h3, the same fault, found by running the outline over all 33 documents rather than the four in the brief's table. Both are fixed. The eight case study headings on /work/ and the two essay headings on /writing/ are h2 now.
- Promoted rather than given an h2 above them, for three reasons. Neither page has any other sectioning, so the case studies and the essays ARE the page's top level sections rather than items inside one. Adding a heading means adding visible text, which was ruled out, and the alternative is a visually hidden heading, which hands a screen reader something a sighted reader does not get, on a site whose whole argument is that the three readers receive the same document. And it matches /systems/, where the content sections are already h2.
- The row is a component and the level belongs to the page, so the stylesheet now names both: .row h2,.row h3. The homepage's rows sit under a section h2 and stay h3, correctly. Identical type either way, which is the point.
- THE FOOTER RAN H4 UNDER AN H2 ON 29 OF THE 32 DOCUMENTS THAT CARRY ONE. The three exceptions were /super/, /work/ and /writing/, whose main ended on an h3, where h4 was legal, and 404.html has no footer headings at all. 64 headings across those 32 files are h2 now. h2 is legal after every page's last content heading, which with /work/ and /writing/ fixed is h2 on 31 of them and h3 on /super/. h3 would also have been legal everywhere, and was rejected: it would say the footer is a subsection of whatever content section happened to come last, which it is not. The footer is a sibling of main under the document's title.
- THE AUDIT WAS FOOLED, AND THE FIX IS DEMONSTRATED RATHER THAN ASSERTED. outsideSkips() walked every heading in the document with one counter, so on /work/ the footer's h4 followed an h3 and read as a legal step: the card said the chrome was clean while the chrome's own two headings sat at h4 under a document h1, three levels down with two missing, on all 32 documents that carry them. It collects the chrome's headings first now, the h1 plus everything outside main, and walks that list on its own, because the chrome is a sibling of main rather than a subsection of it. A broken outline inside main can no longer hide a fault outside it or invent one. Run both versions against both markups: on v1.30's markup the old function returns 0 on /work/ and the new one returns 1, while on /systems/, /book/ and the homepage both return 1. On v1.31's markup both return 0 everywhere, and across all 33 documents the only two where the two versions disagree are /work/ and /writing/, which are exactly the two whose outline was broken.
- The first attempt at that fix was still coupled to main, and the adversarial review found it. It walked the headings in place and reset the counter whenever it passed one of main's, which only fires where main has a heading to fire on: the same chrome markup then returned 0 or 1 depending on whether main contained any heading at all. Collecting the chrome's list first removes the coupling rather than working around it. Nine synthetic documents now pin the behaviour, including that pair, a document with no h1 at all in either direction, and both builds' real /work/ and /systems/ shapes. With no h1 it returns 0 and says nothing, because measured against a level that does not exist an ordinary footer h2 reads as a skip and a footer h1 reads as fine, which is backwards, and a missing h1 is a different fault. Every document here has exactly one h1, so that branch is hardening rather than a fix.
- Verified: all 33 documents have no heading level skip anywhere, in main or outside it. The lens audit on all four lens pages now reports zero skips, nothing found, and nothing outside main.
- THE READER OVERLAY'S TERMS WERE 3.81:1 and had been since the overlay shipped. #8a8272 is a warm grey on the card's white; #756d5f is the same grey two steps down and reads 5.11:1 there. The card's white is hardcoded #fff with no dark variant, and resolved in the browser on all ten sampled pill pages the ground under the terms is rgb(255,255,255) every time, so that is the figure that matters. On the site's paper, #FBFAF6, which is what the card's head strip takes and the only other ground inside this overlay, the same grey is 4.89:1, so it would clear the floor there too. Measured worst pixel under the glyphs on ten pages at two widths, 126 terms in all, the worst goes from 3.81:1 to 5.11:1. The first pass reported 123 and that is three short: the harness clips to the element's rectangle and throws rather than reads when the element is below the fold, which took out the last term on three of the ten pages at 390px. Scrolled into view all three read 5.11:1, the same as every other term, and v1.30's entry already records that clip as the cause of artefact readings. The overlay is the only thing on this site that builds a .kv, so nothing else moves. It is built on 27 of the 33 documents, not 32: /book/, /games/, /systems/ and /work/ suppress the pill with data-no-reader-pill, the homepage carries the lens instead because it has a #reader-frame, and 404.html loads no JavaScript at all.
- THE SAME COLOUR FAILS IN TEN OTHER RULES AND IS NOT FIXED HERE. #8a8272, the exact value replaced in .kv dt for being 3.81:1, is still declared on .sampler-nav button .num, .spage .from, .timeline .y, .prose th, .callout>p:first-child, .tagline, .disclaimer-strip, .super-toc .n, .toc-part and .quote figcaption. Measured against the nearest opaque painted ancestor at 1280px across every document: 142 text nodes on 19 of the 33 carry it, and 139 of them are under the floor, at 3.64:1 on paper and 3.81:1 on white, all between 11.2px and 12.8px where 4.5:1 is required. The three that pass are on dark grounds. This version does not touch them, and the reason is the brief rather than the size of the job: item four was scoped to the reader overlay, and the constraint on this version is no visual change on any page, which changing ten rules across 19 documents plainly is. It is recorded here so the overlay fix is not read as having closed this, because it has not. The worst case for this colour on the site is 3.64:1 and it is untouched.
- TWO THINGS BEHIND A CONTROL DID CHANGE, and no pixel diff can see either, because neither is drawn until a reader moves the lens or opens the overlay. The first is on /work/, and the entry above said the opposite before the review caught it. /work/ carries the lens, and the lens draws a chip on every heading naming its level: the eight over the case studies read H2 where they read H3, and the red "skipped H2" flag beside the first of them is gone. Its audit card goes from "1 to look at" to "nothing found". Measured on both builds rather than reasoned. That is the fix appearing in the one place on this site built to show it, which is the point rather than a side effect, but it is a visible change and the no visual change claim below is about the rendered page at rest. The audit card's "outside main" row also moves on the homepage, /systems/ and /book/, from one chrome skip to nothing, which is the bullet above.
- The second is on /writing/, which carries the overlay rather than the lens. The overlay's Google panel lists the page's h1 and h2, and its AI panel serialises h2 as "##" and h3 as "###". /writing/ had no h2 in main, so the outline was a single line and both essay titles serialised as third level. The outline is three lines now and both titles are second level. Checked by diffing both panels on all 27 documents that build the overlay: /writing/ is the only one that moved. contentRoot() is querySelector("main"), the tag, and every footer on this site sits after </main>, so the 64 promoted footer headings cannot reach either panel on any page.
- THE FIRST ADVERSARIAL REVIEW CAUGHT A REGRESSION THIS VERSION CREATED, and it is the leak CLAUDE.md warns about, arriving from the other direction. .lens-host.lens-ai h1,h2,h3 was unscoped, which was harmless only for as long as nothing outside main was an h1, an h2 or an h3. Promoting the footer's headings put the sitewide chrome inside the reach of a rule written for the lens: on all four lens pages, dragged to the AI state, "Pages" and "Machine layer" lost their .08em tracking. Measured by injecting the old rule back on top of this build, 0.9216px scoped against normal unscoped, on all four. It is scoped to main now, where its companion rules already sat. .sig stays unscoped on purpose: the footer's signature is meant to go mono with the page.
- That first review also found three wrong counts in this entry. It said the footer skip was on 31 documents where it is 29, said the repository was left with two instances of Netlify in a sentence that contained two of its own, and put the overlay on 28 pages where it is 27. All three are corrected above and all three were re-derived from the files rather than taken from the review.
- A SECOND REVIEW RAN ON THOSE FIXES AND FOUND MORE, which is the argument for running it twice. Corrected above: the claim that nothing moved on /work/, which was false and is the worst of them; the Netlify count, self refuting a second time because the sentence written to fix it added an instance of its own; a comment in lens.js still saying the sitewide footer runs h4 under an h2; the first attempt at decoupling the audit from main, which did not decouple it; 123 overlay readings that should have been 126; a page state count read as five widths by three modes when it is 33 documents by five widths; a homepage noise floor quoted to four figures from a single draw of a stochastic field; and four comment claims that were loose rather than false, about which neighbouring rules are scoped, what a colour step is, how many grounds the overlay paints, and what .sig actually matches. Recorded rather than corrected: the ten other rules still declaring #8a8272.
- Two of that review's findings did not survive checking, and saying so is the point of checking. Its worked example for the audit, an h1, h2, h3, h4 chain outside main followed by a footer h4, is legal markup with no skip in it, so nothing was being excused. That example was mine, quoted back from the comment I had written beside the reset, and the comment was wrong about its own case. Probing it is what exposed the real defect underneath, which was the coupling. And its homepage figures run roughly double the ones above because it counted a pixel as differing on any channel change at all rather than on a summed delta past 8, so the two sets are not in conflict; its relative reading did not hold up over five draws either way.
- Verified, no visual change: all 33 documents pixel identical at 390px and 1280px with animations frozen, except the homepage, which never diffs to zero because of the particle field. The field is stochastic, so one draw per cell is not a floor and the first pass treating it as one was wrong, which the review caught. Five draws each, a pixel counting as differing when the summed absolute RGB delta passes 8: at 390px HEAD against itself spans 1.632 to 1.804 per cent and HEAD against this build spans 1.689 to 1.780, wholly inside it; at 1280px the two span 2.090 to 2.191 and 2.082 to 2.240, overlapping almost entirely with one cross draw 0.049 above the highest self draw. Five draws each is not enough to call that a difference, and it is not the evidence anyway. This is: with the field hidden it is exactly zero differing pixels at both widths. The footer band alone, on three pages that have no field at all, is zero differing pixels at both widths.
- Verified after the audit function was rewritten a second time, because lens.js deploys: the four pages that load it are zero differing pixels against v1.30 at 390px and 1280px, the homepage included once its particle field is hidden. The rewrite changes what the card would say about a document whose chrome is broken, and no document here has one, so it says the same thing and draws the same pixels.
- Verified: all 33 documents at five widths, 165 page states, with no horizontal overflow, and reduced motion pixel identical on every sampled page at both widths. The AI state was checked again after the scoping fix, since restoring the tracking widens those two headings: 20 more states, the four lens pages at all five widths, and the document's scrollWidth equals its clientWidth in every one. The only thing in the footer whose box passes the viewport edge is the full stop at the end of the giant wordmark, which .giant has clipped with its own overflow since long before this version and which reads identically on both builds.
- Verified, and this is the check that would have caught the regression above on its own: every CSS rule that matches a footer nav heading, enumerated from the live stylesheet on all 32 documents that have one, on both builds. The computed font, size, weight, tracking, transform, colour, margin, line height and display are identical to HEAD on every heading, and the only rule that changed in the set is footer.site h4 becoming footer.site h2. Nothing written for a content heading reaches the chrome.
- A note on the measuring rather than the site, because it cost a run. The first before and after comparison was taken against a base directory that was two versions old, because a git stash step had failed silently and the archive was never rebuilt. Every port is checked for what it is actually serving now before anything is compared. A JavaScript off comparison was also run without the font harness, which means it was taken in a fallback face: that produced a 27,178 pixel difference on the homepage which is zero when the harness is applied on both sides. CLAUDE.md already records the fallback face as the cause of a whole version's wrong numbers.

## v1.30.0
- The lens is on /work/ and /systems/ now. Both pages load lens.js, both suppress the old reader pill with the data attribute /book/ and /games/ already use, and both take the compact control, because the presentation is chosen by whether the page has a #reader-frame and neither does. Two more pages on one mechanism, not a third one. The pill and its overlay are untouched on the other twenty eight pages that have them.
- WHAT HAPPENS TO THE EVIDENCE BARS, and why. They are decoration in the strict sense: the figure above each bar is text and the label under it is text, and the bar is a picture of both. In the GOOGLE state they stay, and they grey out with the rest of the wave through the sitewide filter on [data-lens-i]. That is the honest half of it. The markup is still there for a crawler, and what a crawler cannot have is the colour coding, so the colour is what goes: measured at the painted pixel the three classes land on rgb(147), rgb(115) and rgb(107), three greys eight units apart, which is the point rather than a flaw. In the AI state they go entirely, because what an assistant receives of each one is two empty elements with no text and no data attribute. Measured: 5px at full opacity in true colour at lens 0, 5px greyed at lens 1, and height, margin and opacity all zero at lens 2.
- The collapse is driven by height, margin and opacity, never by overflow. CLAUDE.md records that overflow:hidden silently establishes a scroll container and stops animation-timeline: view() firing, and these bars are driven by exactly that. Verified rather than assumed: parked at six scroll positions, every one of the eight bar fills reports the identical transform matrix on this build and on v1.29, six distinct states are seen across the scroll where one would mean nothing fires, and the list of ancestors with a scrolling overflow above a bar fill is empty on both builds.
- WHAT HAPPENS TO THE PIPELINE SIMULATION. It runs in the Person state and in the Google state, because Googlebot renders JavaScript and the Google state on this site is about the markup layer rather than about switching behaviour off. Entering the AI state stops it, because an assistant receives the markup as served and not the run. The card returns to exactly that markup, and the threshold is not repeated in pipeline-sim.js: it reads body.lens-ai, which lens.js sets at its own 1.5, so there is one definition of where the AI state begins.
- Measured at five different points in a run, including both rejection beats: no stage is left with a stale class or a stale state, nothing changes six seconds after the switch, and the card's innerHTML differs from its at-rest state by exactly one thing, the button's disabled attribute. Everything else is byte identical, label, sentence, verdict, all eight stages and the caption. Leaving the state re-enables the button and a click runs a complete run again.
- Both pages get one annotation each in the existing .lens-navnote idiom, counted live from the DOM so the sentence cannot drift from the page, and keyed on selectors that exist on one page each, .ev-bar and [data-sim], so neither can appear on /book/ or the homepage. The /systems/ one states the served card's character count, 355, which was checked independently against the source HTML.
- THE AUDIT, REPORTED AND NOT FIXED, because the brief asked for the finding rather than a clean result. /work/ has a real fault: one heading level skip, H1 straight to H3. There is no h2 anywhere on the page; all eight case study headings are h3 inside a section with no heading of its own, so an outline reader gets a title and then eight third level items hanging under nothing. /systems/ is clean inside main, h1 then five h2s, nothing found.
- A second thing the audit shows, which is the audit's own and not either page's. /work/ reports "outside main: nothing" while /systems/ reports one heading skip in the sitewide chrome, and both pages carry the same footer with its h4. outsideSkips() walks every heading in document order with a single counter, so on /work/ the footer's h4 lands under an h3 and reads as a legal step, while on /systems/ it lands under an h2 and reads as the skip it is. /work/'s own broken outline is hiding the footer fault from the count.
- WHAT THE FINE SWEEP FOUND, and this is the part worth keeping. Six defects, all this version's, all fixed, and the measurement method itself was wrong twice before it found them.
- The page was NOT untouched at lens 0. lens.js appends a .lens-href inside every link in main, and three of /work/'s links are a.row, which is display:grid with align-items:baseline, so the annotation became a grid item in its own right and took 30.3px per row at zero width and zero opacity: 3700px of document against v1.29's 3609 at 1280px, 5591 against 5486 at 390px, rows 257,257,239 against 227,227,209. Removing the three nodes restored every figure exactly. It is display:none at rest now and a full width annotation row once the lens is open, which is what it should have been in a grid row anyway, and the rows measure 227,227,209 again. This never showed up on the homepage because there a.row has two children and the annotation lands in the empty third column.
- The AI band's opacity dip is budgeted for paper. .lens-host main dips to 0.75 across the threshold and the v1.21 comment puts the shallowest reading at about 4.9:1 there. On a near black ground the text falls toward the ground while the ground barely moves, and the same depth took every --ink-2 surface on these pages to 4.12:1 computed and under 4:1 as painted. 0.12 instead of 0.25 leaves --panel-dim at 5.23:1 at the bottom of the dip. The reduced motion rule had to be restated at the new specificity or it would have lost to this one and reduced motion would have gained a dip it has never had.
- The flagged verdict does not survive being greyed. Every content block takes filter:grayscale(var(--g)) in the Google state, and --signal-b greyscaled is rgb(115,115,115), which is 4.01:1 settled and 2.76:1 with the dip on top. The one thing v1.28.0's brief asked to stay unmistakable is the flagged state. The verdict crosses to the page's own ink as the page greys instead of being greyed down from orange: at lens 0 it is the --signal-b v1.28.0 measured at 5.45:1, and by full Google it is --panel-ink, which greyscales to rgb(235,235,235). The word is what a machine receives and the word stays readable the whole way across. This also corrects v1.28.0's own note, which claimed those figures held "at every state of a full run": true of the only state the page had then.
- The disabled run button was a 45 per cent wash built for the few seconds of a run, and the AI state holds it indefinitely: 3.98:1, and 2.77:1 at the bottom of the dip. In that state it takes the flat hairline treatment the injected cards take there, which is legible and an honest picture of what it is by then, an element in the markup rather than a control anybody can work.
- The control lands across the footer's last line at the foot of the page, and this one had to be settled by looking rather than by measuring, because every contrast figure it produced was an artefact. wut.js clips to the element's rectangle without clamping it to the viewport, so a line below the fold is screenshotted from wherever the clamp lands: that is how a span 130px past the bottom of the screen came back reading 1:1 against the green of the AI dot, and it is where my own 2.03:1 and the adversarial review's 1.73:1 both came from. Measured only where the line is genuinely on screen and genuinely overlapping the control, both pages read 6.42:1, the same as /work/ at v1.29 with the old pill. Screenshotted at 390px at the foot of the document, though, the last line reads "Static HTML. No framework, no tracking, no" with the pill drawn straight through it and "cookies." behind it, on this build and on /book/ at v1.29 alike, at 94 per cent opacity and at 100. So the footer reserves the room the control occupies. It costs 76px of document at every lens value, which is the price of a control that is centred rather than tucked into a corner.
- Both annotations were clipped mid sentence at 360, 390 and 414px, losing 51px and 70px at 360, 33 and 51 at 390, 15 and 33 at 414. .lens-navnote caps at 6rem so it can grow with --g rather than appear, and that cap was cut for /book/'s picker note, which is one line at every width. 12rem clears the tallest of these by 26px.
- An opacity ramp washes out the text that is already on screen, not just the part still arriving. Every injected annotation faded in on --g or --ap, and the head card's own opacity ramps 0.28, 0.61, 0.93 across the band; its caption, sampled the same way at each of those points, reads 1.16, 1.49, 1.96 against a flat 2.10 to 2.13 when the reveal is carried by size instead. The stylesheet's own remedy for .lens-tag, full opacity by a sixth of the band, only narrows the band. So on these two pages the reveal is size alone. They all already grow, the cards by a measured height inside an overflow:hidden wrapper, the tags and destinations by max-width, the notes by max-height, and the card's opacity now stays at 1 throughout, so a glyph is either fully painted or not yet on screen. A clipped glyph is not text anybody is failing to read; a washed one is. The same ramp is on /book/ and the homepage and is reported rather than touched.
- THE MEASUREMENT ITSELF WAS WRONG TWICE, in opposite directions, and both errors changed the answer. wut.js, this project's own tool, takes the foreground from computed color, so it cannot see an ancestor's opacity or filter and over-reads wherever the lens applies either: it reported 6.47:1 for a label rendering at 4.06:1. Taking the foreground from the painted pixels instead under-reads, because mono text at 0.68rem has no pixel at full coverage: .ev-lab's core paints at 56 where the composite is 134. The sweep now computes the foreground analytically, the element's colour through whatever greyscale an ancestor applies, composited at the cumulative group opacity over the background pixel actually painted under each glyph. The first run of it also left the scroll driven reveals running, so every .ev-* reading was measuring its own fade rather than anything the lens did; they are parked at their finished state now.
- Verified: the control is inside the fold. At 360, 390 and 414px against a 750px visible area it sits at 686 to 734, fully inside with 16px to spare. Scroll position does not move when the state changes: measured against the line the lens actually anchors, the drift is 0 to 1px at both widths and both scroll positions, the same as /book/. An element further down the viewport does move, because the content between it and the anchor grows, which is the transformation rather than drift.
- Verified: with JavaScript off both pages are pixel identical to v1.29 at 390px and 1280px, zero differing pixels, with no control, no lens-host, no injected cards and no annotations, all eight bars at their full 5px and the simulation card and the three outcome tiles still carrying their ground. That last one is not automatic: the new rules read --a, and --a is registered with @property so it resolves to 0 where that is supported, but where it is not the calc would be invalid at computed value time and an invalid var() computes to unset rather than falling back. The rules are scoped under .lens-host, which is never added without JavaScript, so they never match and never reach an invalid value.
- Verified: under reduced motion the lens snaps, --lens going straight to the target across ten frames with no easing, main pinned to opacity 1, the wave's stagger at 0 and every bar at its finished state.
- Verified: all 33 documents. Exactly /work/ and /systems/ lost the pill and gained the control; the other twenty eight that had a pill still have one that opens and closes; /book/ and the homepage are unchanged on both builds; /games/ and 404.html never had one. Every selector this version adds matches on those two documents and returns zero on every other one.
- One low reading is inherited and is recorded rather than fixed. The control's own labels go unreadable while the thumb slides under them: driven by a real pointer drag through 129 positions, the worst is 1.15:1 at lens 1.5938, the AI label over the thumb's lit ring, and /book/ at v1.29 returns the identical figure at the identical value, as does the other of these two pages. The cause is in lens.js rather than in any rule here. apply() only rewrites aria-pressed when the word it announces changes, so through a whole drag the label the thumb is passing under is still the unpressed --panel-dim rather than the white a pressed one gets. It wants its own version. What this version does change about the control, lifting the fill to --panel-2 so it is not the same colour as the page, costs 0.62 at the settled stops, 4.99:1 at worst here against 5.61:1 on /book/, and nothing at all at the drag's worst case.
- The adversarial review found the false claim that produced those two figures, and it was the characteristic one for this repository: the number came from one measurement method and the word "identically" from another, so neither was wrong on its own and the sentence was. It also found the compositing error in the analytic tool written for this version, which composites the foreground over an already composited background and so double counts group opacity wherever an element has a local background of its own. It is under one unit for the card on ground pairs used here, and every headline figure was re-derived by hand independently, but it would be badly wrong for a chip or a button, which is exactly what it was pointed at.

## v1.29.0
- Four pages are dark and all four had a near paper bar sitting directly above a near black page. The site's own sticky header, header.top, is dark on the homepage, /work/, /systems/ and /games/ now. Every light page keeps the light one.
- WHY IT NEVER FOLLOWED ON ITS OWN, which is not quite what it looks like. .top's background is color-mix on --paper, and it is fair to assume the homepage's header goes dark because the homepage overrides --paper somewhere above it. It does not. --paper is declared in four places on this site: :root, body.home main, body.arcade main and body.dark main. Three of the four are scoped to MAIN and the fourth is the root, so no page overrides it at body level and the header, which is a sibling of main inside .shell, never sees a dark value on any page. The homepage's header was dark because .home .top set its background explicitly. This version does the same thing for all four rather than widening any token scope, which is also what keeps it off the other twenty nine documents.
- ONE TOKEN, NOT THREE RULE SETS. /games/ sits on #04070e and the other three on --panel, so the bar reads --top-ground, declared var(--panel) on body.home and body.dark and #04070e on body.arcade, and mixes it at the same 84 per cent the homepage has always used. --top-line follows the same split, --panel-2 on the warm three and the arcade's own --line value on /games/. Nothing is hardcoded to one of the two grounds.
- Measured, worst pixel under the glyphs, 12 pages by three widths by seven scroll positions on each build, 336 header states per build. The brand on the four dark pages: 11.09, 12.03, 11.71, 13.54. The nav links: 5.75, 5.82, 5.74, 7.11. The burger icon with the panel closed, 390px over eight scroll positions: 5.92, 5.53, 5.97, 5.93. All eight light pages read identically to the shipped build to two decimal places, brand, nav and burger alike.
- THE ARCADE IS THE ONE THAT COULD HAVE FAILED, because the bar is 84 per cent opaque with a 10px backdrop blur and /games/ is the one page whose content is bright. A 25 position fine scroll through the neon cabinets gives a worst nav of 6.28:1 at 1280px and 6.16:1 at 860px. The hero game itself is a cross origin document from snap-hit.online that the build container cannot fetch, so it was stubbed with a full bleed pure white page, which is brighter than any real frame of it: the bar lifts only to rgb(44,47,53) under the glyphs and the nav still reads 5.60:1 with the brand at 11.28:1.
- One element beyond the header itself needed changing and it is worth recording. The skip link is var(--ink) on var(--paper), a near black box, and it takes focus on top of the header. On a dark header that box IS the header. body.home already inverted it and body.arcade already inverts the arcade's own .hero-skip, so it inverts here too. Everything else near the bar was checked and needed nothing: the nav underline is the spectrum, the focus ring is --index-b at 4.26:1 or better on these grounds, ::selection in the header resolves --paper at the root value because the override is on main, and the reader pill was given its dark treatment in v1.28.0.
- THE ADVERSARIAL REVIEW CAUGHT TWO WRONG COMMENTS, both in this version's own additions, and the second of them was a warning this version had deleted. Recording both, because the second is the more useful.
- The burger takes --panel-dim on all four bars, not the lit #ADA79A the nav links take, and the comment beside it claimed --panel-dim measures 6.46:1 on the homepage's bar and no lower on the other three. Neither half held. 6.46:1 was a single unscrolled reading, and swept properly over eight scroll positions with the same masked method every other figure on this site uses, the four bars read 6.37, 5.95, 6.29 and 5.80, worst on /games/. The claim is now the measured range. The icon still needs no lit variant, and the reason is size rather than luck: the links are 0.92rem letterforms and the icon is three 2px bars, so the antialiased fringe that drags a glyph's worst pixel down is a smaller share of a straight edge.
- The rewritten nav link comment had dropped a figure the v1.21 comment recorded, 4.05:1 for --panel-dim on those links, on the grounds that it was stale. It is not stale. Forced onto the nav links today, --panel-dim measures 4.11:1 at worst, on the homepage at 860px, still under the floor, with /work/ at 4.74, /systems/ at 4.56 and /games/ at 5.72. --panel-dim is the obvious dark ground token and the tempting simplification, precisely because the burger beside it uses it, so the one quantified reason not to reach for it had to go back in the file. #ADA79A stays, at 5.74:1 to 7.11:1 across the four.
- The 3px spectrum rule at the top of the page is two elements, and only one of them is meant to be seen. body::before is the full spectrum at opacity .26, an unread track, and body::after is the scroll progress painted over it at full opacity. The filled bar renders byte identically on both builds and reads BETTER against the new header than the old one: on /work/ 2.93:1 to 4.99:1 at the orange end, on /games/ 3.08:1 to 5.02:1. The 26 per cent ghost reads 1.31:1 on a dark page and 1.27:1 on a light one, unchanged by this version either way.
- The header's bottom edge is now as strong on the dark pages as on the light ones and no stronger: --panel-2 on /work/ reads 1.20:1 against the bar, the arcade's line on /games/ 1.30:1, against 1.25:1 on /about/ and the homepage's own 1.19:1. The shipped dark pages had a 14.57:1 edge there, which sounds better and was in fact the defect: a hard cut between a paper bar and a near black page.
- Twelve header states came back under 4.5:1 and none of them is this version's. All twelve are the same state, 390px with the burger panel open and the page then scrolled to 700, on all twelve pages including the eight this version does not touch, and the shipped build fails the same twelve with the same numbers. The cause is already documented in this file: with the panel open the .shell is transformed, a transform makes the sticky header stop sticking to the viewport, so the burger drifts up out of the bar and the part still on screen sits over the panel rather than over the header. On the three changed pages it improves slightly, /work/ 3.77 to 3.87, /systems/ 3.73 to 3.85, /games/ 3.87 to 3.91, and the homepage and all eight light pages are unmoved. It clears the 3:1 a graphical control needs and not the 4.5 this version's brief asked for text, and it wants its own version.
- Verified: /book/ was checked rather than assumed, because it is a light page that goes dark in its lens machine states. Its --paper stays #FBFAF6 and its root stays rgb(251,250,246) at every value of the lens, so what goes dark there is a component and not the page. Its header correctly stays light, and the four dark pages are the whole set.
- Verified: the homepage is untouched despite its rules being rewritten. With the particle field hidden and every animation off it pixel diffs to exactly zero differing pixels at 390px and 1280px, and .top, .brand, the nav links, the current page link, the burger and the skip link all compute identically old to new.
- Verified: of the 33 documents, three change. /work/, /systems/ and /games/ differ by 25346, 25346 and 25340 pixels at 390px, which is the 390 by 67 header and nothing else. The other 30, 404.html included, are pixel identical at 390px and 1280px. The burger panel opens and closes correctly on all four dark pages, aria-expanded and the panel's hidden attribute both toggling and the shell transform returning to none, and the panel itself renders identically. 180 page states across normal, JavaScript off and reduced motion at 360, 390, 414, 860 and 1280px, with zero horizontal overflow, and the 660 to 960 band checked at six widths on both changed dark pages. theme-color and color-scheme are untouched.

## v1.28.0
- The site had a system it had not named. The demonstrations are dark, the homepage with its field and its lens and /games/ with the arcade, and the reading is light, /book/, /super/, /writing/, /about/, /colophon/. /work/ is an evidence wall that animates as you scroll it and /systems/ runs a rejection through a governance pipeline in front of you. Both are demonstrations and both sat on paper. They are dark now, and every reading page is untouched.
- WHICH OF THE TWO EXISTING DARK TREATMENTS, because there are two and this invents neither. The warm one, which is --panel and its family: the footer on all 33 documents, the hero band, every dark panel on the site, and the state every visitor arrives in on the homepage. The cold one, #04070e, was chosen on /games/ so that neon reads as light coming out of darkness, and there is no neon on either of these pages. The stated point of this version is to remove the hard cut between the homepage and the page next to it in the nav, and the homepage's own ground on arrival is --panel. One consequence to record: main and footer.site are now the same colour on these pages, exactly as they already are on the homepage, and the 3px spectrum rule at the top of the footer is what marks the boundary.
- The mechanism is the one both existing treatments already use: override the ground tokens on MAIN rather than rewrite the rules, so .pagehead, .section, .prose, .rows and the rest come along untouched. Scoped to main and not to body because the sitewide header, the footer and the reader overlay sit outside it and are meant to stay as they are.
- THE EVIDENCE BARS DID NOT NEED ADJUSTING, and the reason is worth recording: they are BETTER on this ground than on the paper they were drawn for. Measured against #131109 and against #FBFAF6: the orange --signal-b goes from 3.32:1 to 5.45:1, the blue --index-b is unchanged at 4.26 against 4.25, and the green --ok-b goes from 2.46:1 to 7.36:1. The green was below the 3:1 that non text needs and had been since the bars shipped. Against their own track the three read 4.57, 3.57 and 6.17 on the dark ground where they read 2.66, 3.40 and 1.97 on paper, so two of the three were under 3:1 there as well. Not one of the three colours was changed.
- The track keeps the same relationship to its ground that it had: --panel-2 on --panel is 1.19:1 where --line on --paper was 1.25:1, so the groove is as visible as it was, which matters because the unfilled part of the bar is what makes it read as a range rather than a dash.
- WHAT DOES NOT WORK, and this version tried it first: swapping --signal, --index and --ok to their lit variants at token level alongside the ground tokens. It looks like the same one line move and it silently kills the evidence bar hover. The bars' REST state already uses --signal-b, --index-b and --ok-b, and their HOVER state reaches for the deep ones to darken them. Swap the deep ones to the lit ones and hover becomes identical to rest, so the feedback disappears with nothing on screen to say so. The adversarial review caught it. The accent tokens are therefore left alone, and the three places on these two pages that need a lit accent are named individually.
- So the hover was rebuilt for the ground rather than inherited. On paper it deepened the heading and all three bars; on near black, deepening is a colour going out. Each bar now returns to its lit variant and takes brightness(1.25), which lifts #F0561D, #3B6BFF and #1FB878 without collapsing the hues into each other. Measured at the rendered pixel, the three bars move by 44, 42 and 84 out of 765 on hover, against 84, 105 and 114 on the shipped build in the opposite direction, so the feedback is comparable in size and now points the right way.
- The row lift is 6% of --ink and not the homepage's 9%, and the difference is the hovered heading. --signal on that lift measures 3.4:1, so the heading takes --signal-b; at 9% that reads 4.42:1 and at 6% it reads 4.82:1 under its own glyphs, against 5.27:1 on the shipped build. The lift itself is 1.13:1 against the ground where the shipped white lift was 1.04:1, so the row is more obviously hovered than it was, not less.
- /systems/ takes the lit accents in two rules. The verdicts and the active reject state sit on .sim-doc, which keeps reaching for --paper and therefore lands on the page ground, where --signal measures 3.58:1 and --ok 3.53:1. As --signal-b and --ok-b they read 5.45:1 and 7.36:1, measured under the glyphs at every one of the ten states of a full run.
- Leaving the accent tokens alone fixes something else the review found. ::selection is background:var(--signal) with color:var(--paper), and with --signal swapped the highlight would have been the lit orange and selected text would have read 3.32:1 here against 5.05:1 on every other page. Unswapped it is 5.05:1 everywhere. Only the colour is pinned, because --paper is overridden and would otherwise paint selected text near black on the orange.
- THE READER PILL LOST ITS CONTAINER and that had to be fixed here. Its background is --panel, which on paper is a dark chip at 16.1:1 and on this ground is the same colour as the page: measured, pill fill rgb(19,17,9) on ground rgb(19,17,9), with nothing left of it but a 1px border at 1.19:1 and a shadow that does not exist on near black. These are the first dark pages that carry it, because the homepage has the frame instead and /games/ and /book/ set data-no-reader-pill. The fill lifts to --panel-2 and the hairline to --panel-dim, 6.43:1 against the ground, with the pill's own text at 13.31:1. That is a weaker edge than the pill already draws on paper, so it does not become a brighter object than it is elsewhere. The rule is scoped to body.dark, which exists on these two documents and nowhere else.
- Four cards, not two. .sim and the three .outcomes tiles take the same lift the homepage's cards take, #1b1811 off #131109, rather than staying lit islands. .sim-doc inside the simulation keeps reaching for --paper, so it lands on the page ground and stays a step darker than the card holding it, which is the relationship it had on paper. .sim-btn needed nothing: it is background:var(--ink) with color:var(--paper) and inverts itself into a light pill with dark text.
- The scroll driven reveals still fire, and that was verified by reading the transforms rather than by looking: parked at six scroll positions, each of the eight bars reports the identical scaleX matrix on both builds, and six distinct states are seen across the scroll, where one would mean nothing fires. The only scroll container inside main is .pagehead, on both builds. Under reduced motion and with JavaScript off every bar reports scaleX 1 at opacity 1, which is the finished state CLAUDE.md requires, and nothing in main sits under 0.2 opacity.
- Verified: the pipeline simulation runs end to end at 390, 860 and 1280px. Both rejections and the final pass are legible and the flagged line is the most prominent thing on the card: FLAGGED and FLAGGED AGAIN in the lit orange at 5.45:1, the stage marked REJECTED in the same orange, the dot already lit, and the offending phrase highlighted behind it.
- The sweep is 136 measurements, worst pixel under the glyphs, across both pages at 390, 860 and 1280px with the reader overlay closed and open. Nine come back under 4.5:1, in three families, and none of them is this version's doing. .sim-name read 1:1 once, which was the sweep scrolling it under the sticky header rather than the page: measured directly, all four instances read 6.03:1. .sim-btn reads 4.24:1, which is the disabled state the button holds while the simulation runs, at opacity .45 over the card; the shipped build measures 2.90:1 in the same state, so it improves, and enabled it is 15.88:1 against 17.58:1 shipped. And .roverlay-card dt reads 3.81:1 on the reader overlay's white card, identically on both builds and on /about/, which this version does not touch, so it is a pre-existing sitewide fault in the overlay. It is rgb(138,130,114) on pure white and it wants its own version.
- Verified: all 31 other documents, the 30 other pages and 404.html, are pixel identical at 390px and 1280px with animations frozen. The homepage reports 1.638% and 2.208% of pixels differing, and shipped against shipped in the same harness reports 1.620% and 2.163%, so that is the particle field's own frame noise and not a change. The burger panel is pixel identical on these pages too. No horizontal overflow at 360, 390, 414, 860 or 1280px on either page.

## v1.27.0
- The adversarial review found no rendering regression and six wrong or incomplete claims, all corrected above: the homepage kB figures were the test harness's rather than the site's, /book/'s response card moves too so there are three self measurements and not two, the colophon percentages were measured before the head comments were dropped, /games/'s theme-color disagreeing with its own light header was a decision presented as a non event, 404.html's theme-color was one unit of blue off its own ground and is now exact, and the note on the framed games stated its condition more broadly than the measurement supports. It also showed that the head tags do something measurable, which this version had written off.
- Every document on this site computed color-scheme as normal and declared no theme-color, so the browser painted its own defaults for everything the site does not style and had nothing to go on before the stylesheet arrived. Separately /games/, which is near black, kept its ground on BODY, so the root canvas under it stayed paper. All 33 documents now declare both, 404.html included, and /games/ takes its ground on the root.
- THE ONE VISIBLE CHANGE, and it is a real one: the scrollbar on the two dark pages. With color-scheme normal Chrome paints a LIGHT scrollbar whatever the reader's operating system is set to, so the homepage and /games/ had a white bar down the side of a near black page. Measured at 390px, the track reads rgb(252,252,252) on the shipped build in an operating system set to light AND in one set to dark, and rgb(44,44,44) on this build in both. The light pages measure rgb(252,252,252) before and after, unchanged. Everything else on every page is byte identical, verified below.
- A BUG THIS VERSION INTRODUCED AND THEN MEASURED OUT, and it is the interesting part. color-scheme is inherited by a framed document that declares none of its own, and when the embedder's scheme and the child's disagree Chrome stops treating the child's canvas as transparent and paints it opaque WHITE. /games/ frames four cross origin documents from snap-hit.online. Measured with a stub child that declares nothing: the hero cabinet went from #04070e to pure white, because .hero-game-frame's own near black background stopped showing through. Same when the child fails to load. A child that does set its own ground was unaffected. Since those documents are not this site's, what they declare is not ours to rely on, and a white rectangle inside a cabinet is precisely the defect this version exists to remove. body.arcade iframe{color-scheme:light} pins them back, and all three cases then measure identical to the shipped build.
- THE FLASH THIS VERSION IS NAMED AFTER COULD NOT BE REPRODUCED, and the reason turns out to be structural rather than lucky. This stylesheet is render blocking in the head, so the browser paints no frame of the new document until it has arrived: held back 400ms, held back 3000ms, with a known magenta document in front so that anything white would have to belong to the new page, the previous page's pixels simply stay. An earlier reading of one frame of pure white turned out to be the blank tab before the navigation, not this site.
- BUT THE HEAD TAG IS WORTH SOMETHING, just not there. Block the stylesheet outright, which is a 404, a timeout or an extension rather than a hypothetical, and the difference is plain: measured at 390px, the shipped build paints pure white on the homepage and on /games/, and this one paints Chrome's own dark canvas, rgb(18,18,18), with its default text legible on it. /work/ and every other light page paints white on both, which is correct. The two tags cost two lines a page and this is what they buy.
- A DECISION WORTH NAMING, because it is the one place this version changes something outside the page. /games/ declares theme-color #04070e, its ground, and the top 65px of /games/ is the sitewide light header, which style.css keeps there deliberately. So on a phone the browser's own band is now near black directly above a near paper strip, where on the shipped build with a light operating system it matched. The same goes for the overscroll at the top of that page: paper before, #04070e now. The ground won because it is the whole of main and the header is 65px of it, and because the brief asked for the colour the page actually uses; the cost is that one seam at the very top. The overscroll at the BOTTOM improves by the same change, since the footer is near black on every page and used to sit above paper.
- /book/ keeps color-scheme light even though the lens takes it dark in the two machine states. The lens is a scrub that can be held at any value, so following it would mean flipping the scrollbar's colour under a held finger, and a scrollbar that changes colour mid gesture is worse than one that does not match for the seconds a machine state is held. The states are also transient: the page is paper on arrival and paper again when the control is let go.
- The two tags carry no comment in the head, on purpose. /colophon/ prints the first 26 lines of its own source as a live proof panel, and two standard meta tags do not need explaining in 33 files when the reasoning is in the stylesheet. They still cost that panel two of its 26 lines, which is why /colophon/ is the one document whose pixels change: measured, the difference is confined to a single box inside the panel, 0.60 per cent of the page at 390px and 0.81 per cent at 1280px counting pixels that differ by more than 10 of 255, with the page height identical at both widths. The panel still reads correctly.
- Both pages that carry the lens report their own size, and both numbers move. In the machine states the response card prints what the assistant actually received: the homepage goes from 17.7 kB over the wire and 17.4 decoded to 17.8 and 17.5, and /book/ from 30.7 and 30.4 to 30.8 and 30.5. Those are the real figures, taken with nothing intercepting the request, which matters because the test harness strips the integrity and crossorigin attributes from every document it serves and so reports the page 108 bytes lighter than it is. Verified that the size is the ONLY content difference in those states: a text node by text node diff of the whole of main at 1280px in the AI state returns that one line and nothing else, with main at the same offset and the same height, and with the scroll pinned the two builds are pixel identical in both machine states. Three live self measurements on this site move in this version, the colophon's source panel and those two response cards, and all three move because they are telling the truth about a document that changed.
- Verified: a full page pixel diff of all 33 documents at 390px and 1280px, with animations frozen, in an operating system set to light and in one set to dark. Every document is byte identical except /colophon/, for the reason above. With JavaScript off, compared with CSS animation disabled at the stylesheet rather than by script, nine documents including /colophon/ and 404.html are byte identical, and under reduced motion so are all of them. The homepage particle field is still a fixed canvas at z-index -2 painting page wide, not only behind the hero: measured at scroll position 3000, 19.0 per cent of pixels change between two frames on the shipped build and 20.5 per cent on this one, which is the field's own randomness and not a structural difference. No horizontal overflow at 360, 390, 414 or 1280px.

## v1.26.0
- Since the homepage went dark in v1.23, every image inside main on it has sat as a hard white rectangle in a soft dark rounded card. None of these files could be touched: the badges are other organisations' certification marks and recropping someone else's mark is not ours to do. So the white becomes a deliberate surface rather than an accident, entirely in CSS.
- A GEOMETRY BUG THAT WAS ALREADY LIVE, and it is the larger half of this version. .tile img is width:100%;height:100%, written when the img was a direct child of .tile, which is a flex item carrying the grid row's definite height. lens.js wraps every img in main in span.lens-fig, whose height is auto, so the percentage stopped resolving and img,svg{height:auto} took over: the image was sized by width alone and grew past the fixed height of its tile, where overflow:hidden cut it. Measured with JavaScript on: badge-agile rendered 133 by 133px in a 133 by 106px tile at 390px, and 280 by 280px in a 280 by 106px tile at 700px, which is most of a certification mark sliced off. The photographs had the opposite half of the same fault and sat short in their tiles: nathan.jpg was 161 by 161px in a tile 294px tall. With JavaScript off the page was correct all along, which is why it never looked like a bug. Giving the wrapper the height its child was written against restores all of it.
- The three circular badges are clipped to discs with clip-path:circle(closest-side), not border-radius:50 per cent. The tile is not square at any width, and a 50 per cent radius on a 121 by 116px box is an ellipse; closest-side takes the shorter side, so it is a true circle everywhere, and it clips the background as well as the image. Where clip-path is unsupported the badge renders exactly as it does today, which is the state this replaces rather than a broken one.
- Verified that no artwork is clipped, computed from the real rendered geometry rather than from a screenshot: 291 rendered badges across 97 viewport widths, every 20px from 320 to 1920 and every single pixel across the 855 to 870 boundary where the grid changes from two columns to six. The source ink boxes are 373 by 374px in a 449 by 405px frame, 311 by 312px in 342 by 329px, and 385 by 384px in 408 by 408px. The worst case anywhere is badge-agile reaching 89.3 per cent of its disc radius at 1080px, and the smallest clearance at any width is 6.16px, at 861px. Nothing is cut.
- The badges are named in the selector by file rather than by position. A rule matching every plain tile would silently crop the next badge somebody adds, and clipping another organisation's mark is a worse failure than leaving one square.
- The strip is a pill, and that is the judgement call. badges-lead-anz.png is 1600 by 273px, six circular badges in a row on white, so it cannot be a disc. A 14px rounded card was built first and it reads as a different object sitting next to three circles; a pill reads as the same object stretched, and consistency across the row matters more here than any one treatment being optimal. Its width is capped at 400px, and that is not cosmetic: the strip's height follows its tile's WIDTH while the row height is fixed, so in the two column band the tile reaches 757px and the pill grew to 136px inside a 106px content box, eating the card's padding entirely and leaving about 2px of dark between itself and the border. border-radius:999px resolves to half the height, so the end caps grew with it, to 68px against a fixed 22px of padding, and began shaving the outermost antialias off the end badges from about 820px. Capped, the pill is 74.7px tall at its largest, sits centred in its card the way the discs sit centred in theirs, and clears the outer badges' solid ink by 8.16px at its worst across 89 widths, the faint antialias by 7.17px.
- The photographs did not need a radius. .tile is border-radius:16px with overflow:hidden, so it already clips them to the card's own corner; a radius on the img itself would have done nothing. What was actually wrong with them was the geometry above, and they fill their cards now. That took two goes: pinning only the wrapper's height made the img's own width:100% cyclic, so the image resolved from its aspect ratio at that height and the wrapper shrink wrapped to it, which left nathan.jpg, a square photograph, sitting 45.8px short of each side of a 386px card at 860px while the caption gradient still ran the full width. The wrapper needs both dimensions, and the same omission was spilling the badge alt text 9px outside its own dashed border in the Google state.
- The book cover needed one thing more. It is a landscape photograph in a tile that is portrait at phone widths, so object-fit:cover has to crop it hard, and centred the crop cut the title off mid word. The book sits between 50 and 92 per cent across the photograph and the window at 390px is 40 per cent of it, so the window is centred on the book instead. object-position is a proportional alignment rather than a window centre, which is the part that is easy to get wrong: at 390px, 85 per cent puts the window at 51.0 to 91.0 per cent, the book with a per cent to spare each side. That holds from 375px up, and it is the widest the cover can be shown: at 320px the window is only 31.3 per cent of the photograph against a cover 35.7 per cent wide, so something has to go whatever the value, and 85 per cent loses 5.5 per cent off the left. Checked at 320px: the title and the author line are whole and only the strapline is cut. Not worth a breakpoint at a width this site does not otherwise test.
- One thing the disc costs, recorded because it is visible. Padding on a border-box element has a floor, so in the AI state the disc's 6px would have held a 12px box open where lens.js collapses the image to nothing, and the strip's height:auto would have kept its whole rendered height, which follows the viewport: 60.4px at 390px, 74.7px at 700px, 35.9px at 1280px. Both sit at opacity 0 by then, so nothing shows, but the tiles are a fixed height and every pixel held there is a pixel of alt text that overflow:hidden cuts. Zeroed in that state, so the AI state is now pixel identical to the shipped build.
- Known and not this version's to fix: in the AI state the alt text already overflows these tiles and is clipped, on the shipped build as much as on this one, because the bento rows are a fixed 150px and 140px. Measured identical before and after. Unclipping it is a layout change to the evidence wall rather than an image treatment.
- The badges are not all the same size inside their discs, and cannot be. The three files carry different white margins, so at 390px the artwork fills 85.9, 85.4 and 88.3 per cent of its disc, which is a three per cent spread and invisible. Between 861 and about 1000px the six column tiles are narrower than they are tall and the crop is width limited, and there the spread opens to 75.0, 79.6 and 85.5 per cent. The discs themselves stay identical, which is what the eye follows.
- The adversarial review found one real regression and four wrong numbers, all fixed above. The regression was the wrapper width, which was breaking the very thing this version claims to fix, and it was invisible in the two screenshots I had been looking at because it only appears between about 670 and 860px and again above 1080px. The wrong numbers were the pill's end cap radius at 390px, which is 30.2px and not 32, the dark left under an uncentred strip, which is 45.6px at 390px and 59.6px at 1280px and never 55, the height the strip would have held in the AI state, which is a per width figure quoted as though it were general, and the claim that the book cover is whole at every width, which is true from 375px up and not below. It also confirmed independently that no badge artwork is clipped at any width, that nothing leaks to /about/ or /book/, and that the contrast inside the bento is unchanged at every held lens value.
- Verified: every element box on all 32 pages at 390px and 1280px is unchanged, the homepage included, because everything here happens inside the tiles. Every image on /about/, which uses the same four badge files, and on /book/, which uses the same cover, is identical in box and in every computed property. On the homepage six images change and the seventh, the book cover in the band, does not. No horizontal overflow at 360, 390 or 414px with JavaScript on, with it off, or under reduced motion, and the three discs and the pill are the same treatment in all three.

## v1.25.0
- The control that drives this entire page began 3px BELOW the visible fold on a 390px phone. Measured on the shipped build: the slider ran from 753px to 795px, and Chrome leaves about 750px of page above its bottom bar. A page whose one job is to get the lens used was hiding the lens. It now runs 526px to 568px at 390px, 599px to 641px at 360px and 478px to 521px at 414px: inside the top 650px at every phone width, with at least 109px of page still showing below it.
- FIRST, A CORRECTION TO THE TOOLING, because it invalidates numbers rather than opinions. This container cannot reach fonts.googleapis.com, so every height the test harness had ever measured was measured in a fallback face rather than in Instrument Sans and JetBrains Mono. At 390px that put the slider at 826px instead of 753px, a 73px error, and a page that fits would have been called a page that does not. The harness now serves the real webfonts from disk, and only then did every reading agree with the brief to the pixel. Where an earlier version quotes a text position, it was taken in the wrong metrics. Positions of fixed height chrome, contrast ratios and overflow checks are unaffected: a wider fallback face makes an overflow test harder to pass, not easier.
- Most of the fix is one line of markup. The control is now the frame's FIRST child, the way a browser puts its tabs above its address bar rather than below it. The frame had its address bar and an instruction above its tab row, which no browser does, and which also explained the control before showing it. That alone is worth 150px at 390px, and it is the right order at every width, so it is not a mobile only reorder.
- The rest is rhythm, on the homepage and below 700px only: the hero's top padding 3.4rem to 2.4rem, the h1's bottom margin 1.3rem to 1rem, the lede's 1.1rem to .85rem, and the frame's top margin 2.7rem to 1.6rem. The h1 and the lede themselves are untouched at every width. They are the argument.
- The hero's eyebrow is hidden below 700px, and only the hero's, and only on this page. At 360px it wraps to two lines and costs 57px, and rhythm alone could not buy that back: every margin above the frame tightened as far as it can go without the page reading cramped still left the control below the target at that width. What it says is the host you are already on and the city, and the city is also in this page's structured data, its title, and both /about/ and /contact/. Not in its meta description, which names the industries and not the place: that claim was written and then checked and it was wrong. Verified that the five section eyebrows further down the homepage still measure 20px, and that every other page keeps its own at every width.
- The credential line stays where it is. Moving it below the frame was the other 63px on offer, and it needed the hero's wrap turned into a flex column to reorder without touching the markup, which stops every margin in that wrap from collapsing and so changes spacing that was not being asked about. The rhythm and the eyebrow got there without it.
- Desktop: the frame's internal order changes at 1280px too, deliberately, and nothing else does. The frame's own height and the offset of its body inside it are identical before and after, 158px at 1280px and 193px at 390px, so nothing below the frame moves at all. Verified across /, /book/, /work/, /super/rules/ and /games/ at 1280px and 390px: every element's box is identical on all of them except this page's own hero below 700px.
- THE PERSON STATE. The thumb's fill goes from a 16 per cent tint of the reader's hue to 32 per cent, which is what turns it from an outline into a chosen block, and the row it rides in gets a recessed channel so the thumb reads as a cap sitting in a slot. The glow flips from pointing up to pointing down: it pointed up, which was fine while the control sat in the middle of the frame, and the control is the frame's first child now, so an upward glow is drawn straight into .frame's overflow:hidden and thrown away.
- A CONTRAST FAILURE I INTRODUCED, AND THEN A SWEEP TOO COARSE TO SEE THE REST OF IT. The first attempt also took the ring to full hue at 1.5px, matching the /book/ pill. The ring is the one surface on this control that a label can end up sitting on: mid drag the thumb straddles two stops and its vertical edge runs through the middle of a centred label. At full hue that is 2.93:1 under the glyphs of "A person" and 3.81:1 under "Google". Backing the ring off to 55 per cent read as 5.31:1 and that number was wrong: it came from a sweep in steps of 0.25, and the worst case sits at a lens value of 1.90625, under "An AI assistant", which a sweep that coarse steps straight over. Swept properly, in thirty seconds of the range at three widths: the shipped ring, 55 per cent at 1px, measures 5.36:1; 55 per cent at 1.5px measures 4.63:1; 45 per cent at 1.5px measures 5.29:1. So the ring grows and pales by the same amount, and the margin is the one this control shipped with rather than three quarters of it. What makes it read as chosen is the fill, the channel and the attract.
- Two grip marks down the thumb's edges were built, looked at and thrown out. At 360px the left one lands under the reader dot and the right one reads as a text caret. The pill on /book/ has no grips either.
- The two dividers moved off the stops and under the thumb. As a border on the stop itself the left divider is drawn by the stop, which sits ABOVE the thumb so its labels stay readable, and it therefore ran a dark line straight down the selected thumb's own ring. Painted on the row instead, at the thirds the stops actually occupy, they pass beneath the cap the way a channel should. Only once lens.js has taken the row over: with JavaScript off the stops are content sized rather than thirds, and they keep their own borders.
- THE ATTRACT, modelled on the arcade cabinets' press to play card, which is already this site's way of saying touch this. About a second after the control is fully on screen the thumb leans toward the next reader and settles back, twice, with the Person signal colour brightening and decaying, and then it is finished for good. Measured: 30px of travel, which is 26 per cent of a 116px thumb, two runs of 1600ms, the glow reaching 1 and returning to 0, and the class taken off on animationend so no state is left behind.
- Nothing in the attract writes --lens. The value that drives the document is untouched, so it cannot flash the page, cannot move the reader, and cannot disagree with the scrub. Measured across every sampled frame of it: the control's top stays at 526px and the document stays 9102px tall. It is a transform on one absolutely positioned element and an opacity on that element's own pseudo-element.
- It is cancelled rather than left to finish. pointerdown drops the class before anything reads a coordinate, which drops the transform in the same frame and puts the thumb back where --lens says it is. Measured: on pointerdown the class is off, the transform is none and the glow is 0. The value maths measures the TRACK and never the thumb, so a gesture landing mid nudge was arithmetically safe either way; this is so that what the reader sees under their finger is where the thumb actually is. This repo has already shipped a stale animation frame that moved the reader 216px mid transition.
- What counts as having used it, and why it is not a touch. The record goes in localStorage under nh_lens_used, the way the ducks gate on /games/ remembers a cleared run, and it is written when the page actually leaves the person state, not when a finger lands on the control. The control is a full width bar in the middle of the hero and most of what lands on it is a scroll going past; recording those would mean a visitor who has never used the lens never sees the attract again. Verified: a second visit in the same profile runs nothing.
- Reduced motion gets the signal without the movement: no nudge, no pulse, and the ring simply on, held until the control is used rather than timed out, because there is no animation here whose end could take it away. Measured: no animation on the thumb or its pseudo-element, the transform none at every sample, the ring at opacity 1, and the label over it at 7.68:1 at 360px and 390px and 9.51:1 at 1280px. The attract swept across its own travel with the glow held full on is 7.30:1 at worst.
- The observer stays connected until the attract actually fires, so a reader who scrolls past the control inside the second before it starts gets it when they come back rather than having spent it off screen.
- FOUND BY THE REVIEW, AND THE ONE REAL BUG IN IT: the attract could be spent entirely behind the open mobile nav. Open the burger within the first second and the page is translated 283px to the left, so the whole control including the thumb is off screen and the frame is pointer-events:none. Both runs played out unseen and uncancellable, and then it was finished for good. The observer cannot catch this and that is the interesting part: instrumented, it reported the control still 86 per cent intersecting while its own rectangle read x -283, because the shell's transform is composited and the intersection is computed against the untransformed box. So the nav is asked directly at fire time, and asked again every 700ms rather than given up on. Measured after: with the nav open at 3.3 seconds the attract has not started, and it runs the moment the nav closes.
- Also from the review, and tightened rather than argued with: what counts as having used the control. It was only leaving the person state, which meant an arrow key pressed at the first stop, or a drag out and back, recorded nothing and nagged the reader again next visit. A key the slider acts on is now recorded whether or not it changes the value, because a keyboard has no gesture that could be mistaken for a scroll, and so is a drag that actually moved. A finger merely landing is still not recorded: it ends the attract for that page load, because nudging at a thumb somebody already has hold of is rude, and it leaves the next visit clean, because most of what lands on a full width bar in a hero is a scroll going past. Measured all four cases.
- One leak the review caught that nothing else would have: site.js injects its own div.frame-tabs into the reader pill's overlay, on every page that renders the pill, which is all of them. The channel shadow was written unscoped and therefore reached all thirty two. It carries the .frame-live prefix now, the same as the dividers, and /work/, /about/ and /super/rules/ read box-shadow none and no ::before, identical to the shipped build. A class named for one page's control was never only that page's control.
- And three claims in the comments that the review was right to call: "then never again" overstated what the code does, the meta description does not mention Melbourne, and 5.31:1 was not the worst case. All three are corrected above and in the source.
- Verified: 274 contrast measurements across every text class, all three states, both widths, worst pixel under the glyphs: zero below 4.5:1, lowest 4.57. No horizontal overflow at 360, 390 or 414px. With JavaScript off the frame is three tabs and a still picture as before, with no thumb, no channel, no dividers and no attract, and every link resolving. /book/ is untouched: its pill, its own thumb and its box-shadow are identical, and none of the new rules reach it.

## v1.24.0
- The primary nav is Work, Systems, Games, Book, Super, About, Are you AI?, Contact on all 32 pages. Writing and Challenges are demoted from the nav only: /writing/ and both essays are untouched and still in the footer on all 32 pages, in sitemap.xml and in llms.txt, and novlr is still in every footer. Contact was footer only and is now in the nav. Book and Super stay separate until there is a merged page to point at.
- Rebuilding the whole nav element rather than editing items silently fixed a defect: /about/ carried a duplicated Games anchor after Challenges, ten items where every other page had nine. That is the shape of the failure this repo already recorded, a nav edit that missed pages, and it had been live.
- aria-current on this site is SECTION level, not page level: the fourteen /super/ chapters and its appendices mark Super, the three /work/ case studies mark Work, and the two /writing/ essays marked Writing until this version. Matching on the page's own path would have looked right on the eight pages that are themselves a nav destination and on the six that sit in no nav section, and would have silently dropped the current state on the other eighteen. The rewrite marks the longest nav href that is a prefix of the page's own path. Sixteen pages carry the Super state, eighteen carry the state of a section whose landing page they are not, and four change. All four are correct: /contact/ gains it because it is now in the nav, and the three /writing/ pages lose it because Writing is not.
- 404.html has no header and no nav, and it does not link the site stylesheet, only a font one and an icon. It is the thirty third HTML file and it does not participate. Whether it should have a header at all is a different change.
- The nav is a reveal, not a drawer, because nothing on this site covers the page: the lens peels the page into its machine readings and the arcade cabinet becomes the game. The page slides left and scales back, and the nav is revealed behind it on the right with 56px of the page still on screen.
- div.shell wraps header.top, main and footer.site in the STATIC markup of all 32 pages, not at runtime. Re-parenting main would have been two lines of JavaScript and would have torn down and reloaded every iframe inside it, which on /games/ is the hero game and up to three cabinets. It ships in the markup instead, and with JavaScript off it is an ordinary div that does nothing.
- A transform makes the transformed element the containing block for every position:fixed DESCENDANT, which is the bug this repo has already shipped twice with filter. Verified at runtime on thirteen pages rather than assumed: every fixed layer on this site is a direct child of body, so the shell contains none of them. Two were not: button.sh-shrink and a.sh-bail on /games/ sat in the markup between </main> and the footer, at z-index 201, and they are the only way out of a full screen game. They moved below the footer in this version. The one fixed element that does live inside the shell is a game promoted to full screen by body.sh-zoomed. A pointer cannot reach the burger in that state, measured: elementFromPoint at the burger returns button.sh-shrink while a game is full screen. A keyboard can, which the review below caught, so the two states are now locked apart rather than merely unlikely to meet.
- What travels and what stays put, decided rather than discovered. The particle field stays put and keeps running: it is the room, not the page. It is not visible while the panel is open, which is a different thing and worth saying rather than claiming otherwise, because the panel is opaque over most of the screen and in the peek strip the hero band paints over it, the shell being a stacking context while it is transformed. Measured on the homepage at 390px: 20.8 per cent of pixels changing between two frames with the panel closed, 0.4 per cent in the strip with it open, 18.5 per cent again once it closes. Nothing is torn down and nothing needs putting back. The lens control steps out: it cannot travel, being fixed and outside the shell, so it is covered and made inert.
- BUG FOUND AND FIXED DURING THE BUILD: the scale has to happen about the middle of the VIEWPORT, not the middle of the shell. The shell is the whole document, nine thousand pixels tall on the homepage, and scaling that about its own centre pushes everything near the top of the page downward by eight per cent of its distance from that centre. Measured 357px at a scroll position of 400, which threw the sticky header and its own burger into the middle of the screen. Scroll is locked while the panel is open, so one reading taken as it opens stays correct until it closes.
- The 56px peek turns out to do a second job nobody designed it for, and it is worth recording because it is now load bearing. Every IntersectionObserver on this site judges against the viewport, and two of them unload iframes when their target leaves it: hero-game.js parks the hero game and arcade.js sweeps every cabinet, and arcade.js tests the left and right edges explicitly, not just the top and bottom. Because the page never fully leaves the viewport, none of them is starved. Measured with the panel open at 360, 390 and 414px: the shell's right edge lands at exactly 56px at every width, and the element closest to leaving still has 38px inside it. A full width slide would have unloaded a running game to show a menu.
- A guard against that was written, measured, and then deleted. The first measurement said the hero game's iframe went empty the moment the panel opened, so both observers were guarded. Re-measured with the game actually in view, it did not reproduce at all: the first reading was the scroll that set the test up, not the panel. hero-game.js and arcade.js are byte identical to v1.23.0.
- TWO MORE BUGS FOUND BY TESTING THE THING RATHER THAN READING IT. Tapping the burger a second time did not close the panel. The burger lives in the header, which is inside the shell, and the shell's own tap-to-close handler runs in the capture phase, so a second tap ran both in order: the shell closed the panel and the button's toggle then read the new state and opened it again. The button is exempt from that handler now. And two taps inside a single tick left it open, because the close ran immediately while the frame that adds the open class was still pending; that frame now checks the state it was queued for. Both are the interaction the brief names twice, and neither shows up by reading the code.
- The tap-to-close handler also stops propagation, so a tap on the strip of page still on screen closes the nav rather than following whatever link is under the reader's thumb. And a page restored from the back forward cache with the panel open closes it, because a reader pressing back asked for the page, not the menu.
- Progressive enhancement rather than a component. The markup ships the full visible nav, which is what a reader with JavaScript off gets. site.js builds the button and the panel and only THEN adds the class that hides the nav, in that order and in one statement, so the nav can never be hidden without a button to replace it. No checkbox hack. Measured with JavaScript disabled at 390px: eight links, visible, zero burgers, and every link resolving.
- Accessibility: aria-expanded on the button kept in sync, focus into the panel on open and back to the button on close, a real focus trap, and Escape. Measured: zero of eleven tabs escape the panel, shift-tab from the first row stays inside, and focus lands back on the burger. The Escape listener is in the capture phase and stops propagation, because arcade.js listens for Escape on window unconditionally and would otherwise sweep its iframes every time a reader dismissed the nav on /games/. Note that the reader pill overlay this borrows from has focus return and Escape but no focus trap and no aria-expanded; this is the better of the two patterns, and the pill is worth bringing up to it separately.
- Under prefers-reduced-motion the page is still set aside, because that is what makes this a reveal rather than a panel on top of the page, but it gets there with no transition, no depth and no stagger. Measured: the transform is identical from the first frame, the shell's transition is 0s, the row delay is 0s, every row is already opaque, and the transform is a pure translate with scale 1.
- The v1.7 mobile header, which wrapped the nav onto a second row and hid the brand, is now the no JavaScript fallback and moved from 720px to 700px. That changes one 20px band above the burger breakpoint, and it is the only thing above 700px that moved: 701px to 720px went from a 51px header with no brand to the standard 65px header with the brand, which is what the header has been from 721px up since v1.7. Leaving the old block in place would have left a 20px window with no wordmark and no burger. Everywhere else above 700px is unchanged: header geometry identical at 760, 900 and 1280px, and zero differing pixels below the header on /work/, /games/ and /super/rules/, which is also the proof that the shell wrapper changed nothing.
- His name is back on mobile. .brand had been display:none below 720px since v1.7 because the nav needed the room. With a burger the header is nearly empty, and a portfolio whose author is invisible on every mobile page is the wrong trade.
- The hero game on /games/ subtracted 92px for a two row mobile header that no longer exists below 700px, which left a 27px band of ground under it. It subtracts 65px there now. The 721px to 760px band keeps 92px: that has been wrong since v1.17, for the same reason, and fixing it is a change above the burger breakpoint that belongs in its own version.
- The adversarial review found two things that survived being argued with, and both were real. On the homepage, one tap on the strip of page still on screen ran the lens the whole way from the person state to the AI state on its way out: the rule meant to make the control inert names .lens-ctl, which does not exist on the homepage, because lens.js adopts the reader frame as the control there and that is markup inside main inside the shell, with the bottom of its track landing inside the 56px peek. The guard was also a click listener, and the lens drives itself from pointerdown, which fires first. Both halves are fixed: .frame joins the inert list, and the dismissal moved to pointerdown with the click swallowed behind it. Measured after: --lens stays at 0 and the body class list is unchanged.
- And the claim that a full screen arcade game and the nav cannot coexist was a pointer argument, not a keyboard one. Covering the burger with a z-index 200 overlay stops a tap; tabbing backwards out of the game frame reaches it in six. Opened that way, .sh-phone.sh-big, which is the one position:fixed element that does live inside the shell, measured 359 by 5107px at left -303 instead of 390 by 844 at 0,0: the transformed shell had become its containing block and inset:0 resolved against the whole document. That is precisely the bug the shell exists to prevent, found in the shell's own exception. The burger now leaves the tab order while a game is full screen and setOpen refuses outright. Measured after: .sh-big is 390 by 844 at 0,0.
- Also from the review, and all fixed: the multi line comment introducing div.shell was pushing the colophon's own live proof off the page, because site.js prints the first 26 lines of that page's source and the comment took eight of them; it is one line now, with the reasoning where it belongs in the stylesheet. The burger took the dark ground colour on /games/, where body.arcade darkens main and keeps the sitewide light header. The hero game's 65px offset was not scoped to .nav-js, so a reader with JavaScript off got it against a two row header. The matchMedia guard was min-width 701px against a CSS query of max-width 700px, leaving fractional widths between them in neither. Focus was dropped on the floor when crossing above the breakpoint with the panel open. And the justification written above the Escape handler was not true of arcade.js.
- Known and accepted: moving the two arcade controls below the footer puts the footer's links between a full screen game and its close button in the tab order. They cannot stay where they were, being position:fixed in the middle of the shell's run, and the alternative is worse. Worth its own look.
- Verified: all 32 pages at 360, 390 and 414px, not a sample, with the correct nav list, the burger present, the nav hidden, the brand shown, the header exactly 65px, eight panel rows each with its line, aria-current agreeing between the nav and the panel, and no horizontal overflow anywhere. Panel text contrast, worst pixel under the glyphs, is 6.03:1 at worst.

## v1.23.0
- The homepage is dark in every state now. It was paper in the person state and near black in the two machine states, and the particle field is invisible over paper, so two thirds of the effect only existed once you had already moved the control. The palette is the site's own: the warm near black of --panel, --panel-ink and --panel-dim that the hero, the footer and every dark panel already used, and the cold near black of /games/ at the other end. No second dark theme, the two the site already had joined up.
- Where the ground lives, and why it is the only place it can live. Not body: body does not create a stacking context, so its background paints with the in-flow block backgrounds, above the negative z-index fixed layers, and an opaque one hides the field completely. That is the v1.10.0 bug and it has now been rediscovered twice. Not a layer built by script: with JavaScript off there is no lens and no field, and the page is the person state, so the ground has to exist before anything runs. That leaves the root element, and index.html is the only page carrying class="home" on it.
- The palette step is gone, and so is the reason it existed. v1.21.0 introduced --gpal because the page swapped paper for near black: two colours that swap places have to pass through each other, and halfway through a drag the page was dark grey on dark grey, 1.08:1 on the section headings. Nothing swaps places any more. Both ends are light text on a near black ground and the only thing crossing is the temperature of both, so the palette moves continuously with the field instead of jumping past it, and the ground shift is part of the drag rather than an event in the middle of it.
- Does the peel still read as a transformation. Yes, and the ground was the loudest signal but also the crudest. What is left is the temperature crossing warm to cold at both ends of the palette, the grayscale filter draining every accent colour out of the content blocks, the field changing species from an amber cloud to a green lattice to a violet haze, the annotations arriving, and the AI state reflowing the whole document to monospace in one column with the card backgrounds dropped. Judged on the screenshots at both widths, the three states are further apart than they were, not closer, because the reader's attention now stays on what is happening to the content rather than on the page flashing.
- The reader frame goes dark with everything else rather than staying a white terminal on a dark page. Three reasons. It is the control, and a control that inverts its own colours halfway through being dragged draws attention to the wrong thing. A white slab about 1100px by 500px is the largest object on the page and would dominate the field, the headline and the hero at once. And keeping it white would have meant keeping the stepped colour swap for that one element, which is the thing this version exists to retire. It keeps a lifted ground and its own border, so it still reads as an instrument rather than as page.
- The field is no longer clipped to the hero. From v1.21.0 it was inset to the hero's live edges at rest and opened to the viewport as the lens moved, and that could only look like a feature while the rest of the page was paper: a field over paper is invisible, so confining it to the dark band was the same thing as showing it. With the page dark at every value, confining it would mean withholding it from nine tenths of the page. What is left of the spread is the cloud itself growing, which is scaled on the same value.
- Point size is per state, and the person state now wants the SMALLEST of the three, which is the reverse of v1.22.1. The spread scales the group from 1 to 1.35, so the person state's cloud covers 1.35 squared less screen for the same count: about 1.8 times the areal density of the other two, and its points are the most opaque at .95 against .75. Clipped to the hero none of that showed. Page wide it did, and at the size the clipped version needed it stopped reading as points and started reading as fog: 21.4 per cent of the viewport lit against Google's 3.3, and visibly a haze under the body copy. At 1.9 the person state reads 5.8 to 6.5 per cent lit at 390px and 7.3 at 1280px, against Google's 2.9 and 8.1.
- BUG FIX, pre-existing and found by the full re-measure: the veil's mask went stale whenever a reveal settled. The reveals start at translateY(14px) and take .55s, getClientRects reports the transformed position, and a transform does not change main's height so the ResizeObserver never fires. A mask built during that window put the band up to fourteen pixels below where the text ended up, and the element's own top then sat on the mask's ramp: 0.626 alpha at the top edge of a case study tag, 3.15:1 against a solid white field where the plateau gives 5.36. It rebuilds on transitionend now, filtered to transform and opacity, which is also every card that lifts on hover.
- BUG FIX, pre-existing since v1.22.1: the link destination annotation could sit in the gutter the veil opens for the field. It is appended inside its link, and one link on this page is right aligned and white-space:nowrap in a flex row, so the annotation widens that link's min-content and pushes it past the column edge: 1.92:1 at 390px in the Google state. Capping its width cannot fix it, because with the heading's own min-content taking 125px of a 350px column anything past about five characters overflows. It carries its own ground now, which is what .lens-alt beside it has always done, and works wherever it lands. Scoped to the homepage, because that is the only page with a field behind it.
- BUG FIX, pre-existing: the bento tile captions. A single gradient over the whole caption box is only about .38 opaque where the first row of glyphs sits, and on the photograph tile that left white capitals on a light image. Measured on v1.22.0: 2.17:1 at 390px and 3.11:1 at 1280px. The dark palette fixed three of the four tiles on its own; the scrim now reaches full strength just above where the text begins, which fixes the fourth.
- BUG FIX, pre-existing: the mask opened a gap it should not have. In the AI state the head material card drops its background entirely, by design, so its annotation blocks sit straight on the field, and two of them in a row leave about 30px between their text runs, 14px once the pad is off. That opened and the block's left rule landed in it at 1.45:1. The merge threshold is 20px now, which closes gaps that are only padding and margin between two parts of one card. Measured at ten against twenty, the field's lit area moves about half a percentage point and not consistently in one direction, which is the frame to frame variation of a drifting field.
- Defensive: --lens now has a plain declaration as well as its registered initial value. @property gives it 0, but @property is not everywhere, and with JavaScript off nothing writes --lens. On a browser without it every token would have fallen back to the light palette on top of the near black ground the root element paints. It costs one line.
- Contrast, worst pixel under the glyphs against the actual text colours, with the canvas replaced by a solid white plane and every transition and reveal frozen so nothing moves between the two shots. 277 measurements: every text class on the page, up to four instances of each, in all three states at 390px and 1280px, each one scrolled to the middle of the viewport and measured with only its own colour blanked. Zero below 4.5:1, lowest 4.57:1. Twelve held values of --lens from 0 to 2 at both widths, on both the swept and the held battery: zero failures.
- Four traps in the measurement itself, recorded because each one produced a confident wrong answer. Reading a foreground after blanking the page returns rgba(0,0,0,0) and fails all 224 surfaces. Two screenshots either side of a 260ms gap straddle the reveal transitions and difference two different layouts. text-decoration-color here is derived from --ink rather than currentColor, so blanking colour alone leaves every underline painted and a descender crossing its own underline scores as a failure against the text's own decoration. And a parent scored on its children's glyphs reports the child's colour against the parent's: a p containing only a solid orange button read 1.79:1 where the button itself reads 5.27. Only elements that draw their own words are measured now.
- One known exception, unchanged and deliberate: the giant wordmark in the footer is #231F17 on --panel, 1.15:1, aria-hidden and decorative, on every page of the site. The harness cannot even see it, because at that contrast the glyph difference never reaches the threshold that marks a pixel as text.
- BUG FIX found by the adversarial review, and the sharpest thing it has caught: removing the clip left a pause behind that had nothing left to hide. Since v1.21.0 an IntersectionObserver stopped the render loop once the hero left the viewport, which was safe only because the field was clipped to the hero and therefore invisible by then. With the clip gone the same observer stopped the loop while the field still covered the whole page. Measured at 390px in the person state, scrolled past the hero: zero pixels changed over 450ms with 20.3 per cent of the frame lit, which is a still photograph of a particle field. The pause is gone; the tab being hidden is the only one left, and the loop checks that every frame.
- Also from the review, and all fixed: the reader frame's border was #000 and its shadow black, both chosen for a white card on paper, and both invisible on the dark page at 1.19:1 and 1.10:1, which made this file's own claim that the frame keeps its own border untrue. The border is drawn from the ink now and crosses with everything else, and the shadow is gone, because on a dark ground an object is separated by a lighter edge rather than a darker one. The mask rebuild fired on every card hover, since those lift by 3px on a transform, and a rebuild walks every run of text in main; it is filtered to the reveals now, which are the transitions that actually move text. The book band painted --panel directly rather than through the tokens, so it stayed warm while the ground and the cards around it went cold. The link annotation's new ground printed a near black chip inside the solid orange button, which is the one link that already carries its own opaque ground, so buttons are excluded. Six comments still described the clip that had been deleted. And two contrast figures in a comment did not reproduce: selected text unpinned is 3.59:1, not 3.10, and pinned is 5.05:1, not 5.21.
- Verified: reduced motion and JavaScript off both render the dark page with the photograph in the hero, every contrast target passing, and neither builds a veil at all. Every other page renders pixel identically to v1.22.1, /book/ included, in all three of its states, which is what scoping the ground to a class on the homepage's own root element buys.
